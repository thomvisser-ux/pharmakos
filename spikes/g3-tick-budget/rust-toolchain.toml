# SPDX-FileCopyrightText: 2026 Pharmakos contributors
# SPDX-License-Identifier: GPL-3.0-or-later
#
# "Windows and Linux agree on the per-tick hash chain" is only a meaningful
# claim at one pinned compiler version, and a tick budget quoted without the
# compiler that produced it is not a measurement. Same pin as the other three
# spikes: the stable version current at day 0 of week 1 (2026-09-13).
[toolchain]
channel = "1.98.1"
components = ["rustfmt", "clippy"]
targets = []
profile = "minimal"
