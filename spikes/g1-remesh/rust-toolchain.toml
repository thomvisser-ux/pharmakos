# SPDX-FileCopyrightText: 2026 Pharmakos contributors
# SPDX-License-Identifier: GPL-3.0-or-later
#
# A GDExtension cdylib must match the ABI Godot itself was built with; on
# Windows that is MSVC. Pinned to the stable version current at day 0 of week 1.
[toolchain]
channel = "1.98.1"
components = ["rustfmt", "clippy"]
targets = []
profile = "minimal"
