# SPDX-FileCopyrightText: 2026 Pharmakos contributors
# SPDX-License-Identifier: GPL-3.0-or-later
#
# "Identical paths on every operating system" is only a meaningful claim at one
# pinned compiler version (spikes/README.md, "Running a spike"). Pinned to the
# stable version current at day 0 of week 1; MSVC ABI on Windows, to match
# CI's `windows-latest`.
[toolchain]
channel = "1.98.1"
components = ["rustfmt", "clippy"]
targets = []
profile = "minimal"
