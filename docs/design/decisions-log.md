# Decisions log (pre-spec interview)

Status: interview complete. Foundation committed, gated by Phase 0. Spec draft 1 published as airgap-spec.html; draft 2 comes next. See handoff.md.
Note: switchyard-spec.html is an old straw man (a Mindustry fork). It has been superseded; ignore it.

---

## 1. Decided

**Purpose and scope**
- **Purpose:** a playable game and an AI platform, with high spectacle.
- **Builder:** one developer plus AI coding agents.
- **Time model:** strategic. Turn/step and pausable modes at launch; continuous real-time is a long-term goal.
- **AI consumers:** LLM agents (MCP), RL agents (Gym/PettingZoo) and scripted bots, all on one core protocol.
- **Seats:** any seat can be a human or an AI; mixed matches are supported.

**World and content**
- **Voxels:** core identity, built in a sensible order. Terrain is destructible and height is capped.
- **Design sources:** distilled from Mindustry, Zero-K, Minecraft Legends, Timberborn and Dwarf Fortress into one coherent, original design.
- **First format:** sandbox/creative with a very basic attack/defend kit.

**Player presence and control**
- **Commander:** a weak, killable commander (details to come) whose main action is placing beacons.
- **Beacons and governors:** each beacon creates a sphere of influence run by a governor.
- **Default governor:** drafted mandates executed by a rudimentary built-in AI.
- **Key innovation:** full AI integration and customization. Any combination of automation, manual control, hotkeys, groupings, macros, scripts, LLM agents and RL policies is possible.

**Web access**
- Web spectating and replays in the browser. Playing requires the installed client.

**Sandbox attack/defend kit (v1)**
- Walls: basic + reinforced
- Turrets: autocannon (direct fire, beats raiders) + mortar (arcing shell you can dodge, area voxel damage)
- Raider drone + demolition charge (timed explosive that destroys a voxel sphere)
- Repair/reclaim drone + gate/drawbridge (first script-controllable block)
- Scout/sensor unit (cheap vision; basis for a Scout mandate)
- Shield/barrier block (absorbs projectiles; counters the mortar)

**Beacons**
- **Placement:** the first beacon goes anywhere in the spawn zone. Later beacons must be within the commander's range AND touch or overlap an existing own sphere.
- **Enemy overlap:** creates a contested zone where neither side can build or dig. Keeping units near an enemy beacon for N ticks captures it, and its sphere and structures flip to the captor.
- **Destroyed beacon:** governance collapses. Mandates suspend, and units revert to reflexes and retreat to the nearest friendly sphere. Structures remain but are no longer repaired and become unclaimed.
- **Claims in the sandbox:** no protections for now, so everything is attackable; mutual opt-in hostility is rejected. Protections (telegraphed attacks, newcomer shield, host rollback) will be revisited per game mode, since many modes are planned.

## 2. Foundation (not committed; target confidence 90–95% before committing)
Round-3 research is in progress: scripting runtime deep dive (user defers to the recommendation), Godot client and in-game editor UI, sim core and protocol verification, red-team of the full stack.
In-game editors (mandates/scripts/macros) are gameplay UI. Browser access stays spectating/replays only. Research recommends native Godot Controls for editors (GraphEdit is no longer experimental; CodeEdit), plus an external-IDE workflow for scripts with hot reload. No TypeScript editors and no webviews. Pending confirmation together with the "two languages" simplification.

Round-3 results:
- Component confidence is about 85–90% per area and rises to 90–95% if the spikes pass.
- Red team: project-success probability is about 40% as scoped and about 65% simplified. The risk is the number of languages and seams, not Rust or Godot.
- Scripting: wasmi 2.0, pinned exact version, `floats(false)`, fuel per tick, dirty-page snapshots between ticks only, command log is the source of truth with re-execution in CI.
- Crates: fixed / rkyv / postcard / xxh3 / imbl / rmcp 3.3 / prost + buf. Avoid bincode (unmaintained), cordic and hierarchical_pathfinding (stale).
- MCP: push digest reports, because CivBench shows agents under-monitor state. Keep to about 20–30 coarse tools.

Simplifications:
- DECIDED: web viewer is built after the native sandbox ships; native replays and in-game spectator mode until then.
- DECIDED: two languages at launch (Rust + GDScript). Editors use native Godot UI. All schemas are generated from one source. TypeScript arrives with the web viewer.
- DECIDED: the simple socket-based Gymnasium/PettingZoo wrapper comes first. Sim state is laid out for batching now; the fast PyO3 batched path is added only when measured throughput demands it.
- DECIDED (scripts): Rust SDK at launch, built for the AI-assisted development era. The game publishes and maintains API docs so coding agents (Claude Code, Codex, …) can work with the **locally installed game** rapidly and securely, for two purposes:
  1. Help write AI operator programs (scripts/controllers): SDK scaffolds, compile, dry-run under fuel, test against local scenarios.
  2. Act live as the player's customized AI controller, supplementing or replacing the built-in rudimentary governor.
  - Implications: the local game exposes a documented developer surface (a local MCP server + CLI + machine-readable docs such as llms.txt / generated schema docs). Security: localhost-only by default, per-controller tokens with scopes and max lease class, and no host filesystem or network access from scripts. A beginner in-game language can come later.
- DECIDED (math, delegated to the recommendation): integer-first.
  - Own `#[repr(transparent)]` unit newtypes (not an alias):
    - Pos Q16.16 (i32)
    - DistSq Q32.32 (i64; range checks compare r², no sqrt)
    - Angle u16 with a 4096-entry LUT trig
    - HP/resources as integer milli-units
    - water in 1/4096-voxel u32 units, with an exactly conserved flux
  - Floats are allowed only in a walled `sim::solve` module (basic ops + sqrt, quantized on return). Delete that module if the spikes allow.
  - Lints: deny float_arithmetic / as_conversions; disallowed f32/f64/HashMap/HashSet/Instant::now; overflow-checks on in release.
  - Mortar aim uses the discrete-integrator closed form.
  - Confidence: ~70% best, ~90% acceptable. Switch a kernel to restricted floats if it is >2.5× slower than f32.
- Arbitration: research done (it recommended per-unit hybrid leases). The user's decision below replaces most of it.
- DECIDED (control model):
  - **No manual control of any unit except the commander.** The commander is the only directly controlled unit.
  - Every other unit is controlled only through the mandate of its beacon's operator.
  - Beyond that, customization happens through the API: custom mandates and custom AI control.
  - **Changing a mandate costs control points.**
  - Consequences:
    - There are no per-unit manual leases, hand-back or takeover TTLs.
    - Arbitration becomes per-beacon (who may change mandates, what executes them) plus rules for moving units between beacons.
    - Hotkeys, groups and macros act on commander actions and mandate changes, not on units.
    - Reflexes (retreat, return fire) remain part of mandate execution, governed by rules of engagement (ROE) and always announced.
    - Still reusable from the research: stale-order counters (epochs) for late API commands, `Revoked`/`Rejected` events, and deterministic once-per-tick resolution.
- DECIDED (beacon control structure):
  - **Two layers per beacon.** The mandate is WHAT (goal + parameters). The operator is HOW: it executes the mandate every tick. The default operator is the built-in rudimentary AI, replaceable by custom programs.
  - **No control points.** They were removed entirely. The cost of change is **physical**: the commander must travel to a beacon and interface with it to change its mandate or make any other beacon change. Valuable commander transit time and interaction time are the currency.
  - **Future beacon changes** include installing custom operator programs. Programs can be pre-developed, developed in real time, or both, presumably with LLM assistance.
  - **External AI (LLM via local API)** acts as strategist, i.e. it pilots the commander and reads reports. It can also act as a live operator, but only in turn or pausable modes where the sim waits.
  - **Units are bound to a home beacon**, the one that produced them or an explicit reassignment. They follow that beacon's mandate, even outside its sphere.
  - **Operators and beacons can communicate** with each other through API customization, by design.
- DECIDED (beacon interaction rules):
  - **Comms:** operators exchange data messages (threats, requests, shared targets) and adapt within their installed program and current mandate. Messages can NEVER reconfigure a beacon. Mandate changes and program installs always require the commander on site.
  - **Remote info:** the seat receives all reports and all fog-limited vision from its beacons and units, anywhere. Only changes need the commander present.
  - **Interfacing:** takes time scaled to the size of the change (roughly: settings tweak ~3 s, mandate switch ~8 s, program install ~10–30 s by size; exact values TBD). The commander must stay in range and still. Damage interrupts the interface, and the beacon keeps its old configuration.
  - **Commander (DECIDED):**
    - Mobility: one fixed-speed mobile body (e.g. a hover-walker that climbs 1-voxel steps). Terrain, roads, ramps and bridges shape routes; upgrades come later.
    - ROADMAP: late-game vehicles with automatically firing defensive weapons. How they're earned is TBD, since the economy hasn't been designed.
    - Death: respawn at core/first beacon after a delay (e.g. 30 s, growing with repeated deaths). Beacons keep running their current mandates and operators, but can't be changed until the commander returns.
    - Remote changes: none. Strictly on-site.
    - ROADMAP only (NOT in the prototype): weak self-defense weapon, carry and place a demolition charge, minimal construct/dig, sensor ping/scan.
  - **Programs:** written and compiled outside the sim at any time (e.g. by Claude Code through the local game API) and stored in the seat's program library. Installing one requires the commander at the beacon. A live LLM operator is an "uplink" program installed the same way; after installation the LLM drives that operator remotely (turn/pausable modes only).

Leading candidate: an engine-free Rust sim with a Godot 4.x client. The stress test gave it about 75% confidence and proposed these adjustments:
- **Sim internals:** integer/fixed-point gameplay math; plain SoA tables instead of a general ECS; copy-on-write chunks for cheap forking.
- **Portability:** WASM-capable sim from day one, so replays can play in the browser.
- **Rendering:** our own Rust mesher, shared by Godot (via gdext) and the web viewer. Voxel Tools becomes optional.
- **Protocol:** Protobuf schema-first. MCP tool schemas are generated from it; bulk chunk data uses a separate binary channel.
- **Networking:** server-authoritative snapshot/delta, not lockstep, because fog of war must stay on the server.
- **Trial build:** a 3–4 week spike with pass/fail gates. The gates: at least 2k ticks/s; identical replay hashes across platforms; chunk remesh of 4 ms or less; 60 fps with 20 explosions/s; an MCP agent completes a defend scenario.

A final research pass is pending before committing (see §5).

---

## 2.0 LATEST SCOPE DECISIONS (take precedence over anything earlier in this file)
- **v1 ships BUILT-IN operators only.** Beacons run the built-in operator, which follows its mandate and settings.
  - Custom components (custom operator programs, custom reprogramming, custom quartermaster, WASM commander) arrive one per version afterwards, in an order still TBD.
  - Consequences:
    - The wasmi/WASM scripting work and its spikes (G5) move out of v1 and Phase 0 into a later milestone. Its research stays valid for that milestone.
    - AI seats in v1 act through gameplans, mandates and settings, beacon placement and recycling, Quartermaster priority knobs, and voting, all via the API/MCP.
    - The v1 core is the gameplan/API/editor co-design.
- **Radio connectivity v1 = "Mast only":**
  - Radio masts, with range growing with height.
  - No line-of-sight occlusion, no relays, no jamming yet.
  - Chosen as simple and extensible to Horizon/Spectrum later without backtracking. The design must leave room for LOS, relays, jamming, direction finding and satellite.
- **Destroyed capability structure:** the capability is suspended until the structure is rebuilt. Timed weapons keep counting down meanwhile.
- **LORE / THEME:** Terminator-style (an original IP, no borrowed names). Humanity working with AI fights an adversarial AI that works with (collaborating) humanity. Both sides are human+AI alliances, which mirrors the game's human/AI seats. Rejected: "The Hush" proposal; its anti-hacking premise may still be reused.

- **Segment length (DECIDED):** can start smaller, but longer is better, up to 10 minutes. Default: segments grow with match phase from roughly 3 min early to a 10 min cap. Host-configurable. This supersedes the flat 8 min default.
- **Voting: DEFERRED** (the whole feature, including pity votes and voted capabilities). v1 catch-up = rank multiplier on awards + small BMI scaling by standing.
- **v1 capability structures (DECIDED):**
  - Radio Mast (messaging; range grows with height)
  - Sensor Spire (timed reveal pulse with cooldown)
  - Resonance Spire (beacon radius: a crystal lattice grows over about 60 s with an aurora tether; the dome collapses on camera if the spire is destroyed)
  - Deferred: Command Bunker, Vehicle Bay and the rest of the catalogue.
- **Lore research returned:** three original frames, all built on "sealed orders" (radio silence during play explains the movie segments) and a neutral paymaster machine that pays BMI, awards and shames and runs the verifier ("seal inspection"):
  - A **Handfast** (grim; recommended): Quorum vs Handfast Line; Keybearer / Stake / Steward / Signal spar; mandates Raise / Hold / Break / Delve; Scrip; the Lull / the Push; the Ledger.
  - B **Brass Broadcast** (retro pulp): Calculus vs Sparkwright Union.
  - C **Hands-On Mutual** (corporate satire): Ownself Holdings vs Hands-On Mutual, wet signature.
  - Names to avoid are listed in the research (Skynet, Resistance, Sentinels, GAIA, Cylon, etc.).
  - A trademark clearance search is needed before finalizing names.
- **LORE (DECIDED):** Handfast world and tone (option A), with Hands-On Mutual's corporate-bureaucratic humour (option C) in the flavour text for Citations (shames), awards and the Ledger. Names remain working names until a trademark check.
- **RL (DECIDED):** deferred past v1, keeping the door open. v1 still builds the fast headless deterministic sim, command-log replays and the operator seam, so a Gymnasium/PettingZoo interface can arrive later with custom operators. PettingZoo api_test is removed from Phase 0.

- **GAMEPLAN/API DESIGN** is in design-gameplan-api.md: 27 MCP tools, a verifier with ~40 diagnostic codes plus JSON Patch fixes, a map-timeline and rule-list editor, a planner, and worked examples. Decisions from reviewing it:
  - **Recycling:** an on-site gameplan change (about 10 s interface) with the 50% refund at commit. This supersedes "between rounds, instant".
  - **Radio Mast = the Messaging capability structure.** Earning Messaging unlocks it. Building it activates messaging within its range. Destroying it suspends messaging.
  - **Technical defaults accepted:**
    - 20 Hz tick
    - interface-time table: handshake 1.5 s, priority knob 1.5 s, settings 2 s + 0.5 s per extra field, mandate switch 8 s, recycle 10 s, beacon placement 12 s
    - at most 6 changes per visit, committed one at a time
    - Attack reach ≤ 96 voxels outside the sphere
    - Protobuf as the single schema source, with an in-house generator for LLM-friendly JSON Schema (Buf plugin as fallback)
  - **Accepted:** the beginner rule list shows 4 slots, experts get 12.
  - **Rejected:** a "reuse last plan" shortcut. **Every round should be completely different from the last.** A timeout files the safe plan only.
  - **Ready status (DECIDED):** seats see **who** is ready during the planning pause.
  - **No-fog camera during planning (DECIDED):** a host setting.

## 2.1 Re-scoped review (built-ins-only v1)
- **Confidence:**
  - Foundation: 86% now, 93% after Phase 0.
  - Shipping sandbox v1: 70% now, 78% after Phase 0.
  - Separate playtest risk: is 8 minutes of play with no control fun?
- **Phase 0 (~11 calendar weeks):**
  - G5 (WASM) is deferred.
  - Kept: G1 remesh; G2 pathing + P3 travel-time estimates (±15% p90); G3′ 8-minute segment budget with the full kit; G4 determinism + fork.
  - Added:
    - P1 verifier ≤50 ms, deterministic, fuzzed
    - P2 built-in planner (safe plan ≤200 ms)
    - P5 MCP planning session (median ≤3 min, ≤25 tool calls)
    - P6 GDScript editor productivity
    - P7 mast connectivity ≤1 ms
- **Milestones after Phase 0 (~48 weeks; plan 16–17 months from today):**
  1. Sim core, 8 weeks
  2. Units, pathing/ETA, built-in operators, Quartermaster, masts, 9 weeks
  3. Gameplan, verifier, planner, ending in a vertical slice, 6 weeks
  4. API/MCP, 4 weeks
  5. Godot client and editor, 10 weeks. Overlaps milestone 3.
  6. Economy layer, 5 weeks
  7. Hardening, 6 weeks
- **Tech adjustments:**
  - Remove wasmi from v1.
  - Plan model, verifier and travel-time queries in Rust, reached through gdext. GDScript is for views only.
  - Operators are native Rust in v1. This supersedes "operators are WASM only".
  - Verifier and planner output must be deterministic.
  - Cross-OS determinism protects only replays and CI.
  - Water units deleted.
  - Check whether gameplan JSON Schema should come from Rust (schemars) rather than Protobuf.
- **Minimum seam kept for later WASM:**
  - `Operator` trait: beacon-local view + mailbox + mandate in, intents out, no global state
  - abstract per-tick work counter (becomes fuel later)
  - `program_id` in beacon config
  - `kind`/`version` tag on the gameplan envelope
  - `Quartermaster` trait
- **SUPERSEDED by 2.0/2.1:**
  - 2a "default AI as WASM"
  - 2a WASM-based "qualifying" definition. The v1 definition becomes schema check + semantic check + upkeep warning + content hash.
  - Message fuel charging
  - Install time from module bytes
  - Firewall Vault (fuel) in v1
  - "WASM-capable sim from day one", now deferred along with the web viewer
- **Open contradictions to interview:**
  1. Can the verifier simulate anything at all under "no dry runs" (travel-time estimates)?
  2. Messaging has no sender in v1. Can a gameplan send? Does mast connectivity gate only operator messages, or also remote reports?
  3. Voting needs 4+ players while v1 allows 1–4 seats, so catch-up is weaker in 2–3 seat matches.
  4. Mandate fields budget/priority/override are obsolete; Mine should deliver to beacon storage.
  5. Segment length of 8 minutes vs about 3 minutes.
  6. Commander-death fallback required in gameplans.
  7. RL shape in v1: gameplan-per-segment actions, operator step mode, or defer RL.
  8. Which capability structures ship in v1 (only the Mast is decided).
  9. How AI seats agree to a speed-up.
  10. Ready-status leakage.
  11. Tick rate (assume 20 Hz).

## 2a. Decisions after the final review
- **LORE:** a commander must physically contact a beacon to change it because this prevents remote hacking. That is the in-world reason for on-site-only changes, and for messages carrying data only.
- **LLM participation (replaces the "live uplink operator" idea):**
  - The LLM acts during timed turn-mode pauses (default 5 min).
  - Before the timer runs out, it must upload "safe/qualifying" scripts:
    - a commander gameplan script (drives the commander during play)
    - any per-beacon operator script changes, which the commander then installs on site
  - Scripts can be pre-verified or verified on the fly.
  - The verification mechanism is part of the public API, so the LLM can run it itself first. On-the-fly verification is then just a formality.
  - Proposed technical definition of "qualifying":
    - compiles to an allowed ABI version
    - only allowed imports; floats disabled
    - within memory and module-size caps
    - passes a sandboxed dry run under the fuel cap on a verification scenario
    - emits only mandate-contract-valid intents
    - content hash recorded
  - The same verifier runs locally through the API and at upload.
- **Default AI:** implemented as a WASM operator through the same public interface and per-tick fuel budget as custom programs. It doubles as SDK dogfooding and gives players a starting point to fork.
- **Operator perception:** beacon-local (its own sphere and units) plus messages from other operators.
  - Open: should the default built-in operator be able to message other operators at all? Messaging capability might be EARNABLE and a core part of the meta. Decide in the economy/progression interview.
- **v1 scope:**
  - No flowing water (roadmapped).
  - Local host with 1–4 seats (human and/or AI); no internet multiplayer or dedicated servers.
  - "Streamable" means a native spectator camera with a no-fog toggle, captured via OBS; the web viewer comes later.

- **MATCH FLOW (DECIDED; a major shift):**
  - Matches alternate play segments and planning pauses. There is **more game time than planning time**.
  - During play the sim runs **like a movie**, with **no manual control**. Every seat's commander, human or AI, follows its uploaded gameplan. People chat and enjoy the action.
  - Planning pauses are shared by all seats (default max 5 min), and play resumes early when everyone is ready.
  - Humans need a **real method to script or plan the commander** during the pause. This is required in v1.
  - ROADMAP: manual control, where a human takes full control of their commander for a round.
  - Human seats use the same gameplan API and verifier as AI seats.
  - **Timeout or failed verification:** keep the last valid configuration, and a **default "safe" plan is filed on the seat's behalf**. It comes from the **same mechanism built for computer-opponent control** (the built-in planner).
  - Implications:
    - There is perfect parity between human and AI seats during play.
    - The built-in AI has two roles: the per-beacon default operator, and the commander planner that generates default and safe gameplans. A computer opponent is simply that planner plus the default operators.
    - Adapting mid-segment is only possible through the gameplan's and operators' conditional logic, so how expressive a gameplan can be becomes central.
    - Step mode remains for headless RL training.
  - **Pacing (DECIDED):** default 8 min play / ≤5 min planning, with ready-up. Both are host-configurable.
  - **During play (v1):**
    - chat
    - free or auto-director spectator camera
    - live event timeline plus a highlight replay/scrub after each segment
    - speed controls in local matches (2–4× or skip, if all seats agree)
    - The user notes "lots of room for imagination here"; keep a roadmap list of watch-mode features.
    - Not chosen for v1: revealing opponents' plans after a segment.
  - **Planning information:** each seat plans only from its own fog-of-war view.
  - **GAMEPLAN (DECIDED):**
    - **Format:** typed declarative data plan in v1 (steps, triggers, fallbacks, bounded loops, every wait has a timeout; verifiable; LLM-producible via JSON Schema). A WASM commander program comes later under the same outer format.
    - **Human authoring in v1:** map timeline + gambit-style rule list + templates.
      - **Spend lots of effort here.** It is critical to the detailed API design: API design and editor design must feed each other (co-design).
    - **No dry runs.** During play, units and buildings follow their most recent mandate instructions.
      - Re-programming them to operate independently is a capability that is earned and activated.
      - **ROADMAP:** the custom reprogramming capability (it needs more custom script creation and verification). Not in the initial deployment.
    - **Plan information:** other seats learn nothing about your plans, ever. No reveal after segments.
  - **Research notes (earlier), for reference:** gambit list + map timeline + templates + JSON share string; limits of 64 steps / depth 6 / bounded loops / forward jumps only; verifier = schema + semantic checks, diagnostics in rustc-JSON style via MCP; planner = templates + utility scoring, with bounded fork rollouts for Hard. Tension still open: the research suggested ~3 min segments vs the 8 min chosen.
    - Research recommends: typed declarative data plan in v1, edited via a gambit list, a map timeline, templates, and a JSON share string. WASM commander in v2 under the same schema.
    - Limits: 64 steps, depth 6, bounded loops, forward jumps only, every wait has a timeout.
    - Two-stage verifier: schema + semantic checks, then an advisory dry run on the seat's own knowledge. Diagnostics in rustc-JSON style, exposed via MCP.
    - Planner: templates + utility scoring, with bounded fork rollouts for Hard difficulty.
    - Flagged tensions: research suggests ~3 min segments or scaling pauses, vs the 8 min chosen; AI dry-run advantage (quota?); ready-status leakage; plan reveal.

- **ECONOMY (DECIDED so far; "simpler is better"):**
  - One currency: **$**. No metal/energy.
  - Each beacon consumes $ (upkeep). Building anything costs $.
  - Income per round:
    - "BMI" (meaning to be confirmed)
    - plus creative per-round individual **awards**
    - minus creative per-round individual **"shame"** penalties for lame behavior
  - **Mined $** is the other route to extra spending. Mining drones physically deliver it to beacon storage.
  - Units come from fabricators built inside spheres (Build mandate), are bound to that beacon, and cost $.
  - **Capabilities** (e.g. messaging, vehicles, beacon tiers):
    - EARNED by matching triggers
    - also VOTED on by users between rounds
    - occasionally awarded at RANDOM
    - Matching the right trigger to the right capability matters a lot for the meta and for fun.
  - **Open:** spend control, i.e. how spending is prioritized among operators and budgeted between rounds. The user asked for a simple, elegant solution.
  - **BMI = Basic Minimum Income** (DECIDED): a flat $ amount per seat per round.
  - **Unspent $** sweeps back to the treasury at round end (DECIDED).
  - **Mined $** goes into the mining beacon's own spendable pool for that round, then sweeps (DECIDED).
  - **Spend control requirements (DECIDED):**
    - Envelopes may be uninteresting complexity; investigate simpler options.
    - **Accounting is not fun.**
    - Spend control must be customizable within operator scripts AND ship with a reasonably effective built-in default.
    - A bankrupt beacon must be a willful mistake or a deliberate strategy, never a frequent oversight.
  - **SPEND CONTROL (DECIDED):**
    - A built-in **Quartermaster** auto-budget, with no envelopes to manage.
      - It reserves next round's upkeep for every beacon first.
      - It then fills operator spend requests by urgency: defend under attack, then repair, units, build, mine.
      - Fair-share caps stop one beacon hogging funds.
      - One optional knob per beacon: low / normal / high priority.
    - Bankruptcy safeguards (all adopted):
      - upkeep always reserved first
      - the verifier warns on plans whose upkeep isn't covered, which need an explicit "allow dormant beacons" to run (same for AI seats)
      - an unfunded beacon goes dormant (operator paused, structures stay, capture slower) and revives when funded
      - a plain-language low-funds alert in the recap
    - **Beacon recycling:** beacons can be strategically recycled between rounds for an instant refund of 50% of build cost.
    - **Custom quartermaster: ROADMAP.** When built, it will be pre-installed via the offline verifier before a match and can't be modified in-game except to switch back to the default. For now, focus on the default. (The user doubts there is much fun in the minutiae of spend automation.)
  - **AWARDS AND CATCH-UP (DECIDED):**
    - **Award cap:** total awards ≤ 50% of BMI per seat per round, with at most 3 awards per seat per round.
    - **Catch-up levers:**
      1. A rank multiplier on awards, so trailing seats earn more from the same award.
      2. **BMI scaled by standing.** Sandbagging risk; mitigation still to be decided.
      3. **Random "pity votes":** between rounds, a ballot sometimes offers a big cash bump for the last-place seat.
    - Not chosen: underdog-only awards; a supply-drop pity timer.
    - **Beginner hand-up:** no separate rookie handicap. The catch-up levers above cover beginners.
    - **Shames:** a small $ penalty (≤25% of BMI each), floor at zero, shown as a funny title in the recap. A seat's first timeout per match is free. Same rules for AI seats.
    - **Standing:** smoothed net worth (treasury + beacons + structures + units, averaged over the last 2 rounds).
    - **BMI scaling by standing:** small but not insignificant. It must not encourage sandbagging or discourage winning. Proposed tuning: last place +10%, leader −5%, linear in between. Validate in playtests.
    - **Pity vote:** appears at random (seeded) about 1 in 4 pauses, never twice in a row, AND only when the gap is large. Proposed threshold: last place's standing is below 50% of the leader's. Payout 1× BMI to last place.
    - **Voters:** seats only for now (spectator voting to be revisited). AI seats vote via the API.
    - **Default operator messaging:** receive-only until messaging is earned. Once earned, default operators also send.
    - **Voting:** happens only in matches with 4+ players. A tie counts as a failed vote (for now).
  - **CAPABILITIES (DECIDED so far):**
    - Duration: tools (messaging, dig/construct, etc.) last the whole match; weapons and combat boosts (vehicle auto-weapons, carrying a charge, extra fuel) are timed, 2–3 rounds.
    - **Every capability award is enabled by placing a voxel building.** Capabilities are physical structures in the world, not abstract unlocks.
    - **Avoid simplistic, boring upgrades** such as +damage or +radius. A beacon radius tier is acceptable only if it's graphically impressive or tied into the story.
    - Messaging depth is open and being researched:
      - a radio antenna, ideally placed at elevation?
      - radio jamming?
      - satellite?
      - how much connectivity at all?
    - How often capabilities are awarded at random: DEFERRED (meta detail).
    - **Research returned** (sources written from memory, not verified live):
      - **Connectivity model, "Horizon" (medium depth), recommended for v1:**
        - radio masts whose range grows with isqrt of height
        - line-of-sight blocked by voxels (integer DDA, cached per chunk version)
        - relay pylons
        - jammer dome as a timed weapon
        - link-snap effects mid-movie
        - union-find graph in observations
      - **Roadmap:** direction finding (v1.1), satellite uplink with scheduled passes (v1.2), per-hop latency later, never remote reconfiguration.
      - **15 capability structures:** Radio Mast, Relay Pylon, Jammer, Uplink Dish, DF Loop, Sensor Spire, Resonance Spire (beacon radius, growing crystal lattice + aurora tether that collapses when cut), Vehicle Bay, Excavator Rig, Charge Armory, Transit Rail, Command Bunker, Decoy Beacon, Firewall Vault (operator fuel), Drone Hangar.
      - **Lore proposal "The Hush":** the Whisper intrusion rode radio waves and rewrote machines. Beacons = Lodestones keyed only by a living hand. Operators = Wardens (may speak on the air, never obey it). Commanders = Couriers.
  - **Awards/shames/capabilities/voting research returned** (not yet interviewed; sources were not fetched live, verify later):
    - 12 awards + 6 shames, deterministic from sim counters
    - awards capped at 0.5×BMI, rank multipliers favour trailing seats
    - a capability trigger map
    - rotating ballots, weighted 50/50 seats vs spectators
    - supply drops with a pity timer and a rank rubber band
    - default operator can receive messages but not send them until messaging is earned
  - **ECONOMY PRINCIPLE (DECIDED):** reward good gameplay, but also give trailing players a way to catch up and beginners a hand-up. Awards, shames, base income, voting and random drops must all be tuned together to meet this.

## 2b. Final review after the control redesign
- **Scores:** foundation-correctness confidence is about 82% now, about 92% if all Phase-0 spike thresholds pass. Probability of shipping sandbox v1 is about 68% now, about 75% after Phase 0.
- **Verdict:** 90–95% can only be reached by measurement, not more research. Commit gated by Phase 0 (8–9 weeks of spikes G1–G6).
- **Spikes:**
  - G1: destruction remesh within 3 frames at 60 fps
  - G2: pathing churn repath p99 ≤5 ms
  - G3: tick p99 ≤25 ms with 300 units
  - G4: identical hashes across 3 OSes, plus fork
  - G5: 30 WASM operators ≤8 ms/tick; an agent writes an operator from llms.txt alone
  - G6: LLM-via-MCP turn-mode scenario; PettingZoo api_test
- **Technical defaults adopted** (technical decisions delegated to the recommendations; the user can veto):
  - Operator messages are delivered at tick+1, ordered by (sender beacon id, seq), bounded in size, charged against fuel, same team only.
  - Install duration comes from deterministic inputs (base + k × module bytes, or the mandate diff), and programs are content-addressed by hash and stored with replays. IDE hot reload updates only the seat library, never installed instances.
  - The float `solve` module never feeds persistent state unless G4 proves bit-identical output; otherwise it's presentation only.
  - Q16.16 positions cap the world at ±32,768 voxels, which is far above planned map sizes.
  - GDScript covers UI/HUD only; operators are WASM only.
- **Conflicting facts to verify in Phase 0:** rmcp latest version (3.3.0 vs 3.1.1 reported); whether GraphEdit is still marked experimental in 4.7 (reports conflict). Neither is load-bearing.
- **Milestones after Phase 0** (about 12 months, plan 14–15 with buffer):
  1. Sim core, 8 wk
  2. Units + pathing + default AI, 10 wk
  3. API (protobuf/MCP/llms.txt/PettingZoo), 5 wk
  4. WASM operators + library + install + uplink, 6 wk
  5. Client + spectacle, 8 wk
  6. Hardening + release, 5 wk
- **Major design gap:** the ECONOMY hasn't been discussed (resources, production, how units, beacons and vehicles are earned).

## 3. Drafted mandates (defaults, v0)
A mandate is data: `{type, sphere, params, budget, priority, rules_of_engagement, report}`. Its default executor is the built-in governor, a rudimentary utility AI or behaviour tree. Any other controller can replace or override it.

**Fields every mandate has**
- **budget:** resource cap per period, plus a drone/unit allocation.
- **priority:** used when mandates compete for shared drones or resources.
- **report:** which events to surface, such as "blocked", "under attack", "quota met" or "budget exhausted".
- **override policy:** what manual or other controllers may pre-empt.

### Build
Construct and maintain structures inside the sphere.
- **Params:**
  - `targets`: one or more blueprints/placements
  - `order`: priority order
  - `repair_threshold`: e.g. 60% HP
  - `rebuild_destroyed`: on/off
  - `terraform_allowed`: none / fill / dig / both
  - `protected_zones`: areas it must not modify
- **Default behaviour:** pick the highest-priority unbuilt or damaged target it can afford, then assign the nearest idle drone.

### Defend
Hold the sphere and protect listed assets.
- **Params:**
  - `protect`: structures or areas; defaults to the beacon and everything inside
  - `engagement_radius`: at most the sphere radius
  - `ROE`: hold fire / return fire / engage on sight
  - `retreat_hp`: e.g. 30%
  - `pursue`: never / to the sphere edge / limited distance
  - `auto_fortify`: request walls and turrets from a Build mandate on/off
- **Default behaviour:** threat-weighted targeting; units stay inside the engagement radius; damaged units fall back toward repair.

### Attack
Project force against a target.
- **Params:**
  - `target`: structure, beacon, area or unit group
  - `staging_point`: inside the sphere
  - `launch_condition`: force size, timer or manual go
  - `force_composition`: minimums
  - `retreat_condition`: losses %, target destroyed or timeout
  - `avoid`: zones to avoid
  - `breach_allowed`: demolition/digging on/off
- **Default behaviour:** gather at the staging point, then launch when the condition is met, path around avoid zones, focus the weak point, then retreat or regroup.
- **Open:** whether an Attack mandate may act outside its sphere, and how far.

### Mine
Extract resources inside the sphere.
- **Params:**
  - `resource_priority`: ordered list
  - `quota`: target rate per period, or stockpile cap
  - `deliver_to`: core, storage or logistics link
  - `dig_rules`: max depth; no digging under structures; keep a support pillar every N tiles
  - `flee_on_threat`: on/off
- **Default behaviour:** nearest reachable highest-priority ore; idle when the quota is met; flee to the beacon when threatened.

**Open:** later mandates such as Scout, Logistics/Haul, Terraform, Research/Produce, Salvage/Reclaim, Patrol.

---

## 4. Extrapolation: what the decisions imply

### A. A layered control stack
Seat (commander) → beacon governors → units/drones. Every layer is a **controller slot**, and every controller emits the same typed commands.

**Controller types**
- Manual (UI)
- Built-in governor
- Script (sandboxed, in-sim)
- External agent (LLM over MCP, RL policy or bot over the protocol)
- Client-side conveniences: hotkeys, control groups and macros compile down to commands or mandate edits

**Required composition rules** (new spec area)
- **Authority/precedence:** e.g. manual > macro > script/agent > built-in governor.
- **Unit leases/locks:** which controller currently owns a unit.
- **Conflicts:** how conflicting orders are resolved.
- **Hand-off:** whether control can be temporary ("take over for 30 s, then return to the governor").

### B. Where controllers run
- **In-sim:** deterministic, tick-budgeted and sandboxed (e.g. WASM with fuel). Fast enough for RL and replayable by re-execution.
- **Out-of-sim:** external controllers over the protocol. Their commands go into the log, so replays stay deterministic.

This choice adds a new foundation question: which deterministic sandboxed script runtime to use.

### C. Mandates bridge the latency gap
LLMs act at the mandate level: set, tune and react to reports. Built-in governors, scripts and RL act at the unit level every tick. So slow agents stay competitive, and continuous real-time later becomes feasible: the LLM steers while governors execute.

### D. Beacons do several jobs
- **Claim:** a build zone owned by a seat, which solves griefing in the shared sandbox (non-destructible to others by default; arenas are opt-in).
- **Scope of governance and of observation:** a sphere-local view gives natural token budgets for LLMs and fixed-size egocentric tensors for RL.
- **Weak point:** destroying a beacon collapses its governance (Legends-style portal). A spectacle hook and a clear attack objective.
- **Candidate vision/fog provider.**

### E. The protocol needs new message families
- `beacon.*`: place, move?, upgrade, destroy
- `mandate.*`: create, edit, pause, remove, report stream
- `controller.*`: attach, detach, lease, override, release
- `macro.*`, `group.*`: whether these are stored server-side, so they're shared with agents and replayable, or kept client-only

### F. The UI gets heavier
The client needs a mandate editor, a beacon overlay, a controller-attachment panel, and a macro/script editor (possibly node-graph). This weighs on the client choice: Godot UI vs web tech.

### G. Spectacle
Spectators see beacon spheres, mandate states and controller badges ("LLM", "RL", "script", "manual"). The chronicle logs governor decisions, so the story shows *who* decided what.

### H. Fairness
The same limits apply to every controller type: command points per game-time. Scripts get instruction budgets. External agents can't exceed what an in-sim governor could do.

---

## 5. Open items for the final interview pass
1. **Beacon mechanics:** cost, radius, max count, overlap between own and enemy spheres, contesting and capture, upkeep, placement range from the commander, what happens when one is destroyed.
2. **Commander details:** abilities, respawn in the sandbox, whether the commander is itself a controller slot.
3. **Authority and composition model:** precedence, leases, temporary takeover.
4. **Controller hosting:** in-sim scripting runtime choice (WASM/wasmtime vs wasmi vs Rhai vs Luau); per-tick budgets.
5. **Mandates:** Attack scope outside the sphere; which later mandates ship when; whether mandates can be nested or chained.
6. **Where macros and groups live:** server (shared, replayable) or client.
7. **Sandbox attack/defend kit:** approve the proposed kit (walls + reinforced walls, gate/drawbridge, autocannon, mortar, raider drone, demolition charge, repair/reclaim drone).
8. **Remaining design tensions:** balance with no win condition ("stress test" wave button?); physics vs determinism (fixed-point kinematic projectiles, client-only debris); 3D logistics (surface belts + ramps + node network); pathfinding and water under destruction (chunked flow fields + 2.5D water); colony depth (no personalities; veterancy + chronicle); logic processors vs agents (same sandbox and budget); spectacle vs readability (telegraphs, overlays); mode order; licence (GPL-3.0?).
9. **Foundation:** commit after the final research pass.

- **PRELIMINARY GAME NAME (DECIDED):** AirGap. The world and lore frame stays Handfast (Quorum vs Handfast Line). Trademark check still pending.

## 2.2 PRE-BUILD INTERVIEW (2026-09-13, overrides earlier sections where it conflicts)
- **Match end (DECIDED):** elimination plus a round cap. A seat's first beacon is its core. A seat is eliminated when it has no beacons left. The match ends when one seat remains, or at the host-set round limit (default 10), when the highest standing wins. The round limit defines the match phase used by segment growth.
- **Local humans (DECIDED):** one human per match in v1, plus up to 3 AI seats, on localhost only. Human-vs-human waits for internet multiplayer (roadmap). Consequence: the single human decides speed-up and skip, and AI seats don't vote (resolves the open speed/skip question).
- **Push camera (DECIDED):** own fog by default. While the human's seat is alive, the camera and director show only that seat's vision, the same as the AI seats' get_segment_feed. The full map unlocks when the seat is eliminated or the match ends. A host "casual" toggle allows no-fog and marks the match unranked.
- **Seats and teams (DECIDED):** v1 is a 3-seat free-for-all at most: 1 human plus up to 2 AI seats, or all AI. No teams. Messages stay within a seat. Friendly fire means hitting your own units or structures. Reserve a team_id field for later. Consequences: remove "4 seats" from the spec. Phase 0 gate G3′ can keep 4 seats as performance headroom. The planner needs a target-spread rule so both AIs don't pile onto one seat. Watch for kingmaking in 3-way FFA.
- **Fun test timing (DECIDED):** keep the milestone order. The movie fun test moves from the end of M3 to after M5, once the full Godot client exists. The M3 slice becomes a technical slice only (headless sim + plans + verifier + planner). Accepted risk: the biggest design risk is measured about 10 months later.
- **Lull timer (DECIDED):** a shared timer set by the host (3 min to untimed), default 5 min. The first Lull of a match defaults to 10 min. Gate P6 changes to: a first-timer builds a qualifying plan from a template in ≤5 min, and a 10-step plan from scratch in ≤10 min. LLM seats wait by long-polling.
- **Poverty (DECIDED):** an existing upkeep shortfall is a warning (W06xx) and is flagged in the recap. It is an error only when the plan adds uncovered upkeep (placing a beacon, queuing a structure) without "allow dormant". When money runs short, the Quartermaster makes beacons dormant in a fixed order: lowest priority first, then furthest from the core, and the core last. The principle is now "you only make it worse on purpose".
- **Capture (DECIDED): destroy only in v1.** This reverses the earlier capture-by-presence rule, and capture moves to the roadmap. Beacons are destroyed by damage. Their structures become unclaimed and are absorbed by the first uncontested own beacon whose sphere covers them. Bound units retreat, then re-home to the nearest friendly beacon (same as Recycle). Overlapping enemy spheres still block building and digging. Remove "dormant beacons take longer to capture".
- **Messaging (DECIDED): no radio until a Mast.** Drop "receive-only". Without a Radio Mast, beacons neither send nor receive. With one, operators inside coverage send and receive threat and help messages, and the commander can broadcast go-codes while in coverage. Beacon reports to the seat stay remote and always on. Attack launch conditions without a Mast use clock or force size.
- **Caps principle (DECIDED, user rule):** no artificial caps. Use a cap only where it is technically necessary, and every such cap must be explained coherently in the story. Audit existing limits against this rule (unit caps, awards per round, the 6 changes per visit, the Build target count, etc.). Plan and verifier limits that guarantee termination and bounded cost are technical, but still need a story framing (Seal inspection).
- **Power (DECIDED): kW replaces $ upkeep.**
  - **Roles:** $ is a stock spent to build. kW is a continuous flow that sustains everything fielded: beacons, units, turrets and shields.
  - **Supply:**
    - Each beacon's key-core gives a small base output.
    - Generator structures, built for $ with the Build mandate, sit on a finite number of geothermal vents on the map.
  - **Grid:** one seat-wide grid. No grid islands in v1 (roadmap). No batteries or storage in v1, so no J/Wh.
  - **Overdraw:** fabricators stop first, then beacons brown out into dormancy in the order already decided (lowest priority, then furthest from the core, core last).
  - **Replaces:**
    - $ upkeep and the Quartermaster's "reserve next round's upkeep" step are removed.
    - The poverty rule now applies to kW: an existing shortfall is a warning; a plan that adds draw beyond supply is an error unless "allow dormant" is set.
  - **Ceiling:** the unit ceiling becomes a world property, total vent supply on the map. The engine keeps a hard safety limit far above what any map can power, and map generation/design must keep total supply within the G3′ budget.
  - **Story (user):** no solar, because the world is in perpetual darkness. The sky is permanently dark (working name "the Pall"; name TBD), so power comes from geothermal heat and key-cores. Spectacle: glowing vents and lit domes in the dark, with visible brownouts.
  - **Roadmap:**
    - charge individual units energy for remote operations (user)
    - grid islands
    - batteries/storage
- **Start state (DECIDED):** each seat starts with a pre-placed core beacon (Build mandate) in its spawn zone, a commander, 2 build drones, 1 mining drone and $ equal to 2 rounds of BMI. The core's key-core provides base kW for the starting force plus headroom, and a vent sits a short walk away. **Every beacon has a built-in fabricator** that produces units for its mandate through the Quartermaster; there is no fabricator structure. Units cost $ to build and draw kW while fielded. No per-beacon unit cap (Tuning: starting numbers).
- **Maps (DECIDED):** a seeded, deterministic generator with 3-way rotational symmetry: equal spawn zones, equal nearby vents and ore, and richer contested vents toward the centre. About 384×384 wide by 64 tall (tuned by G3′). One ore type ("scrap seams") of varying richness, plus salvage from wrecks. Mine's "resource priority" becomes richest / nearest / safest. Maps save as files so handmade maps can come later. The generator is about 3 weeks in M1.
- **Placement setup (DECIDED):** choosing the mandate type is free at placement. Every other initial setting or target is charged at the normal interface rates after the 12 s deploy. **The 6-changes-per-visit limit is removed** (caps rule): a long visit is its own risk. The verifier's plan-size limits still bound cost.
- **Replays (DECIDED):** two formats. The deterministic replay (seed + plans + log) stays in the host's private match cache, powers scrub, recap and bug reports, and is never exposed through the API or UI in v1. Exported or shared replays are **recordings**: keyframes plus state deltas and events, with no plans. About 2 weeks of extra work. Secrecy on a local host is enforced by the API and UI, not by cryptography.
- **Capabilities (DECIDED): thematic licences, fabricator-built.**
  - **Licences:** earning a capability grants a permanent Ledger licence.
  - **Triggers:**
    - Radio Mast: hold 3 or more beacons at the end of a segment.
    - Sensor Spire: lose a structure to an attacker you never saw ("blindsided").
    - Resonance Spire: run Generators on 2 vents at once.
  - **Building:** once licensed, queue the structure on site at any own beacon. That beacon's built-in fabricator builds it regardless of mandate, for $ plus kW draw.
  - **Counts:** no count limits, except one Resonance Spire per sphere, since two spires would interfere (a technical and story reason).
  - **Rebuilds:** destroyed structures are rebuilt without re-earning.
  - **Tuning:** trigger thresholds, and anti-gaming for "blindsided".
- **Citations (DECIDED): deferred to post-v1.** When they arrive, they start cosmetic only: funny Ledger titles with no $ penalty. Small or large fees may be added later. v1 has awards but no Citations. The "first timeout is free" rule and the Snooze Button are moot in v1; a timeout just files the safe plan.
- **Recap timing (DECIDED):** the recap is its own phase between the Push and the Lull: awards plus about 3 auto-picked highlights, at most about 60 s, skippable by the human. The Lull timer starts when the recap ends or is skipped. The snapshot is frozen at segment end. During the Lull, a side panel lets the human re-watch or scrub highlights while the timer runs. AI seats long-poll through the recap.
- **Interrupts (DECIDED): damage never interrupts a visit.** Only the commander's death, a rule firing, a destroyed beacon, or leaving range stops it. The pressure comes from risking death, and dying interrupts the visit along with its other consequences. A rule firing aborts the remaining changes, and committed changes stay. Overlap with an enemy sphere blocks building and digging but not interfacing.
- **Scopes (DECIDED):** seat tokens can never hold spectate.nofog; it exists only for separate spectator tokens. Admin covers lobby and match control only and can never read another seat's plans, drafts or knowledge. The host "casual" toggle gives no-fog during the Push to every seat equally (the human's camera and the AI seats' get_segment_feed), while planning knowledge stays fog-limited. On a local host, enforcement is by the gateway only.
- **Fork (OPEN, revisit in draft 3):** G4 "determinism + fork" conflicts with "no dry runs". Recommendation on the table: snapshot/restore only (no fork), scrub from recordings, and plan-core/verifier/planner/gateway crates barred from depending on sim stepping. The user is unsure; draft 2 flags it as open.
- **Licence (OPEN, revisit in draft 3):** recommendation on the table: GPL-3.0-or-later for the game (sim, client, planner); MIT OR Apache-2.0 for schemas, SDKs, docs, llms.txt and example plans; CC BY-SA 4.0 for assets. Alternatives: permissive everything, or AGPL for the game. Must be decided before the repo goes public.
- **Name (DECIDED):** AirGap is the internal codename only, with no logo, domain or store page. A final title and faction names, plus a professional clearance search, come before anything goes public. World terms live in one lore table so a rename is a find-and-replace. The 2026-09-13 quick search found the Steam game "Interstellar Airgap" (2021), AIRGAP trademarks held by Airgap Networks/Zscaler (serial 88813915) and Transcend (99572619), and a 2025 board game called "Quorum". Handfast and Keybearer came up clean.
- **Pre-build interview status:** all 20 items done. Open for draft 3: fork (item 19) and licence (item 20).

## 2.3 PRE-BUILD INTERVIEW PASS 3 (2026-09-13, overrides 2.2 and earlier where it conflicts)
- **Fork (DECIDED, supersedes the 2.2 OPEN entry):** research-only fork. The core sim has snapshot/restore, which saves and replays also use. `fork` exists only behind a compile-time `research` Cargo feature that release builds never enable, and CI builds both configurations. The plan-core, verifier, planner and gateway crates can never depend on it, enforced by a dependency check in CI. Phase 0 G4 tests determinism, snapshot/restore round-trips, and fork equivalence in the research build. "No dry runs" stays a hard guarantee in shipped builds.
- **Licence (DECIDED, supersedes the 2.2 OPEN entry):** GPL-3.0-or-later for the game (sim, client, planner, gateway, gamectl). MIT OR Apache-2.0 for the schemas (`.proto` files and generated JSON Schema), SDKs, docs, llms.txt and example plans. CC BY-SA 4.0 for art, audio and other assets. Each licence gets per-directory LICENSE files and SPDX headers, and a DCO sign-off is required on contributions. Console ports are out of scope (GPL vs NDA SDKs).
- **Key-core power (DECIDED):** seal-only key-cores. Story: a key-core taps just enough heat through its own shallow bore to keep its Ledger seal lit, while the founding core sits on a deep bore with spare heat. Rules:
  - A non-core beacon's key-core output equals that beacon's own base draw, so its net is zero. It never goes dormant from its own draw, but its fabricator, structures and units draw from the seat grid.
  - Only the core provides surplus kW.
  - Generators on heat vents are the only scalable supply, which closes the beacon-spam loophole.
  - The map generator guarantees a reachable vent near each start.
- **LLM seats (DECIDED):** the game never calls an LLM and never holds API keys.
  - `gamectl seat run --agent claude|codex|<command template>` launches the user's installed coding agent headless once per Lull, e.g. `claude -p` or `codex exec`.
  - Each launch gets a seat prompt (from llms.txt) plus a generated MCP config holding a short-lived per-seat token.
  - Keys, model choice and billing stay in the user's agent tool. The lobby shows token and cost usage when the agent reports it.
  - Small per-agent adapters ship alongside a generic command template.
  - A timeout, crash or non-filing agent results in the safe plan being filed and an event in the log.
- **AI-only matches (DECIDED):** in v1 an AI-only match uses the normal client flow in realtime, with a spectator camera in place of the human seat. There is no headless or accelerated mode. Pace controls (speed-up, headless runs) go to the roadmap and get added once bulk AI testing and tuning needs them. The sim's fixed tick and runner already allow faster-than-realtime stepping later.
- **Elimination details (DECIDED):**
  - **Last beacon lost:**
    - Story: with no key-core seal left, the Ledger voids that seat's licences.
    - The seat is eliminated that tick. Its units shut down and disband on the spot.
    - Its structures go dark as neutral ruins. Anyone can recycle them for $ (a new recycle target for the planner).
  - **Core destroyed, other beacons survive:** no succession.
    - Story: the deep bore collapses.
    - The surplus kW is lost permanently, and the seat lives on vent Generators.
    - Brownout "furthest from core" is measured from the core's former site.
    - The commander respawns at the surviving beacon nearest to the death location.
    - Losing the core is not elimination.
  - **Human eliminated while AI seats continue:**
    - The human becomes a spectator with a full-map view, watching in realtime.
    - They can end the match at any time. Standings freeze at that tick and the recording is saved.
- **Round-limit winner (DECIDED, replaces smoothed net worth):** "Held + destroyed", framed in the story as the Ledger's final audit.
  - Score = value held at the final tick + build cost of enemy assets this seat destroyed during the match.
  - Value held = $ + build cost of its beacons, structures and units.
  - Credit goes to the seat that dealt the killing damage.
  - The score is computed by the game, shown live in the standings, and forms the recap's final ranking.
  - Risks to watch in the fun test: kingmaking and farming the weakest seat.
- **Caps audit (DECIDED):**
  - **Build targets and protected areas:** the ≤16 and ≤4 limits are removed. Both count toward the single plan size budget ("Ledger seal inspection"). That budget is technical: FULL verification must stay under 50 ms. The editor and the verifier output show a size meter.
  - **Attack reach and Defend pursuit:** the 96 and 32 voxel numbers are removed. Story: operators can direct units only where the beacon can see or relay orders.
    - Reach = the beacon's sphere + sight coverage (Sensor Spire extends it) + relay coverage (Radio Mast extends it).
    - Pursuit ends when the target leaves that coverage, which keeps the termination guarantee.
    - The verifier checks reach against the capability state at plan time.
  - **Awards:** the ≤3 per seat and ≤50% of BMI limits are removed. Story: each audit, the Ledger releases a fixed award fund, tuned relative to BMI, split among that segment's award winners in proportion to awards won. No per-seat count cap exists.
  - **Rule slots:** the beginner 4 / expert 12 tiers are removed. Gambit rules count toward the plan size budget, which is technical because rules are evaluated every tick for every seat and the 20 Hz tick must hold. The beginner template starts with 4 rules as a starting point, not a cap. Phase 0 profiles the per-rule tick cost to set the budget.
- **Dispatches (DECIDED, seat-to-seat talk in v1):**
  - **Story:** "sealed orders" means radio silence during the Push. A Dispatch is the one open-air transmission a seat's network makes as the seal closes. Operators "speak on the air, never obey it".
  - **Writing:**
    - An optional text field in the plan editor, and a `dispatch` field on filed plans.
    - No extra LLM calls.
    - One note per seat per Lull, about 280 characters (tuning). The limit is framed as the brief transmission window, and it is a real technical limit because notes cost tokens in every agent's next Lull.
  - **Gate:** a powered Radio Mast is needed both to send and to receive. Without one, a seat neither sends nor hears. Losing the Mast cuts the seat off.
  - **Audience:** open air only. Every Mast-holding seat hears every Dispatch. No directed or private notes, so there is no secret collusion against the human.
  - **Timing:** all Dispatches are delivered together at Push start. No replies within the same Lull.
  - **Binding:** never enforced by the Ledger. Bluffing is allowed.
  - **Display:**
    - Push-start ticker, Lull side panel, and recap.
    - `get_segment_feed` includes them as `dispatches[]`, each with `from_seat`, `round` and `text`.
    - They are public, so they appear in recordings and the event log.
  - **Built-in planner:** sends template flavour lines based on its intent and events, sometimes misleading. It never reads incoming Dispatches, and the lobby labels it "built-in".
  - **Host toggle:** Dispatches on or off.
  - **Security:**
    - Dispatch text is untrusted data. It is plain text with control characters stripped, and links and markup are never rendered.
    - It is delivered only in labelled data fields and never merged into the agent prompt's instructions. llms.txt tells agents that Dispatches are opponent speech.
    - `gamectl seat run` adapters launch agent CLIs with only the game's MCP tools allowed: no shell, file or web tools. Worst case, a manipulated move, never a host breach.
  - **Roadmap:** directed or encrypted notes, DF Loop interception, jamming, live Push chat, and Ledger-enforced pacts.
- **$ pools (DECIDED):** one seat treasury.
  - Mined $ is credited to the seat treasury the moment ore is delivered. Per-beacon pools and the per-round sweep are removed.
  - All spending draws from the treasury, allocated by the Quartermaster's urgency and fair-share rules: fabricators, setup charges, licensed structures. Recycling refunds are paid into it.
  - Each seat has exactly one $ stock and one kW flow.
- **Minor follow-ups for draft 3 (not interviewed):**
  - Placement range becomes a tuning value.
  - The "timed weapons 2–3 rounds" rule is removed from v1, since there are no timed boosts in v1.
- **Pass 3 status:** all 10 items done. Next: revisit priorities and build order to maximize efficiency, then draft 3.

## 2.4 PRIORITIES & BUILD ORDER REVIEW (2026-09-13, overrides spec sections 16–17 where it conflicts)
- **Diagnosis presented:**
  - The fun test comes about 10 months after Phase 0.
  - Milestones are horizontal layers, with integration only at M3 and M5.
  - The fun test lacks the economy and capabilities (M6).
  - The API arrives late (M4) even though it enables agent-driven testing.
  - Phase 0 spikes are mostly throwaway.
  - Pass 3 adds about +4 weeks.
- **Capacity (DECIDED):** solo developer plus coding agents. The user directs and reviews, and Claude Code / Codex write most of the code. Milestones run sequentially, with heavy automation (headless tests, golden files, CI hashes) so agents can verify their own work. Human review is the bottleneck. Client feel and art are the weakest area for agents.
- **Build strategy (DECIDED): "Skeleton → slices".** The user asked for the walking skeleton improved to mitigate its downsides.
  - **Walking skeleton:**
    - Locks the never-churn contracts at full quality: Protobuf envelope with `buf breaking` from day 1, determinism rules and lints, gateway auth and scopes, plan envelope with kind/version, `Operator`/`Quartermaster` traits, snapshot and hash formats.
    - Content stays narrow but on real code paths, so deepening adds variants instead of rewriting.
    - Includes a real vista: own voxel mesher with destruction on a small map, plus a basic director camera. Movie Maker clips for progress posts.
    - Phase 0 gates are embedded: G4, P1 and P5 run on the real code; G3′ and G1 run against the skeleton under synthetic load.
  - **Vertical slices:**
    - Each slice runs through sim, verifier, API, planner, editor and view, and ends playable.
    - Each has a named depth task for its risky layer, done properly once.
    - Each has a machine-checkable definition of done: determinism hashes, golden plans, scripted headless scenarios, and an MCP agent Lull run. Agents iterate to green CI; the user reviews slice demos.
  - **Draft slice order** (risk and fun value; may shift after the fun probe):
    1. mining + power
    2. combat + destruction
    3. expansion + licences
    4. radio + Dispatches
    5. planner depth
    6. editor depth
    7. recap/recordings
  - **Hardening** pass at the end.
  - The vista and contract quality cost about +2 weeks on the skeleton.
- **Fun testing (DECIDED): probe + gate + final.** Pass/pivot criteria are written before each checkpoint.
  1. **Fun probe** at the end of the skeleton (about week 12):
     - 3–5 testers play a 3-round match against the built-in planner and a Claude seat.
     - Metrics: "would you play another round?" and attention during the Push (watching vs tabbing away).
     - Testers are told to judge the loop, not the content.
  2. **Fun gate** after slice 3 (combat, expansion and licences in): the real go/pivot decision.
  3. **Final balance playtests** during hardening.
  - Each checkpoint costs about 1 week of user time. This replaces "fun test at end of M5".
- **Phase 0 (DECIDED): 3-week stack spikes, the rest embedded.**
  - **Throwaway spikes before the skeleton** (failure changes the foundation):
    - G4: cross-OS integer determinism plus save/restore on a toy sim, including fork equivalence in the research build.
    - G1: own greedy mesher plus destruction remesh in Godot 4.7.
  - **Gates embedded where their real code first exists:**
    - Skeleton: P5 MCP session and P1 QUICK checks.
    - Slices 1–2: P1 FULL checks and per-rule tick cost.
    - Expansion slice: G2+P3 pathing and travel estimates.
    - Radio slice: P7 mast connectivity.
    - Planner slice: P2 planner.
    - Editor slice: P6 editor productivity (the probe uses template-only editing).
    - Every slice: G3′ perf as a CI budget check.
  - Saves about 8 weeks. Fallbacks for every embedded gate are written up front, because a late failure costs rework.
- **Agent harness (DECIDED): harness first plus a free league.** Built in skeleton weeks 1–2:
  - AGENTS.md / CLAUDE.md covering determinism rules and crate boundaries.
  - `cargo xtask ci` as the single command.
  - `gamectl scenario run`: headless match from a scenario file (map seed + plans + assertions on events and hashes).
  - Golden files with readable diffs, and headless Godot screenshots for visual checks.
  - Contract files (.proto, lints, determinism code) need explicit user approval.
  - At most 2–3 agents in parallel git worktrees, bounded by review capacity.
  - A nightly built-in-planner league (free) catches balance drift and desyncs. LLM-seat matches run only at checkpoints (fun probe, gate, pre-release).
- **Scope tiers (DECIDED).** Each tier is the definition of done for its checkpoint; nothing outside a tier is built before it.
  - **Fun probe (skeleton):**
    - World: small seeded map; core plus placing beacons.
    - Mandates and units: Mine + Defend; mining drone, raider and autocannon.
    - Plans: route plan (visit / go / place) with `on_death`, `fallback` and flee rules; QUICK verifier.
    - API: gateway, MCP, and `gamectl seat run --agent claude`.
    - Planner: safe plan plus an Easy template planner.
    - Client: vista view, own-fog camera, template-only plan wizard (3 templates).
    - Match: Lull timer, basic recap (standings).
    - Economy: $ with BMI, kW with core surplus and Generators.
  - **Fun gate (after slices 1–3):**
    - Power grid and brownouts.
    - Build + Attack mandates.
    - Destruction combat with mortar, wall and demolition charge.
    - HPA* pathing and travel estimates.
    - Licences and all 3 capability structures.
    - FULL verifier.
    - Award fund, final score, elimination and ruins.
    - 3-way symmetric map with fairness tests.
  - **v1 (slices 4–7 + hardening):**
    - Radio, Mast coverage and Dispatches.
    - Normal/Hard planner and target spread.
    - Full editor: map route, clock, rule list.
    - Recap highlights and scrub; shareable recordings.
    - Casual mode.
    - llms-full.txt and golden plans.
    - Codex + generic adapters.
    - Accessibility basics.
- **Cuts to roadmap (DECIDED):**
  - Planner personalities: Balanced only, difficulty levels remain.
  - Director camera: v1 has own-fog, free and follow-commander cameras.
  - Kit trim: gate, reinforced wall and shield block move to the roadmap. v1 kit is wall, autocannon, mortar, raider, demolition charge, repair/reclaim drone, scout, Generator, and build and mining drones.
  - Expert editor view: humans get the beginner editor only; selectors, branch/repeat, raw JSON and command palette move to the roadmap. The schema and API keep full power for agents. Accepted risk: a plan-sophistication asymmetry against the human.
  - Savings: about 6 weeks.
- **Economy placement (resolved by tiers, no interview needed):** BMI, $ and kW basics are in the skeleton. Grid/brownouts, award fund and licences come in slices 1–3, before the fun gate. The old M6 "economy layer" milestone is dissolved.
- **Going public (DECIDED): after the fun gate.**
  - The repo stays private through the skeleton, probe and slices 1–3.
  - If the gate passes, naming plus a professional clearance search run in parallel with slice 4 (calendar time, not dev time).
  - The repo then opens under the final name, around week 38, with a devlog and "not accepting PRs yet" at first.
- **Revised plan (DECIDED): 58 weeks, re-baselined at checkpoints.** Replaces spec sections 16–17.

  | Ends wk | Stage | Weeks |
  |---|---|---|
  | 3 | Stack spikes (G4, G1) | 3 |
  | 15 | Walking skeleton (harness first) | 12 |
  | 16 | Fun probe | 1 |
  | 21 | Slice 1: mining + power (depth task: P1 FULL) | 5 |
  | 27 | Slice 2: combat + destruction | 6 |
  | 33 | Slice 3: expansion + licences (depth task: G2+P3) | 6 |
  | 34 | Fun gate; naming and clearance start; public around wk 38 | 1 |
  | 38 | Slice 4: radio + Dispatches (depth task: P7) | 4 |
  | 42 | Slice 5: planner depth (depth task: P2) | 4 |
  | 48 | Slice 6: editor depth (depth task: P6) | 6 |
  | 52 | Slice 7: recap + recordings | 4 |
  | 58 | Hardening + release | 6 |

  - The estimate assumes one full-time developer with no agent speed-up. With a 20% buffer, plan for about 16 months (around Jan 2028).
  - Velocity is measured after the skeleton, and the remainder is re-estimated at the fun probe and again at the fun gate.
  - Chance of shipping v1 (estimate): about 75% now, about 82% after the fun gate passes.
- **Build review status:** all 9 items done. Next: draft 3 of the spec, republished to the same artifact URL.

## 2.5 PASS 4 INTERVIEW (2026-09-13, after draft 3 was published; overrides earlier sections where it conflicts)
- **Attack reach (DECIDED): sight includes unit vision.**
  - Story: operators direct units toward anything the seat sees or recently saw.
  - Reach = the beacon's sphere + live vision of the seat's units and structures (scouts, raiders) + Sensor Spire reveals + Radio Mast relay coverage.
  - The verifier checks reach against known positions at plan time. A target seen within the last N minutes counts (N is tuning).
  - Pursuit ends when the target leaves all of those.
  - Scouting first enables early attacks; the Mast and Spire extend and stabilise reach.
  - Risk: raids can chain as advancing units see further.
- **Fun probe conflict (DECIDED): Attack-lite joins the skeleton.**
  - Attack mandate with a fixed target, launch on force size or segment time, and retreat on losses. No selectors, waves or go-codes.
  - Reach uses unit vision. The scout joins the probe kit.
  - Beacon destruction plus elimination with ruins move into the skeleton.
  - The skeleton grows about +1.5 wk, so the probe lands around wk 17.5. Slice 2 deepens Attack (selectors, waves, go-codes) instead of introducing it.
- **Human editor selectors (DECIDED, partly reverses the 2.4 expert-view cut):**
  - The v1 human editor keeps late-bound selectors. Alt-click turns a fixed target into a selector (nearest, weakest, safest, most threatened), and templates may pre-fill them.
  - Chips show "resolves when the step starts".
  - Branch, repeat, flags, raw JSON and the command palette stay on the roadmap.
  - Slice 6 grows about +1 wk.
- **Match length (DECIDED): about 75 min by default.**
  - Round limit defaults to 6 (host-settable).
  - Lulls: 5 min, with the first at 10 min.
  - The Push grows 3→8 min (was 3→10).
  - Licence triggers, awards and segment growth are tuned for 6 rounds.
  - The fun probe still uses 3 rounds.
- **Save and resume (DECIDED): at Lull boundaries.**
  - The host saves during any Lull, from the frozen segment-end snapshot, and resumes from the lobby.
  - Seat tokens are reissued. LLM seats relaunch (they are stateless per Lull).
  - Saves are stamped with the rules hash and verifier version, and refuse to load on a mismatch.
  - Save files contain plans and drafts, so they live in the private match cache.
  - No mid-Push save. Slice 7 grows about +1 wk.
- **Platforms and distribution (DECIDED):**
  - Windows and Linux are first-class: tested and released.
  - macOS runs in CI for determinism. Releases are best-effort and unsigned; signing and notarisation are roadmap.
  - v1 distribution is GitHub Releases plus itch.io. Steam comes later, after name clearance and an audience.
  - Agent adapters are tested on Windows and Linux.
- **Onboarding (DECIDED): Ledger probation match.**
  - A "Probation" lobby preset: the human vs one Easy built-in, 3 rounds, untimed Lulls.
  - The Ledger issues contextual memos from deterministic triggers (dark Stake, first death, shortfall, first licence).
  - The first Lull opens the template wizard with a guided pick. No separate tutorial levels.
  - Doubles as the fun-probe setup. Slice 6 grows about +1 wk, plus memo copy in the Ledger voice.
- **Ruins salvage (DECIDED): reclaim drones.**
  - Ruins behave like wrecks. Repair/reclaim drones from any beacon salvage ruins within that beacon's reach, crediting $ to the treasury on delivery.
  - No commander visit is needed.
  - Salvage value is tuning (e.g. 25% of build cost). Risk: it can snowball the eliminating seat.
- **BMI standing (DECIDED, confirms draft 3):** rank on the live final-audit score. One number drives the standings and the BMI scaling (last +10%, leader −5%, tuning). Known effect: destroyed value never decays, so an early aggressor keeps the leader penalty.
- **Playtest feedback (DECIDED): local opt-in bundle.**
  - A playtest mode adds a 3-question end-of-match survey, including "play another round?".
  - Local metrics file: Push time with the window focused vs unfocused, Lull time used, skips and speed-ups, camera use, verifier errors hit.
  - "Export playtest bundle" packages survey + metrics + recording + logs as a file for the tester to send manually.
  - No network telemetry.
  - Built in the skeleton (about +1 wk) so the probe can use it.
- **Schedule impact of pass 4 (estimate):**
  - Skeleton +2.5 wk (Attack-lite +1.5, feedback bundle +1), so the probe lands around wk 18.5.
  - Slice 2 −1 wk (Attack introduced earlier).
  - Slice 6 +2 wk (selectors, probation match).
  - Slice 7 +1 wk (save/resume).
  - Total about 62.5 wk (was 58); about 17 months with a 20% buffer. Re-baselined at the probe as decided.
- **Pass 4 status:** all 9 items done. Draft 4 (v0.4) is published to the same URL.

## 2.6 PASS 5 INTERVIEW (2026-09-13). Overrides 2.5 and everything earlier
Agenda:
1. Scouting and support units
2. Early-rush protection
3. Commander as a target
4. Kill credit for the final audit
5. LLM memory across Lulls
6. Agent launch sandbox
7. Art direction and asset pipeline
8. Audio scope
9. Minor rules: tie-break, resubmitting, English-only strings

- **1. Scouting: hybrid with a Survey mandate** (the user asked for more research first; precedents are Factorio radar, Screeps Observer, Northgard Scout Camp, Settlers lookout, Majesty rangers and explore flags).
  - Spheres give passive vision, as before.
  - **Defend picket:** a short scout ring just past the sphere edge, for local warning.
  - **New fifth mandate "Survey"** (a Ledger survey post; working in-world name "Watch"). Settings: probe areas or directions, scout count, `roe`, retreat.
  - **Survey operator:** scans unexplored cells first. Next it re-checks the sightings closest to expiring from the Attack reach window, enemy beacons first (Factorio-style rescan of the oldest sighting). It avoids known threats.
  - Every sighting carries its age, shown in the UI and API. If Attack arrives and the remembered target is gone, its after-action or retarget rule takes over.
  - **Build owns repair/reclaim drones:** they repair its structures, then salvage wrecks and ruins in reach. Defend's damaged units fall back to the nearest friendly repair drone.
  - **Pitfalls to watch:**
    - Perfect-intel snowball: scouts die to any fire; jammers and decoys are roadmap.
    - Stale-intel baiting.
  - **Cost:** skeleton +0.5 wk (Survey-lite, needed for Attack-lite reach); slice 2 +0.5 wk. Templates stay at 8; the Staged Attack template gains an optional Survey post.
- **2. Early rush: fortified core plus spawn distance.**
  - Founding Stakes were armistice-hardened: the core gets high HP and a built-in autocannon powered by its deep bore (it draws from the core surplus).
  - The map generator guarantees raider travel time between spawns of at least about 1.5 early Pushes. The fairness tests check it.
  - No time-based protection rule.
  - Watch in the nightly league: massed rushes, core turtling. Values are Tuning.
- **3. Commander hunting is a real strategy.**
  - The user rejected "opportunistic only" as too artificial.
  - Attack gets an `enemy commander` selector. It uses the last sighting with its age, and retargets live while any friendly unit sees the commander.
  - **Defence:** Defend's protect list can include `my commander`. Its units escort the commander and intercept threats near it while it is within that beacon's reach.
  - Existing defences stay: routes that avoid known threats, flee rules, `on_death`, and the growing respawn delay.
  - Further counters (armour, decoys, the roadmap self-defence weapon) come only if nightly league data shows hunting dominates.
  - Risk: hunting dominance.
  - Cost: +0.5 wk in slice 2.
- **4. Kill credit is split by damage dealt.**
  - The build cost of a destroyed enemy asset is split among seats in proportion to the damage each dealt since the asset was last at full HP.
  - Each asset keeps at most 3 integer counters, one per seat; integer apportionment with deterministic remainder handling.
  - The recap shows "shared kill". Replaces the killing-blow rule.
- **5. LLM memory: seat notebook.**
  - A private per-seat notebook: the `save_notes` tool, returned at the top of `get_briefing`. It has plan-level secrecy, is included in saves, and is vendor-neutral.
  - Size = the briefing token budget (about 2k tokens, Tuning). This is a technical limit.
  - The human gets a notes box in the editor. The built-in planner ignores it.
  - No session resume. The agent guide teaches note-taking.
  - Cost: +0.5 wk in the skeleton.
- **6. Agent sandbox: API-key and subscription modes are both first-class.**
  - The user ruled that provider terms of service are the end user's concern: they use a service they pay for. No vendor clarification step.
  - **Shared contract for both modes:**
    - Pre-launch tool-list check: Claude's stream-json `system/init` tools list; `codex debug prompt-input` for Codex.
    - Watchdog: kill the agent on any non-game tool call, file the safe plan, flag the seat.
    - Empty per-seat run directory, plus a scan for CLAUDE.md or AGENTS.md in ancestor directories.
    - Game MCP tools are seat-scoped and have no side effects beyond the seat's own actions.
    - CLI versions pinned to tested ranges; the `gamectl seat doctor` canary (shell, file read and web must all fail).
    - Lobby shows Locked (API-key isolation) or Guarded (subscription login, or a weaker OS sandbox).
    - Dispatches are withheld only if the pre-launch check fails.
  - **Claude, API key:** `--bare` plus tool flags.
  - **Claude, subscription:** no `--bare`; built-in tools disallowed (removed from context), only `mcp__airgap__*` allowed, hooks disabled, minimal setting sources. Personal CLAUDE.md may load.
  - **Codex, API key:** clean `CODEX_HOME` plus `CODEX_API_KEY`.
  - **Codex, ChatGPT login:** the user's own `CODEX_HOME`, `--ignore-user-config`, `--ignore-rules`, read-only sandbox, shell/unified_exec/web/view_image/multi_agent/apps/plugins disabled, `project_doc_max_bytes=0`. `apply_patch` and `update_plan` can't be removed, but the sandbox blocks writes.
  - **Open, being researched:** a technical workaround if `claude -p` (headless) usage becomes billed differently from interactive subscription usage.
- **6b. LLM seats are interactive sessions; no headless vendor CLIs in v1.** This supersedes the headless parts of 6 and of 2.3.
  - **Why:** the user's vision is someone using Claude Code or Codex against an existing game through a bridge. Headless is judged a dead end given the looming subscription policy change (the June 2026 Anthropic billing split was announced, then paused).
  - **"Your agent" seat:** `gamectl connect` adds the game MCP server and a seat token to the user's own running session. It keeps its normal tools (it can script against the API) and the user supervises. User plus agent = the one human seat. Dispatches reach it only through an explicit, labelled `get_dispatches` tool, and can be switched off per seat.
  - **Opponent LLM seats:** the lobby opens a terminal window running an interactive Claude Code or Codex session with the airgap command or skill.
    - Game tools are pre-approved by allow rules. No bypass-permissions mode.
    - A PreToolUse hook denies non-game tools and reports liveness and tool calls to the game (the watchdog).
    - `await_next_event` always returns within about 240 s (heartbeat loop: wait → plan → submit).
    - A Stop hook sends the agent back into the loop if it ends its turn mid-match. It has a failure counter.
    - Deadlines are enforced by the game (safe plan on a miss). The notebook plus the per-Lull briefing survive compaction.
    - Codex opponents need a separate `CODEX_HOME` login or an API key (OAuth refresh race, issue #10332).
    - Claude channels replace the heartbeat loop when out of preview.
  - Unattended AI-only matches, the nightly league and CI use the built-in planner plus local models (Ollama or LM Studio via an MCP host).
  - **Roadmap:** API-key and subscription headless operation as an alternative to interactive sessions.
  - Cost for item 6 overall: about +2 wk.
- **6c. The AI and scripting ladder is adopted.** The user's rules, in development priority order:
  1. **Playable without an external AI:** built-in planner, editor, the 8 templates, Probation. The skeleton and the fun probe need no MCP or Claude seat; the probe is human vs built-in. The Seat Gateway (JSON-RPC) stays in the skeleton because the editor uses it.
  2. **Customisable sample scripts, editable by hand:** the shipped templates and sample plans as readable local files. The editor offers Save as template / Load. Local-first: the gateway sees them only as drafts or plans. **Format still under investigation:** it must segue into tiers 3 and 4 without complex conversions.
  3. **Scripting API for AI-assisted script generation:** a published docs site generated from the schema (examples, JSON Schema, golden plans, llms.txt, a "Write a script" guide), `gamectl` JSON in/out, and an official Python SDK with sample scripts. Scripts run locally and only produce plans.
  4. **Live conduit:** the MCP server and interactive sessions (6b). A thin layer over tier 3. The Push stays sealed.
  - **Trials:** a new **Trial** tag for interaction details still to be settled. Checkpoints at the fun gate and after the scripting slice compare editor only, hand-edited files, AI-written scripts and live sessions. The API follows minor-version rules so direction can change.
  - **Build plan:** the skeleton drops MCP, the Claude adapter and P5 (about −2 wk). New slice 8, scripting (docs site, Python SDK, samples; about 3 wk). New slice 9, live conduit (MCP, interactive sessions, hooks; about 4 wk; P5 moves here). An MCP smoke test runs at the fun gate, and the gateway is agent-shaped from day 1.
- **6c (format / shared stack): DEFERRED by the user.** "Defer this question momentarily. We will build upon this investigation with Fable after compact." The user's criteria: the format must segue into later tiers without complex conversions, and this starting point strongly shapes the initial build direction.
  - **Investigation so far (three rounds):**
    - **Data format: JSONC** (canonical proto JSON plus comments).
      - Typed parameter nodes as a proto oneof: `{"param":"x"}` / `{"literal":"3"}`. Avoid `${var}` strings and `$param` keys.
      - `jsonc-parser` 0.33.2 CST edits preserve comments (MIT). Python round-trip via json-five.
      - Proto→JSON Schema: bufbuild/protoschema-plugins (alpha; chrusty's is archived). VS Code has only limited support for draft 2020-12, so consider draft-7.
      - `$schema` key collides with deny-unknown-fields: strip it, or use `json.schemas` settings.
      - Rejected: YAML (Rust crates unsound or unmaintained, no comment-preserving editor), TOML (poor for trees), KDL / txtpb / HCL / RON.
    - **Candidate A: JSONC + Starlark → Python SDK.**
      - starlark-rust 0.14.2: Apache-2.0, very active, no API-stability promise. It ALLOWS recursion (starlark-go doesn't). Tick, heap and callstack limits exist. `load()` can be disabled. `true`/`false`/`null` can be predeclared.
      - starlark-pyo3 2026.1.2: single maintainer; no macOS x86_64 wheel, no linux-aarch64 wheel for Python 3.14.
      - No game precedent. LLMs write Python features that Starlark rejects. Converting JSONC to Starlark needs syntax changes.
    - **Candidate B (last recommendation): JSONC + TypeScript.**
      - `.ts` `makePlan(briefing): Plan` runs in-game via rquickjs (QuickJS-ng, MIT) after type stripping with `swc_ts_fast_strip`. No `Date`, seeded `Math.random`, memory and interrupt limits.
      - Tier 3: the same files run in Deno (`--allow-net=127.0.0.1`) against `@airgap/sdk`, generated from proto via protobuf-es or ts-proto `onlyTypes`, with no enums.
      - Tier 4: MCP agents write the same TS ("code mode"). A thin Python client only on demand.
      - JSONC pastes into TS unchanged. Types help LLMs (type-constrained decoding more than halves compile errors, PLDI 2025). Synergy with the roadmap TS spectator.
      - Risks: rquickjs Windows MSVC support is experimental (fallback: Boa, 94% Test262); determinism must be built by us.
      - Proposed new stack spike **G6 script runtime** (+0.5 wk). +1.5 wk in slice 6.
      - Would change 6c tier 3 from a Python SDK to a TS SDK.
    - Option C: defer the language to a trial and ship JSONC-only tier 2.
  - **Precedents gathered:**
    - Cities: Skylines (verified source: Sunwood AI Labs, Codex, May 2026) is a C# mod serving a localhost HTTP API with `/state/*` and `/commands/*` plus a SKILL.md. Lesson: small explicit commands, then re-check state.
    - Factorio Learning Environment: Python REPL with ~23 typed methods; 56% of steps in successful runs raised errors; agents rarely create reusable functions, so seed the library.
    - CodeAct: code actions beat JSON actions on multi-tool tasks by up to ~20 points.
    - Anthropic "code execution with MCP" and Cloudflare Code Mode: TypeScript APIs.
    - Voyager: JS skill library.
    - CivBench (Civ VI via 76 MCP tools): agents rarely check state and 35–52% of their plans go unexecuted → use few coarse MCP tools and add a plan-vs-outcome report.
    - Mindcraft: parameterised commands plus raw code as the escape hatch.
    - Claude Plays Pokémon: a self-edited knowledge base.
- **Research already gathered for pending items 7–8** (not yet presented to the user):
  - **Art:** author in MagicaVoxel (freeware, closed source, models owned by the author) or Goxel (GPL). Load `.vox` in Rust with `dot_vox` (MIT) and mesh with our greedy mesher; skip GDScript importer addons. Lighting under the Pall: emissive voxels plus glow, few unshadowed short-range pooled lights, Rust flood-fill voxel light baked into vertex colours; SDFGI optional High setting (static only); no VoxelGI or lightmaps.
  - **Audio:** Kenney CC0, Freesound CC0/CC-BY only, sfxr / ChipTone output CC0. Sonniss GDC bundles are a custom licence (and v2.0 forbids AI training), so keep them out of the BY-SA repo. Music: OpenGameArt, ccMixter, Opsound (per-track licence check). Godot: AudioStreamPlayer3D, buses, `max_polyphony`, pooled players. CC BY-SA 4.0 is one-way compatible into GPLv3. Keep a REUSE-style licence manifest.
- **Draft 5 (v0.5)** was written and published mid-pass at the user's request ("rewrite the draft spec. prepare for compact"). Build plan 71 wk: skeleton 13.5 (MCP moved to slice 9), probe wk 17.5, slice 2 6 wk, slice 6 8.5 wk, slice 8 scripting 3 wk (wk 60), slice 9 live conduit 5 wk (wk 65), hardening to wk 71; about 20 months with buffer. Open items are listed in spec section 19.
- **Pass 5 status:** items 1–5 decided; 6 and 6b decided; 6c ladder adopted but its format/stack DEFERRED; items 7 (art), 8 (audio) and 9 (minor rules: final audit tie-break, resubmitting during the Lull, English-only externalised strings) not yet asked.

## 2.7 PASS 6: v1 SIMPLIFICATION AND SPEC REVIEW (2026-09-13). Overrides 2.6 and everything earlier

- **6c resolved by simplification (item 1 of pass 6).** The multi-agent investigation (36 agents; report in `6c-recommendation.md`) found that in v1 a script's only job is to produce a playbook during the Lull, so nothing user-written needs to run inside the game in v1; the owner then restated v1 in three levels and deferred external scripting past v1.
  - **The owner's v1 requirements, verbatim in substance:** the commander is operated by a playbook generated with the built-in "rudimentary" tool (manual control deferred); a built-in default **operator** generates and executes a safe playbook; built-in default **mandates** are installed on beacons; built-in default **programs** run units and buildings; externally generated scripting, mods and the API are deferred past v1 but designed for on the roadmap.
  - **Vocabulary adopted for draft 6:** playbook (the gameplan), operator (commander level: generates, files the safe playbook, executes), mandate (beacon level), program (unit and building level). The spec's "planner" becomes the operator; the beacon-level "operator" wording becomes the mandate itself.
  - **Format:** playbooks and templates are JSONC files (canonical proto JSON with comments). No SDK, no script language and no script runtime in v1. The Seat Gateway stays internal (editor and operator use it), kept agent-shaped, not published in v1.
  - **Seams reserved now** as proto oneofs with only the `builtin` arm implemented: `operator {builtin|script}`, `mandate {builtin|script}`, `program {builtin|script}`, `author_kind SCRIPT`, and a plan fingerprint.
  - **Roadmap language:** JavaScript/TypeScript-family, chosen at a post-v1 trial (rquickjs builds clean on MSVC; the earlier "experimental on Windows" note was wrong; Boa or Luau are the fallbacks). Python is ruled out as an in-game language but fine as an external client later.
  - **Slices 8 (scripting API) and 9 (live conduit, MCP, LLM seats) move past v1** as v1.1 and v1.2. v1 ends after slice 7 plus hardening (about wk 63). G6 is cancelled for v1; its paste and round-trip checks join the v1.1 trial. The +2 wk language reserve and the +1.5 wk slice-6 runtime are released.
  - **Findings kept for later (from the report):** a saved playbook is bound to seat, round, beacon ids and voxels, so cross-match reuse needs a rebind step (gateway stamps the header; verifier suggests the matching live beacon; `extract_template`); packaging and code signing are unbudgeted (~4–5 wk; macOS needs a Mac or CI runner); `deno run` does not type-check by default and `--allow-net=127.0.0.1:PORT` refuses `localhost`.
- The owner's direction for the rest of pass 6: finish the review interview quickly, then rewrite the spec as draft 6, "moving forward with v1 or even v0.1".
- **Item 2, art pipeline: option 1.** Author in MagicaVoxel (Goxel on Linux), `.vox` loaded in Rust with `dot_vox` (MIT), meshed by the same greedy mesher as the terrain; no importer addons. Lighting under the Pall: emissive voxels plus glow, a few short-range pooled unshadowed lights (beacons, fire), flood-fill voxel light baked into vertex colours on remesh, SDFGI optional High setting; no VoxelGI, no lightmaps. Placeholder palette models until the fun probe; art polish stays in slice 6.
- **Item 3, audio: option 1.** Minimal v1 from CC0/CC-BY sources only: Kenney (CC0), Freesound (CC0 and CC-BY), sfxr/ChipTone output; two or three music tracks from OpenGameArt or ccMixter with per-track licence checks (none in v0.1 is acceptable). Sonniss GDC bundles excluded. Godot: `AudioStreamPlayer3D`, three buses (master, SFX, music), `max_polyphony`, pooled players. A REUSE-style licence manifest and a credits screen for CC-BY attribution.
- **Item 4, round-limit tie-break: option 1.** Ordered: unsmoothed net worth at the final audit → enemy value destroyed → fewer beacons lost → shared win.
- **Item 5, resubmitting during the Lull: option 1.** The latest verified submission replaces the previous one, any number of times until the timer ends; the safe playbook is filed only if nothing verified was ever submitted.
- **Item 6, strings: option 1.** English only in v1; every user-facing string (UI, diagnostic catalogue text, plain-language playbook rendering) lives in one string table; diagnostic codes stay language-neutral; translation is a roadmap item.
- **Item 7, first release: option 1.** **v0.1 = the fun-probe build, released as a public preview about wk 19** (after probe fixes): one map, human vs the built-in operator, JSONC playbooks, editor with 8 templates, Survey-lite, notebook, placeholder art, no AI seats; unsigned Windows/Linux zips, macOS later. Each later slice ships a numbered preview; **v1 = the hardened build about wk 63**. Consequences: name clearance moves before wk 19; about 1 wk of packaging enters the skeleton; the remaining packaging and code signing (~3–4 wk, macOS needs a Mac or CI runner) is budgeted in hardening.
- **Item 8, packaging and signing: option 1.** Budget ~4 wk: ~1 wk in the skeleton (v0.1 unsigned Windows/Linux zips), ~3 wk in hardening (signed installers, three OSes). macOS builds and notarisation on GitHub-hosted macOS runners (free for public repos) with an Apple Developer ID (US$99/yr); Windows signing via SignPath's open-source programme or an OV certificate. No Mac hardware unless CI-only testing proves insufficient.
- **Item 9, name and theme (in progress; clearance sweep running on "Pharmakon").** The owner's thematic statement, to be written into draft 6 (§1 vision, §2 lore, §4 commander): *pharmakon* is remedy and poison in one object, the white-hat/black-hat duality of security tooling (the owner's inference from Metasploit). That duality is the reason for the air gap: who or what can you trust? Trust is engineered, not assumed: sealed orders, the seal inspection, no live control, the game never holds keys. **The commander is the physical conduit of that trust chain**: orders reach a beacon only by a body travelling there and interfacing on site ("touch to change"); nothing over the air can reconfigure a beacon; radio exists only once a Mast is earned and even then only carries data messages. If the name clears, "Pharmakon" becomes the title and "air gap" survives as the in-world term for the sealed Push and the trust model.
  - **Roadmap assumption (owner):** over-the-air communication is interceptable or otherwise hackable. Design consequences now: radio messages are data-only inputs (never reconfiguration) and are treated as untrusted; the message schema carries a sender and a Mast of origin so later jamming, interception, spoofing and decoys can be added without changing the playbook format; the on-site interface is the only trusted path and stays so. Roadmap items: interception (an enemy Sensor Spire or Mast reads broadcasts), spoofing (fake MARK_TARGET / GO), jamming, and counters (rolling go-codes, courier-only doctrine). Jammers and decoys were already roadmap from pass 5 item 1.
- **Review items (from the 192-agent spec review; agenda in `review-agenda.md`; 22 editorial fixes applied silently in draft 6; 21 findings moot under the v1 simplification).**
- **Item 10, power shortfall: option 1, "the beacon is the unit of power".** A dormant beacon powers down everything homed to it (fabricator, its structures, its bound units, which park where they stand at 0 kW); seal, sphere, interfacing and recycling stay lit off the key-core. The core autocannon runs off the deep bore, outside the grid, never shed. Grid supply = core surplus + Generators. Priority is kW-only (brownout order); $ is allocated by the urgency ladder, then round-robin within a band. One Generator per vent, output set by vent richness (story: the vent's heat is the limit, not the tap). A dormant beacon revives only when supply exceeds draw by a margin (Tuning), so domes do not flicker.
- **Item 9 decided: the title is PHARMAKOS** (the scapegoat: fed and honoured, then driven out or killed so the city stays clean — a role, not a person; same root as pharmakon, so the remedy/poison duality is inherited). Always shipped as a lockup, working subtitle "PHARMAKOS: THE SEALED ORDER" (owner may change the subtitle). AirGap remains the codename and the in-world term for the trust model. Clearance sweep (2026-09-13): Steam zero results; no PHARMAKOS mark in classes 9/28/41 on TMview; UK register empty. Risks: *PHARMAKOS – The Lunghouse* (Howling Stars, itch jam build Jan 2026, studio is Steam-verified, no Steam page yet); drift to "Pharmakon" (live Steam puzzle game, app 654660) and the German Pharmakon/pharmacon class 9 marks; a March 2026 romantasy novel; pharmakos.com is a pharma consultancy; @pharmakos taken widely. Actions: register pharmakosgame.com, pharmakos.io/.dev and @pharmakosgame handles plus a GitHub org now; professional clearance search before v1 (USPTO at source, phonetic variants, Italian FARMAKOS class 9 marks). Not legal advice.
- **Item 11, sphere and enemy overlap: option 1.** Definition: "A beacon's sphere is the ball of voxels within its radius (Tuning); a Resonance Spire enlarges its own beacon's sphere." Placement: the site must lie inside one of your own spheres and within the commander's placement range (Tuning, distinct from the ~4-voxel interface range); the commander stays for the full 12 s; death, leaving or an illegal site aborts with a full refund. The enemy-overlap ban is deleted: build, dig and deploy are legal anywhere; combat decides. One Resonance Spire per beacon. Sphere radius added to Tuning.
- **Item 12, reach: option 1, two words.** **Sphere** is the only range for anything physical: build, dig, salvage, guard escort, placement anchor. **Reach** is only what the seat knows: its spheres, the live vision of its own units and structures, Sensor Spire reveals, within memory N. Relay coverage is removed from every v1 reach definition (v1 has no relays); the Mast's v1 job is messaging only (operator threat/help messages, go-codes). Live retargeting reads the same seat reach as plan time: one reach, one verifier check. "Mast coverage" replaces "relay coverage" in §16 P7 and slice 4.
- **Item 13, sighting freshness: option 1.** Reach freshness (seen within N game-minutes, Tuning) is a qualification test at plan time only; the target is pinned at seal and launch never re-tests it; a moved or dead target is handled on arrival by the after-action or retarget rule. A sighting's age accrues on match game time across Pushes; Lull and recap add nothing. Attack target selectors take freshness from N (no per-condition `KNOWN_WITHIN` for targets); the §10 example reads "within the reach window". Survey scouts refresh sightings before the next Lull.
- **Item 14, Dispatches: option 1.** The player-facing Dispatch subsystem is cut from v1: no plan dispatch field, no editor box, no `get_dispatches`, no `dispatches[]` feed entry, no Mast hearing gate, no untrusted-text or anti-collusion rules, no length tuning item. Kept: a one-way engine **chatter** ticker — built-in seats emit short engine-authored lines from intent and events (Push-start ticker, recap, recordings), framed as intercepted open-air chatter. The proto field number is reserved; player Dispatches return in v1.2 with LLM seats (roadmap). Slice 4 becomes "radio" (Mast coverage, Sensor Spire, go-codes, P7); slice 5 keeps the flavour lines.
- **Item 15, target spread: option 1, seat-blind.** The built-in operator scores a target lower by how much pressure *any other seat* (human included) is already putting on it; no standings term (catch-up stays in BMI scaling and the award fund's rank multiplier). The §19 kingmaking/farming checkpoint moves to the nightly league (observable: share of damage aimed at the trailing seat), since the rule ships in slice 5 after the fun gate.
- **Item 16, match end: option 1, the one-tick rule.** End conditions are evaluated every tick; the Push halts at the first tick where fewer than two seats remain; standings freeze; the recap plays; the last seat standing wins. If no seat survives that tick, the final audit at that tick decides (no draw state). "The final tick" = the last tick of the last Push, before the recap. An eliminated human keeps the full-map spectator view; the control is "Abandon match (no winner)": recording saved, no result recorded, keep watching is the default.
- **Item 17, kill credit edge cases: option 1.** Only damage dealt by other seats counts, clamped to HP actually removed; reaching full HP clears the counters; an asset lost with no enemy damage on the clock (self-inflicted, terrain, disband on elimination, ruins) credits nobody; an eliminated seat's accrued share is dropped, not redistributed; integer apportionment by largest remainder, ties to the lowest seat id. Live standings show totals only; the recap's "shared kill" line shows each seat's share. §15 determinism row names the per-seat integer counters (≤3 per asset) and the remainder rule. No live damage bars, no own-share predicate (roadmap).
- **Item 18, audit and banking: option 1, value follows condition.** An asset's value is build cost × current HP wherever value is read: held value at the final audit (and live standings) and the recycle refund (50% of remaining value). Removal by recycle books destruction credit exactly as destruction does, via the damage split. $ stays at par; salvage unchanged; any seat's reclaim drones may salvage any wreck (value small, Tuning). Story: the Ledger pays for what stands, in the state it stands in. "Hoarding alone doesn't win" is deleted; hoarding joins the §19 audit risk line for the nightly league and fun gate.
- **Item 19, income timing: option 1.** At each recap the Ledger settles: BMI plus the fixed award fund (split among that round's winners in proportion to awards won) are credited to the treasury, shown as a recap line. During a Push the only income is mining and salvage. The final audit reads "value held after the final settlement". "Audit" now means only the final audit; §7 says "at each recap". `bmi_next` is "paid at the next recap".
- **Item 20, beacon death: option 1, local elimination.** When a beacon dies its structures become neutral ruins immediately (as on elimination): inert, no supply, no draw, no capability, unrepairable, zero audit value, salvageable by any seat's reclaim drones. No "unclaimed" state, no absorption. Units re-home and keep their role; where the new mandate has no job for them they follow only the common settings (roe, retreat_hp_pct) and idle at the beacon. A mandate switch clears the old mandate's settings and targets; the beacon's priority survives. Any of your Build beacons may repair any structure of yours within its sphere; a destroyed capability structure is never auto-rebuilt and needs a fresh 5 s on-site queue.
- **Item 21, commander: option 1.** Healing: repair drones repair the commander like any friendly unit; no passive regeneration. Death: the death counter and its growing respawn delay are per-Push state (reset each segment); a pending respawn always completes by segment end, so every Lull snapshot has a live commander at a known place. Score: the commander is not an asset (no build cost, never in held value or kill credit); it despawns at the tick its seat is eliminated; a kill pays only in the victim's lost interface time. Speed: commander speed is a world constant stated in §4 with a starting range (Tuning row); map scale and spawn separation are expressed in commander travel as well as raider travel; touches-per-Push confirmed at the probe.
- **Item 22, production and seams: option 1.** Seams are finite: digging consumes ore voxels, richness sets total yield, a spent seam reads as spent, and "richest" means richest remaining. A beacon's fabricator produces a drone only while its mandate has outstanding work its current drones cannot keep up with (unbuilt or damaged targets, reachable ore), and stops when the work is covered; no counts, no cap; headroom is the normal state. Tuning rows: ore yield per richness; backlog threshold. Stale `stop_at_pool` dropped.
- **Item 23, spending: option 1, paid means yours.** $ leaves the treasury the moment an order commits (deploy starts, structure queued, unit ordered). From that instant the asset counts at its full build cost for every purpose: audit, live standings, recycle refund (× HP per item 18), kill credit if destroyed mid-build. No partial value anywhere; construction time is HP and spectacle, not accounting; no refund if it dies mid-build. An order the treasury cannot cover fails like any other step failure (on_fail / skip_if); E0601 / W06xx cover plan time.
- **Item 24, plan execution: option 1.** At most one rule body runs at a time; no rule pre-empts another; only the self-preservation reflex interrupts anything. The reflex fires at commander HP ≤ 20% (fixed, not disableable, no `reflex_hp_pct` option): it aborts any visit, walks the commander to the safest own beacon, then the route continues from the step it was on; it re-fires only after new damage. Author-tuned bravery is an ordinary flee rule. Each interface row is one change that commits at the end of its own duration (multi-field edits are all-or-nothing); a resumed visit is a new visit and pays the handshake again. §6's unit-level "reflexes" are renamed "return fire". This supersedes the co-design doc's preempt flag and handler stack.
- **Item 25, locomotion and friendly fire: option 1.** Every unit uses the commander's locomotion (its own fixed speed, climbs 1-voxel steps); nothing flies in v1. Walls, craters and trenches block every unit, yours included (a moat seals you in too; Build's fill/dig undoes it). `breach_allowed` means the Attack may spend mortar or charge time digging or demolishing through instead of stopping. Friendly fire is on: your mortars and charges damage your own voxels, units and structures; built-in mandates avoid own assets when targeting. Pathing handles edited terrain from slice 1.
- **Item 26, fog leaks: option 1.** Live standings show a seat's own score and rank only; full per-seat detail at the recap and match end; rank (standing) is a field of the briefing. Fog is a per-match server-side policy applied by the gateway's fog filter: fogged by default, no-fog in casual matches, unlocked on elimination and at match end; scopes gate spectator tokens only, so "seat tokens never hold spectate.nofog" stays literally true and no token is reissued mid-match. The playtest bundle contains survey, metrics, recording and diagnostics log only; plans, drafts, the notebook and the private replay are excluded.
- **Item 27, notebook: option 1.** Size is 4,000 characters (Tuning; ~1k tokens at the 4-chars-per-token estimate), which always fits the smallest briefing level; no tokenizer. Saves contain plans, drafts and notebooks; the notebook is in the secrecy list; `save_notes` sits under the plan scope. The editor's notes box reaches it through the gateway like every other editor feature.
- **Item 28, v1 vocabulary: option 1, editor reach is the schema.** The v1 playbook vocabulary is exactly what the editor renders: route steps with guards, handlers (rules), selectors, on_death, fallback, options. `set_flag`, `clear_flag`, `branch`, `repeat` and the flag predicates leave v1 with their proto field numbers reserved; they return in v1.1 with the expert view and scripts (minor version, no converter). Load rejects any out-of-vocabulary construct with a verifier error (code plus JSON Pointer), never a silent strip. E03xx shrinks; jump/loop termination rules and the interpreter's branch/repeat work leave the skeleton. The editor pre-loads last round's plan as an editable, re-verified draft; the no-reuse sentence is deleted. P6 becomes "template ≤2 min, scratch ≤5 min". The "human plans less sophisticated than LLM plans" risk row is dropped.
- **Item 29, watch mode: option 1.** The v1 watch surface, all in the skeleton (+0.5–1 wk): one camera rig (own-fog camera with free-look and a follow-commander toggle), a live event list, 2–4× speed, skip = jump to segment end (free and instant), full map unlocked on elimination or match end. Probe criteria compute fun on unskipped Pushes and treat skip rate as a named pivot signal. The plan-vs-outcome report moves to the roadmap.
- **Item 30, segment length: option 1, a fixed ladder by round number.** Round 1 = 3 min, 2 = 4, 3 = 5, 4 = 6, 5 = 7, round 6 and later = 8 (values Tuning), independent of the round limit; the host may instead set one flat length for the match. The ladder is shown in the lobby; the coming segment's length (ms) is part of the frozen snapshot and returned by `get_status` and `get_briefing`; the editor's clock, fits pill and `render_plan` read it. The probe names its schedule: 3 rounds at 3 / 5 / 8 min via the flat override. "Match phase" is removed from the spec.
- **Item 31, checkpoints: option 1 — one gate on a v0.1-playable build; AMENDS item 7.** The standalone wk-17.5 fun probe is deleted. Pre-gate path: skeleton (spawn-distance rule applied to its seeded map) → destruction and combat (mortar, wall, demolition charge, repair/reclaim drones, Build mandate) → a short authoring stage (rule list, size meter, Probation preset with 3–4 scripted memos and the guided first-Lull pick, plus the Resonance Spire) → **one go/pivot gate at about wk 33–35** (exact weeks re-baselined in draft 6). **v0.1 = that gated build, released publicly** (not the wk-19 skeleton). Licences generally, Sensor Spire, Radio Mast, chatter and the award fund follow the gate; slice 6 shrinks to polish and samples; P6 runs at the authoring stage while its fallback is still affordable. Net weeks roughly flat. Item 7's consequences (name before release, ~1 wk packaging before the first public build) now attach to the gate.
- **Item 32, fun-gate criteria: option 1.** A §16 gate row: When = the single gate on the v0.1 build; Owner = the owner alone, criteria frozen before the authoring stage starts; Setup = 3–5 testers, one Probation-shaped match (3 rounds at 3/5/8 min) vs built-in seats; Metrics (from the playtest bundle) = "play another round?", Push time focused vs unfocused on unskipped Pushes, Lull time used, skip rate; Go if a majority answer yes and Push attention is a stated majority of the segment; Fallback in order = shorter default Push ladder with more rounds → pull the roadmap manual-control round into v1 → narrow v1 to the Probation-sized match. A tester who cannot file a plan is a wizard bug plus the guided pick, never a fun result; P6 is judged separately at the authoring stage. The MCP smoke test leaves the gate.
- **Item 33, stage ordering: option 1.** (a) A minimal Build mandate (one blueprint, the Generator; the 2 starting build drones; no terraform, repair threshold or protected areas) plus a single-treasury Quartermaster stub move into the skeleton (+~1.5 wk); slice 1 keeps the grid, brownouts and full Build settings. (b) The harness is split: wk 3–5 = AGENTS.md/CLAUDE.md, `cargo xtask ci`, lints, golden files, determinism CI; a named half-week at the skeleton's end = `gamectl scenario run` and headless screenshots. (c) P1 certifies QUICK plus per-rule cost per decision tick at slice 1; FULL ≤50 ms p99 and the plan-size budget become slice 3's exit criterion after HPA*. (d) G3′ = a synthetic tick check in the spike, a real 300-unit/40-beacon measurement as the combat stage's exit (sets the per-map power budget for the generator); "scrub ≤1 s" moves to slice 7. All weeks re-baselined in draft 6.
- **Item 34, balance evidence: option 1.** The nightly league is deleted. Three fixed hand-written adversarial playbooks — Rusher, Turtle, Hunter — run nightly in CI by `gamectl scenario run` against the Balanced built-in operator on a fixed seed set, from the combat stage onward, each with a written alarm band (an arm winning more than ~65% of seeds opens a tuning task). Hunting counters, rush/turtle watching and the §19 risk row point at these scenarios. "Headless" is split: the headless sim runner (sim plus playbooks, no client; hash parity with the client path) is v1 dev harness and is what G3′'s "≥4× headless speed" measures; only the player-facing accelerated AI-only match mode stays Roadmap.
- **Item 35, art/audio/testers/macOS in the plan: option 1, no new stage.** v1 ships placeholder voxel models and CC0 sound; art polish and the sound pass live inside slice 6's existing weeks; packaging uses ~1 wk before the first public build and ~3 wk in hardening; macOS is a signed CI artefact (replaces "best-effort and unsigned"). Gate testers are recruited from the owner's own reach, named in the gate row. §1 and §8 stop promising "spectacular to watch" in v1; the art pass is roadmap.
- **Item 36, licence triggers: option 1.** All three triggers are evaluated once, at the segment-end settlement, announced in the recap, held for the next Lull. State checks at that tick: 3+ beacons held; Generators running on 2 vents. "Blindsided" is an occurred-this-segment flag: a beacon or licensed capability structure destroyed by an attacking unit with no sighting in the seat's knowledge within reach memory N; walls, units and drones never count. Licences stay permanent. The "blindsided anti-gaming" tuning item is deleted.
- **Item 37, catch-up: option 1, one dial on held value.** BMI scaling by standing is the only place rank touches income; standing for BMI is the rank on held value ($ plus HP-weighted build cost of beacons, structures and units) among living seats, tie-break lower seat index; eliminated seats leave the ladder. The award fund keeps its fixed size and is split purely in proportion to awards won; the rank multiplier is deleted (and dropped from Tuning). Displayed standings keep the full audit score. Ledger line: "the ration is set by what you hold, not what you broke".
- **Item 38, map generator with fewer than 3 seats: option 1.** Unoccupied spawn zones are terrain only: the generator omits the unused zone's near vent and starting seam; terrain is unchanged (in-world: a claim the Ledger never issued). Fairness tests check vent and ore parity and the ~1.5-early-Push spawn travel distance across the occupied spawns. The gate row names its seat count (Probation: 2 seats).
- **Item 39, verifier depth: option 1.** `verify_plan` takes `depth: quick | full` (default full); submit always runs full. Depth is the fifth input of the determinism sentence; `report_hash` is comparable only within one depth. P1 gates "QUICK ≤5 ms p99; FULL ≤50 ms p99 at the size budget"; "editor re-check ≤16 ms" is removed and editor typing feel is a P6 concern.
- **Pass 6 interview COMPLETE (39 items).** Draft 6 (v0.6) was written by a 25-agent workflow, published, then reviewed by a 60-agent workflow (65 findings; 61 editorial edits applied; republished as "Spec v0.6 draft, reviewed").
- **OPEN after the draft-6 review (six decision items, not yet asked; details and recommendations in `handoff.md`):** R1 gate segment schedule (3/5/8 not producible by the ladder or a flat length → recommend a per-round length override); R3 chatter gating (recommend ungated engine flavour, no Mast); R9 name clearance timing (recommend the professional search before the v0.1 public release); R10 numbered previews after the gate (recommend keeping them, unsigned zips from green CI); R22 the two senses of "seal" (recommend keeping both, glossing the physical sense, and rewording "when a seal goes dark"); R46 cross-reference style (recommend "section N" everywhere).
- **Item 40 (R1), gate segment schedule: option 1, per-round length list; AMENDS item 30.** The host's "flat override" becomes a per-round length list: empty = the ladder (3/4/5/6/7/8), one value = that length for every round, N values = round i takes the i-th entry and rounds beyond the list take the last entry. The gate schedule "3 rounds at 3/5/8 min" is produced by the list `[3,5,8]`. The list is shown in the lobby; the coming segment's length (ms) stays in the frozen snapshot, `get_status` and `get_briefing`. Item 30's "one flat length" wording and the gate row's "via the flat override" are superseded.
- **Item 41 (R3), chatter gating: option 1, ungated engine flavour.** One-way engine chatter (from S5, item 14) is open-air crew talk overheard on site, not mast traffic: it plays in every match with no Radio Mast precondition. §8's Mast gate on chatter is deleted; the Mast keeps its licence and knowledge roles only. The radio-as-untrusted-data theme is carried by licences, the Mast and the roadmap interception work, not by chatter.
- **Item 42 (R9), name clearance timing: option 2, professional search before v1, exposure stated.** The professional trademark search stays a pre-v1 task (hardening stage), not a gate criterion. v0.1 ships under PHARMAKOS on the strength of the owner's own sweeps (item 9); the spec states the exposure: a failed search after v0.1 means a rebrand of a released build (domains, handles, early press). Item 31's "name before release" means the sweeps plus registrations (pharmakosgame.com, pharmakos.io/.dev, @pharmakosgame, GitHub org), not the professional search.
- **Item 43 (R10), numbered previews after the gate: option 1, keep them cheaply.** After v0.1, each slice ends with a numbered preview cut from green CI: v0.2 at S4 (wk 41.5), v0.3 at S5 (45.5), v0.4 at S6 (53), v0.5 at S7 (57.5). Each is an unsigned zip (Windows, macOS, Linux) with a tagged changelog and a two-line "how to open an unsigned build" note; no packaging or signing work until hardening. Restates item 7's preview cadence for the post-gate slices.
- **Item 44 (R22), the two senses of "seal": option 1, keep both, gloss the physical one.** "Seal" keeps both meanings: the seal on sealed orders (a filed playbook) and a key-core's seal (the lit mark that shows the beacon is powered). The physical sense is glossed at its first use in §5; §2's "When a seal goes dark…" is reworded to "When a Stake falls…" so a brownout is not read as ruin; the glossary carries both entries.
- **Item 45 (R46), cross-reference style: option 1, "section N" everywhere.** Every cross-reference in the spec reads "section N" (lower-case, linked); "§N" and bare section numbers are rewritten. The decisions log keeps its own "§2.x" numbering for its passes.
- **Draft-6 review decision items COMPLETE (items 40–45).** Next: apply items 40–45 to `pharmakos-spec.html`, republish (same URL), then move the workspace to a project folder and start the stack spikes (G4, G1).
- **Items 40–45 applied to the spec (2026-09-13).** 14 text edits plus every cross-reference rewritten as a linked "section N"; a 55-agent verification pass (7 checkers, 3 refuters per finding: 16 candidates, 3 confirmed) then fixed the draft-5 changelog bullet's "flat-length option", renamed engine chatter's in-world name from "traffic on the air" to "crew talk on site" (the air = radio = Mast in this spec's vocabulary) and reworded the §2 chatter lore to match. Republished as "Spec v0.6, items 40–45 applied".
- **Harness part 1 bootstrapped (2026-09-13).** Repo at `C:\Users\PC\pharmakos`; toolchain installed (rustup 1.98.1 MSVC, MSVC 14.43, SDK 22621, protoc 36, buf 1.73, Godot 4.7.2); `cargo xtask ci` green. Two contract questions raised by the bootstrap fixer, pending the owner's answer: playbook durations int32 vs int64 (int32 applied provisionally: bare JSON numbers, matches the §10 example); the envelope `kind` tag reserved as field 7 rather than defined (to be added with the skeleton's envelope).
- **Item 46, playbook durations: option 1, `int32` game milliseconds.** Every duration field in `gp.v1` (`timeout_ms`, `cooldown_ms`, `hold.ms`, `known_within_ms`, …) is `int32`, so canonical proto JSON emits bare numbers and the §10 example loads as written; negative values are rejected by the verifier with a code and a JSON Pointer, not at decode. `int64` (quoted strings in JSON) and `uint32` (decode-time failure without a pointer) rejected.
- **Item 47, envelope `kind` tag: reserved now, defined with the skeleton.** `Playbook` reserves field 7 / name `kind`; the real field and its enum arrive with the walking skeleton's envelope work, when the example and golden files change together.
- **Initial commit made 2026-09-13** (harness part 1, spec v0.6, spike plans), signed off per the DCO rule.
- **Spike G4 measured on Windows (2026-09-13), verdict PROVISIONAL pending the three-OS CI run.** Toy sim in `spikes/g4-determinism` (rustc 1.98.1 MSVC; xxhash-rust 0.8.18, rkyv 0.8.18 little-endian/32-bit pointers, postcard 1.1.3, imbl 7.0.2, all pinned with Cargo.lock). G4-b: 50/50 save/restore round trips in fresh processes for both formats. G4-c: 20/20 fork equivalences, parents unperturbed. Repeat, core-pinned and debug runs give the same trace digest; 26 XXH3 reference vectors pass; default build carries no fork symbol. Contract candidates for the owner (interview to follow): the canonical encoding and seed `0x5048_4152_4D4B_4F53`; snapshot format (postcard ~4.5 KB vs rkyv ~6.8 KB at tick 4800, both hash-transparent); the split counter-based RNG streams; the determinism rule set. Full numbers in `docs/spikes/G4-determinism.md` §9.
- **Spike G4 verdict: GO (2026-09-13).** The `spike-g4-determinism` workflow (run 34802164421, commit `6e7a156`) found the 10-match per-tick hash traces and both snapshot hash lists byte-identical on ubuntu, windows and macOS (ARM); with G4-b 50/50 and G4-c 20/20 already measured, the per-tick xxh3 hash is a cross-OS, cross-architecture contract and no part of the fallback is taken. The four frozen choices (hash encoding + seed, snapshot format, RNG construction, determinism rule set) go to the owner as interview items 48–51.
- **Item 48, hash encoding and seed: option 1, approved as implemented in spike G4.** Canonical little-endian byte encoding of the state tables in id order (lengths as `u32`, fixed-stride `Option` encoding, `ENCODING_VERSION = 1` first), digested by xxh3-64 under `STATE_HASH_SEED = 0x5048_4152_4D4B_4F53`. Pinned values: `digest(b"") = 0xFE1AF732B02810AD`, `digest(b"pharmakos/g4") = 0xE17026C8A5EA4D30`. Changing the seed or the version byte invalidates every golden file.
- **Item 49, snapshot format: option 1, postcard.** postcard 1.1.3 (`default-features = false`, `use-std`) with serde; every snapshot field fixed-width, `Option` as a sentinel, `SNAPSHOT_VERSION` in the bytes and checked on restore. Measured 4,563 B at tick 4800, 0.04 ms save, 0.24–0.32 ms restore, byte-identical across ubuntu/windows/macOS. Known bend of the "never serialise usize" rule: `Vec` lengths are LEB128 varints, value-identical on every target for realistic lengths; documented at the encoder. rkyv (7,160 B, heavier dependency tree) and a hand-written codec rejected.
- **Item 50, RNG construction and stream ids: option 1, approved as implemented in spike G4.** Counter-based, stateless: `draw(n) = mix64(key ^ n·GOLDEN)` with `key = mix64(k1 ^ (seat<<32) ^ sub)`, `k1 = mix64(k0 ^ tick·GOLDEN)`, `k0 = mix64(match_seed ^ stream_id·GOLDEN)`, `GOLDEN = 0x9E3779B97F4A7C15`, `mix64` = the SplitMix64 finaliser with wrapping multiplies; ranges by multiply-shift. Stream ids are explicit constants, additive only, never renumbered: Combat = 1, Spawn = 2, Map = 3. Revisit only if a future feature needs audited statistical quality (large sampled fields in the map generator).
- **Item 51, determinism rule set: option 1, approved as implemented in spike G4.** In sim crates: no `f32`/`f64`; no `as` casts (`From`/`TryFrom` with an explicit failure mode); no `HashMap`/`HashSet` (`BTreeMap`, sorted `Vec`, `imbl::OrdMap`); no wall-clock time; `overflow-checks = true` in every profile, release included, `debug-assertions = false` in release; every sort key ends in a unique id; fixed-width types only in hashed state, lengths as `u32`; no recursion. Enforced by crate-level `deny` attributes plus the workspace clippy configuration; the walled presentation module carries an explicit allow list.
- **G4 contract items 48–51 COMPLETE.** Harness part 1 may now encode them (golden determinism hashes, the lint set) as the walking skeleton's sim crate takes shape.
- **Spike G1 verdict: GO — on Windows/Vulkan/Quadro P4000 only; the Linux half of the gate (plan §3 step 8) is outstanding (2026-09-14).** Toy build in `spikes/g1-remesh` (own greedy mesher in Rust, 288 chunks of 32³, single-voxel explosions, two upload paths into Godot 4.7.2 via gdext 0.5.5), measured over 25 Godot launches and 4 headless `meshbench` passes; full numbers in `docs/spikes/G1-destruction-remesh.md` §10. Gate rows, path B, 3 × 3 600-frame repetitions: **G1-a** remesh latency p50 0 / p90 1 / p99 **1** / max 2 frames (limit p99 ≤ 3), nothing left queued; **G1-b** 20 explosions/s sustained for 3 600 frames (frame-driven, so 209–220/s wall-clock with vsync off); **G1-c** frame time p99 **5.65 ms** median of 3 (limit 16.67), max 11.0 ms, 0 of 3 600 steady-state frames over budget; ~2.6× p99 headroom, and 40 explosions/s still holds latency p99 at exactly 3. Mesher CPU ~450 µs per chunk in-engine (headless best-of-5 p99 343 µs flat / 503 µs cratered), 0.001 allocations per meshing, 16-bit indices with 10× headroom, peak heap 18.8 MiB, video memory 82.9 MiB. Path B (direct `RenderingServer` RIDs) beats path A (`ArrayMesh`) on the two clean measurements — 57 vs 84 µs per chunk upload (1.48×, non-overlapping) and 0 vs 5 798 `ArrayMesh` objects per minute — with identical geometry; frame time is a tie. Budget candidate K = 4 chunk surfaces, B = 512 KiB per frame, nearest-camera-first with an ageing term. No rung of the fallback ladder is taken. Three real bugs found and fixed, all carried into §10.12 for the skeleton: Godot 4.7's front face for triangle primitives is clockwise under `CULL_BACK` (the first vista showed the inside of the terrain) — *corrected 2026-09-14: this entry first said “counter-clockwise”, inverting the fact the plan's §10.7 records; the spike's geometry was right and only the recorded word was wrong, caught when the harness PR copied the lesson into `crates/mesher`'s crate doc*; a `Gd<Material>` dropped after `get_rid()` leaves a dangling RID that draws nothing; the in-place surface update must compare the index array, not just the counts (29 of 2 570 in-place updates in the headline run had changed indices). Also: `godot --headless` cannot take a screenshot (dummy driver), so the CI `geometry` job renders under xvfb + lavapipe and compares against a committed 1280×720 golden (0 of 921 600 pixels differ on Windows/Vulkan; lavapipe untested). Not run: Linux/Vulkan on a real GPU, D3D12 on Windows, the Defender-exclusion A/B, a vsync-on 60 Hz confirmation. Three review findings corrected the first write-up: frame times come from a raw monotonic clock, not `_process(delta)` (the physics jitter fix quantises it to 1/720 s); K = 4 has no measured frame-time justification (chosen as the smallest K meeting latency); only the mesher's p99 is a usable CI budget (the max flaps 3×). The four-part decision plan §9 asks for (upload path, budget rule, chunk size, thread + wall placement) goes to the owner as items 52–56 below.
- **Spike G1 CI green (2026-09-14, run 34823744065).** `spike-g1-remesh` passes on every job: mesher CPU budgets on ubuntu and windows, and the Linux `geometry` job under xvfb + lavapipe matches the Windows/Quadro golden (mean 0.0039/255, 1.14 % of pixels differ at all, 0.000 % by more than 32). That measures the geometry half of the cross-OS question; the Linux frame-time half is still unmeasured (no GPU runner). Four things learnt on the way, recorded in the plan's §10.12 for the skeleton's CI: a non-editor Godot loads GDExtensions only from `.godot/extension_list.cfg`, so every fresh checkout needs `godot --headless --import` first; do not hard-code the lavapipe ICD path (the loader then finds no driver and Godot silently falls back to OpenGL 3); GitHub hides job logs *and* step summaries from logged-out viewers, so CI diagnostics are published as `::notice::` annotations; and the golden was regenerated once, explained: the shot frame moved from 880 to 180, and a Quadro render and the lavapipe render both differed from the old golden by the same 4.278 % over 32.
- **Item 52, G1 cross-OS half: option 1, the single-platform frame-time GO is accepted.** Geometry is measured identical on Linux (lavapipe, run 34823744065); frame time and latency are measured on Windows/Vulkan/Quadro P4000 only, with 2.6× p99 and 3× latency headroom. The Linux frame-time half is deferred to G1's re-run against the walking skeleton under synthetic load (§2.4), where it is added if a Linux GPU machine or runner exists by then; the `frame-time` CI job stays `if: false` until a GPU runner exists. Rejected: a live-USB/dual-boot or cloud-GPU run now (half a day to a day, blocks items 53–56, a cloud card is not this card) and WSL2 (a D3D12 translation layer, so it would characterise the wrong driver).
- **Item 53, upload path: option 1, path B (direct `RenderingServer` RIDs) with path A (`ArrayMesh` + `MeshInstance3D`) kept as a build-time switch in the skeleton's bridge.** Measured: B is 1.48× cheaper per chunk upload (57 vs 84 µs), writes 77.0 vs 96.6 KB per chunk, and creates no resources against A's 5 798 `ArrayMesh` objects a minute; frame-time p99 is a tie within noise; geometry is byte-identical. The switch exists so that a retreat to A is a flag, not a rewrite, because B carries four bug classes the bridge must keep guarding against: dangling RIDs that silently draw nothing (a `Gd<Material>` dropped after `get_rid()`), the surface-format probe, the index-array equality guard before an in-place region update (counts alone are not enough: 29 of 2 570 in-place updates in the headline run had changed indices), and a colour quantisation that must match Godot byte for byte. Rejected: B only (a retreat becomes a rewrite under pressure) and A only (churn of that order is the load that shows as hitches on a larger world or a higher K, which the spike did not measure).
- **Item 54, per-frame upload budget: option 1, K = 4 chunk surfaces and B = 512 KiB per frame, and the plan's selection rule is amended to "the smallest K that meets the latency gate".** The plan's "largest K keeping frame p99 under 16.67 ms" selects unbounded, i.e. no budget; the amended rule selects K = 4 (K = 1 and 2 fail G1-a outright, K = 8 and unbounded also pass). Drain order: anything older than 2 frames first, then nearest-to-camera, ties by chunk index; a chunk re-dirtied while queued is coalesced and keeps its earliest issue frame; the first chunk of a frame always goes through even if it alone exceeds B. B = 512 KiB is one doubling above the point where B stops binding (256 KiB), a margin for a heavier future vertex format, not an optimum. Recorded honestly: no measured frame-time reason separates K = 4 from K = 8 on this machine (the maxima were noise), so the choice is structural, a bound on per-frame work; and the ageing term never fired in any run, so it ships as insurance against the drain order's starvation mode with no measurement behind it. K and B are tuning values in the rules table, stamped into the rules hash, not constants in code. Rejected: dropping the ageing term (ships a known starvation mode untested either way) and K = 8 (doubles the worst-case work the budget exists to bound while the gate is met at a third of its latency allowance).
- **Item 55, chunk size and destruction granularity: option 1, unchanged — 32³ chunks and single-voxel destruction; no rung of the G1 fallback ladder is taken.** Measured on Windows/Vulkan: dirty fan-out max 8 chunks per explosion at radius 4 (pad = light reach + 1, both derived from `LIGHT_MAX`/`LIGHT_ATTEN`), ~450 µs mesh cost per chunk, 16-bit indices with 10× headroom (6 660 vertices max against 65 536), peak heap 18.8 MiB, video memory 82.9 MiB, radius 8 at 20 explosions/s still within the gate. Caveat carried from item 52: the headroom is Windows-only; if a Linux GPU result is ever materially worse, rung 1 (2³ edit groups) is the lever, and it does not touch the sim's voxel grid. Rejected: taking rung 1 pre-emptively (gives up single-voxel destruction for a problem no measurement shows) and 16³ chunks (eight times the chunk count and draw calls, and the chunk size is a determinism-adjacent contract).
- **Item 56, mesher thread and the presentation wall: option 1, the mesher runs on the main thread, in its own walled crate `crates/mesher` (`pharmakos-mesher`).** Measured: 450 µs per chunk × K = 4 is 1.8 ms of a 16.67 ms frame against a measured frame p99 of 5.65 ms, and Godot's single-safe thread model makes path B's `RenderingServer` calls legal from `_process` with no render-thread hop; a worker pool is not needed for v1's load and would put a second thread near world state. The wall line, as G1 found it: everything up to and including the voxel edit, the dirty set and the drain order is integer; floats begin at the first vertex coordinate; nothing a float touches is read back by the sim. The mechanism: `crates/mesher` takes integer chunk data in and produces vertex buffers out, is listed in `WALLED_PACKAGES` (and the `clippy.toml` header), is depended on by `client-gdext` and never by `sim`, `plan-core`, `verifier`, `operator` or `gateway` (`wall-guard` enforces it), and links without gdext so the headless CPU proxy and the CI geometry check can reuse it. The spike's per-file `#![deny(clippy::float_arithmetic)]` shape does not transfer. This resolves the AGENTS.md §3 placeholder for the mesher; the `math` crate question stays open for the skeleton. Adding the crate is a §5 contract change, approved here; it lands as a small harness PR at the walking skeleton together with the AGENTS.md crate-map edit. Rejected: the mesher inside `client-gdext` (breaks the thin-client rule and nothing headless can reuse it without linking gdext) and a worker pool from the start (no measurement asks for it, and path B's uploads stay on the main thread regardless).
- **G1 decision items 52–56 COMPLETE (2026-09-14).** Spike G1 is closed: GO, path B with an A switch, K = 4 / B = 512 KiB under the amended rule, 32³ chunks and single-voxel destruction unchanged, main-thread mesher in a walled `mesher` crate. Next: spikes G2 + P3 (pathing and travel estimates) and G3′ (segment budget), then the walking skeleton.
- **Spike G2+P3 verdict: GO at cluster 32 — the editor may promise a travel time, not a range; the cross-OS half of step 9 (path identity on Linux) is pending the first CI run (2026-09-14).** Toy build in `spikes/g2-pathing` (own HPA* over a 2.5D surface graph of 384×384×64 — 147 456 columns, one node per column; integer octile costs 10/14 plus a climb surcharge of 4; a hand-written binary heap ordered by the total order (f, h, node_id); dense generation-stamped scratch, no `HashMap`, no floats, no `as`, no recursion, `u32` ids, `i32` costs with the map's maximum asserted at start-up; cluster = chunk footprint, entrances rescanned and intra-cluster edges rebuilt eagerly per dirtied cluster plus one CSR rebuild per tick; a union-find connectivity oracle answers “no path” before any search; the estimator is the abstract search alone with ticks = ceil(cost / 3)); no dependencies at all; 42 tests, including A* against exhaustive Dijkstra on 1 000 pairs with 0 mismatches and a test that the library reads no clock. Full numbers in `docs/spikes/G2-P3-pathing.md` §9. Gate rows at cluster 32 (three runs of 20 000 repaths under 20 craters/s, 10 000 estimates, 2 000 accuracy pairs): **G2-a** repath p99 cold 1.80 / 1.84 / 1.84 ms (limit 5), max 2.2–4.1 ms; **P3-a** estimate p99 0.82 ms (limit 1), 0.887 ms within the worst band (>256 cells); **P3-b** estimate vs walked |error| p90 **2.6 %** (limit 15), 1.3 % on routes over 256 cells, and never optimistic — the signed error equals the absolute error at every percentile, because the estimate prices the unsmoothed refinement and the smoother only removes cost. Cluster sweep: 16 passes G2-a (1.08 ms) and P3-b (3.8 %) but its estimate p99 inside the >256-cell band is 1.076 ms against an aggregate of 0.972, so it fails P3-a on exactly the routes an ETA is read for, and its cluster is no longer a chunk; 64 fails P3-a outright (1.43 ms, every band) and pays 18.5 ms p99 to repair one dirtied cluster. Accuracy improves as clusters coarsen (3.8 → 2.6 → 1.6 %), so latency, not accuracy, is what fixes the size. One abstract level: endpoint insertion is 91 % of a median query's expansions (1 774 of 1 943), so a second level could save at most 9 %. Repair, not search, is what a walker actually waits for: cold + the tick's repair share p99 2.95 ms, max 8.9 ms; 1.48 ms p50 per dirtied cluster, 1.35 clusters per crater, 0.50 ms per CSR rebuild — about 2.5 ms per crater. Peak demand 1 474 repaths per game-second against 1 235/s of single-thread throughput, which is plan §6's case for a deterministic per-tick repath cap. “No path” fires on 4.5 % of repaths and costs 0.0001 ms; one walker was sealed in by its own destruction, which the sim needs as a designed state. Recorded separately and not part of the gate: fog (×1.5 per abstract edge when either endpoint is unknown) |error| p90 48.6 %, capped at +50 % over the clear estimate; dynamic terrain (the estimate at issue against the tick of actual arrival while craters fall) |error| p90 12.5 %, p99 62.5 %, so the gate's “on static terrain” is load-bearing. Memory: 4.1 MiB of structures, 11.2 MiB peak live heap, 14.5 MiB peak working set read from outside the process. Same-machine path identity: four bench runs walked byte-identical routes and two `--hash-paths` files (3 200 hashes covering the pristine map and 300 crater repairs) are identical under `cmp`, digest `ef29afabb1d26148` at cluster 32. Measured differently from the plan, stated in §9.9: fog is priced per abstract edge, not per cell (the per-cell count is the refinement the estimator may not do); “cold” means the first repath against a CSR rebuilt this tick, because the design holds no per-query cache to be cold; the second level was bounded from the expansion split rather than built; the generator's columns are 100 % walkable, so the graph's structure lives in the 1-voxel step rule (20 craters/s removed 131 356 directed steps and shattered the largest component from 130 937 to 35 461 cells); peak RSS was sampled from outside because the spike may take no dependency. One structural fix identified and deliberately not built: a corner entrance, which would remove most of the ≤32-cell band's error (p90 9.2 %, max 66.6 %). 28 review findings over three lenses, all applied: four blockers (the generation counter was reset without re-zeroing the stamps, so a query after a resize could read stale state; cold/warm was a label rather than a measurement; `--hash-paths` covered only static queries, not the repair path; the CI `path-identity` job inherited a working directory that does not exist on its runner), the rest majors and minors including the CI thresholds, which now assert the G2-a gate itself (5 ms) and 2.5 ms for the estimate as regression alarms. Tuning values the numbers depend on, all to become rules-table data at S3: 10 / 14 / 4, speed 3 cost units per tick with ceil, headroom 2, long-run 6, lookahead 16, cluster 32, crater radius 3. The decision plan §8 asks for — the headline and its five parts — goes to the owner as items 57–62 below.
- **Spike G2+P3 CI green (2026-09-14, run 34861229486).** `spike-g2-pathing` passes on every job on the first run: fmt, clippy `-D warnings`, 42 tests, the bench and accuracy gates on ubuntu and windows, and the `path-identity` job — Linux and Windows agree on all 3 203 lines of the path-hash file at every cluster size (digests `d98ba7776dc48e9c` / `ef29afabb1d26148` / `8daf1375f13e8619` for 16 / 32 / 64), which is the cross-OS half of plan §3 step 9 and closes the last open measurement: the (f, h, node_id) total order holds across operating systems. The hosted runners also reproduced the gate: ubuntu cold repath p99 1.37 ms and estimate p99 0.67 ms, windows 2.21 ms and 0.98 ms, all at the same routes digest `360bb858b87df6e9` across the three repeats on each OS; the Windows runner is the slower of the two and comes within 2 % of the 1 ms estimate budget, which is why the CI estimate alarm sits at 2.5 ms. The G1 lessons carried over cleanly: concurrency + timeouts, assertions inside the binaries, diagnostics as `::notice::` annotations read through the check-runs API, `python` via setup-python on both runners.
- **Item 57, the G2+P3 headline: option 1, the editor promises a travel time, rendered as a number.** Measured on static terrain at cluster 32: estimate vs walked |error| p90 2.6 % against the 15 % budget, 1.3 % on routes over 256 cells, and never optimistic (the estimate prices the unsmoothed refinement and the smoother only removes cost), so a unit is never later than promised on the terrain the playbook was sealed against. Two rules the editor must honour, carried into item 61's contract: a short route's ETA is rounded generously or shown in whole seconds (the ≤32-cell band has p90 9.2 % and max 66.6 %, i.e. one second on a two-second route), and a fogged leg is drawn as the upper bound it is, not as an ETA (48.6 % p90 under the ×1.5 rule). Recorded honestly: the promise is made on static terrain and the Push is not static — under 20 craters/s the same estimate is 12.5 % p90 and 62.5 % p99 against actual arrival, and the player sees the discrepancy at arrival, not the caveat. Plan §6's fallback (“show ranges; favour arrival triggers”) is not taken in any part; the playbook vocabulary keeps its clock-phrased conditions alongside the arrival-phrased ones. Rejected: a range anyway (discards a measurement 5.8× under budget and drifts the vocabulary towards arrival triggers for no measured reason) and a number on clear routes with a band on contested ones (two renderings to teach, and “contested” is not decidable by the estimator without the simulation work §3 forbids; the decidable part, fog, is settled in item 61).
- **Item 58, cluster size and abstract levels: option 1, clusters of 32×32 cells (one chunk footprint) and a single abstract level.** Measured at 32: repath p99 1.84 ms, estimate p99 0.82 ms (0.887 ms inside the >256-cell band), error p90 2.6 % — the only size that passes all three assertions including on long routes. A cluster equals a chunk, so a crater's dirty chunk list is its dirty cluster list with no translation; repairing one dirtied cluster costs 1.48 ms p50 / 3.30 ms p99, 1.35 clusters per crater, plus 0.50 ms per tick for the CSR rebuild. One level, on evidence rather than intuition: endpoint insertion is 91 % of a median query's expansions (1 774 of 1 943) and is cluster-local by construction, so a second abstract level could save at most 9 %; coarsening to 64 is the same trade and it breaks the 1 ms budget. Recorded honestly: 32 leaves 18 % of the estimate budget and the band max already touches 1.03 ms; the hosted Windows runner came within 2 % of the budget, so a slower target machine or a busier map is the risk, and the lever if it bites is the endpoint sweep (cache the commander's start transitions while it stands still; goals are usually static beacons), not the abstract graph. If a later map is much larger than 384², re-check the ratio of abstract expansions to endpoint-sweep expansions before revisiting a second level. Rejected: 16 (estimate p99 1.076 ms inside the >256-cell band — fails P3-a on exactly the routes an ETA is read for — twice the graph to repair, the worst dynamic error at 16.3 % p90, and a cluster that is no longer a chunk), 64 (fails P3-a by 43 % in every band and pays 18.5 ms p99 per cluster repair), and 32 with a second level built as insurance (a second deterministic graph to repair per crater for a measured ceiling of 9 %).
- **Item 59, the integer cost model: option 1, `STEP_CARDINAL = 10`, `STEP_DIAGONAL = 14`, `CLIMB_SURCHARGE = 4` per one-voxel step up or down, `MOVE_COST_PER_TICK = 3`, and cost → ticks by ceiling (`ticks = (cost + 2) / 3`), all four as rules-table data stamped into the rules hash.** 10/14 is the octile approximation, so no square root and no float anywhere and the heuristic stays admissible and consistent; ceiling is the rounding that keeps the estimate one-sided (never optimistic, the property item 57 rests on) and is what the walk integrator is tested against; the map's maximum path cost is 2 654 208 against `i32::MAX`, 810× headroom, asserted at start-up. Recorded honestly: ceiling adds up to one tick (50 ms) of pessimism per query, a systematic floor under the measured 0.7 % median bias; the surcharge has no gameplay evidence behind it and is a tuning question for S3. Rejected: a finer 100/141 scale (ten times the accumulated magnitude for no measured gain — the octile error is not what limits accuracy, the missing corner entrance is), an asymmetric up/down surcharge (makes the graph directed, which the CSR and the connectivity oracle would both have to learn; raise as tuning if climbing should feel expensive), and round-to-nearest (can round a walk down by up to half a tick, breaking never-late).
- **Item 60, incremental repair: option 1, cluster = chunk footprint, repaired eagerly at edit time — rescan the dirtied clusters' entrances plus the neighbours whose border transition set actually changed, rebuild their intra-cluster edges, then one CSR + connectivity rebuild per tick.** “Affected neighbour” is computed, not assumed; three tests pin it (repair equals a from-scratch build, equals the conservative all-neighbours set, and holds at every cluster size). Measured at 32: 1.48 ms p50 / 3.30 ms p99 per dirtied cluster, 1.35 clusters per crater, 0.50 ms per tick for the rebuild — about 2.5 ms per crater, 5 % of one core at 20 craters/s. Two riders taken with it, both deterministic: (a) a per-tick repath cap served by a round-robin queue ordered (seat, beacon, unit), because peak demand was 1 474 repaths per game-second against 1 235/s of single-thread throughput (plan §6's named G2-a lever, taken pre-emptively as a bound on per-tick work, cap value a tuning number for S3); (b) “sealed in” is a designed unit state — park, report to the seat — not a repath loop, because one walker at cluster 32 and seven at 16 were buried by their own destruction and “no path” fires on 4.5 % of repaths (answered by the union-find oracle in 0.0001 ms). Recorded honestly: repair, not search, is what a walker waits for — cold repath plus the tick's repair share is 2.95 ms p99 and 8.9 ms max — and at 20 craters/s this competes with the mesher and the rest of the tick, whose budget G3′ has not yet fixed. Rejected: lazy repair on first query (turns an amortised cost into a spike when many units repath at once, and makes graph contents depend on query order — a determinism hazard for hashed state), finer-than-cluster granularity (the intra rebuild is the expensive half and needs a dependency structure that does not exist; 1.48 ms does not justify it), and running on a stale path for bounded ticks (G2-a passed 2.7×; buys nothing and shows as walking into craters). Decided under the owner's standing instruction of 2026-09-14 to take the recommended option until told otherwise.
- **Item 61, the estimator's API and error contract: option 1, a tick count, not a range.** `estimate(world, clusters, scratch, start, goal, fog) -> Option<Estimate { cost, ticks, legs }>`; `None` means no route and is answered by the connectivity oracle before any search (a normal, cheap result — a moat seals you in as well); `legs` is the number of abstract edges crossed, which is what the editor uses to draw the polyline. The estimator is the abstract search alone: it never refines, never steps, never forks (AGENTS.md §3 rule 2). Error contract, written into the gateway's `docs` output: within ±15 % at p90 of walked time on static terrain (measured 2.6 %; the margin is S3's to spend on the difference between the toy and the real sim, not accuracy to bank), and **never optimistic** — the editor's rounding depends on that property, so it is part of the contract, not an implementation detail. Fog: priced ×1.5 per abstract edge when either endpoint cell is unknown (integer `cost * 3 / 2`), an approximation of §11's per-cell rule chosen because counting unknown cells is the refinement the estimator may not do; measured |error| p90 48.6 %, capped at +50 % over the clear estimate. What the editor may render from a fogged leg: a dashed leg (spec section 13) labelled as an upper bound, never as an ETA — the route's total shows the clear legs' time plus the fogged bound, marked as a bound. Short routes: rounded generously or shown in whole seconds (item 57). No estimate cache: P3-a passes with 18 % headroom and a cache is hashed state that has to be invalidated identically on every platform; a per-(start cluster, goal cluster) cache invalidated on repair stays plan §6's named P3-a fallback. Rejected: always returning a (low, high) pair (discards the measurement, and the “high” would be the arbitrary ×1.5 bound) and a cache now. Decided under the owner's standing instruction of 2026-09-14.
- **Item 62, the total-order tiebreak — part of the determinism contract alongside items 48–51: option 1, search nodes are ordered by `(f, h, node_id)`, ascending in all three, in a hand-written binary min-heap.** `f = g + h`; ascending `h` among equal `f` prefers the node further along; `node_id` last makes the order total, so no two distinct nodes ever compare equal and the pop sequence is a function of the graph and nothing else. Every other sort key in pathing — entrances, neighbours, dirty sets, the repath queue — ends in a unique id; open set is the heap, closed set and g-scores are dense arrays indexed by `u32` node id with generation stamps (no `HashMap`, no `usize` in anything hashed), costs are `i32`, no recursion. Measured: `the_tiebreak_is_total_under_neighbour_permutation` passes; four bench runs at cluster 32 walked byte-identical routes; and CI run 34861229486's `path-identity` job found Linux and Windows identical on all 3 203 lines at every cluster size (digests `d98ba7776dc48e9c` / `ef29afabb1d26148` / `8daf1375f13e8619`). Open: macOS is not in this spike's matrix; G4's matrix covers it and S3's certification on real code inherits that. Rejected: `(f, node_id)` only (still total but discards the goal-ward preference and expands more nodes for the same answer) and `std::collections::BinaryHeap` (no order among equal keys, and the order it does produce depends on insertion history and the std implementation — the trap plan §5 names; the total-order key makes the container irrelevant, which is the point). Decided under the owner's standing instruction of 2026-09-14.
- **G2+P3 decision items 57–62 COMPLETE (2026-09-14).** Spike G2+P3 is closed: GO at cluster 32; the editor promises a travel time; 10 / 14 / 4 / 3-per-tick with ceiling as rules-table data; eager chunk-footprint repair with a deterministic per-tick repath cap and a designed sealed-in state; the estimator returns ticks under a never-optimistic ±15 % contract with fog as a labelled upper bound; the `(f, h, node_id)` total order joins the determinism contract. Structural fix carried to S3, not built: a corner entrance per cluster. Next: spike G3′ (segment budget), then the walking skeleton.
- **Spike G3′ verdict: GO on G3′-b, the binding constraint; G3′-a conditional on the per-tick repath cap; the 3/5/8 ladder affordable as written; Linux/macOS and the three-OS hash-stream comparison pending the first CI run (2026-09-14).** Toy build in `spikes/g3-tick-budget`, started from a copy of G4's toy sim (same fixed-point types, canonical encoding + xxh3 under the item-48 seed, counter-based RNG streams, kill-credit rule) and extended to run-time counts: eleven named phases — broadphase (CSR uniform grid rebuilt by counting sort, r² queries in Q32.32), programs, movement, combat, kill_credit, power, quartermaster, decision (every 5th tick, all seats), voxels (32³ copy-on-write chunk store, 20 craters/s), hash (full and incremental), and a `pathing` phase that stands in for G2: a real integer path-follow over the unit table plus G2's measured repath (0.77 ms, 1.84 ms once in a hundred), cluster repair (2.5 ms per crater) and CSR rebuild (0.5 ms per tick) charged as a calibrated, state-free busy loop; G4's pinned dependencies only; 25 tests including the incremental hash equal to the from-scratch fold and sensitive to every field, the tick reading no clock, broadphase equal to brute force, and the hash stream identical across runs. Full numbers in `docs/spikes/G3prime-segment-budget.md` §9. Measured with overflow checks on, pinned to one core from outside, clocks recorded (the pinned core held 108–112 % of nominal, no thermal trend), Defender not excluded, 2 000 warm-up + 9 600 measured ticks, median of three: **G3′-b** mean tick 10.620 ms, 4.708× real time (limit 12.5 ms / 4×), 15 % of headroom; **G3′-a** tick p99 **56.2 ms** with the per-tick repath cap unbounded (limit 25 — FAIL), **16.5 ms** at a cap of 16 (PASS), 28.1 ms at 32 (FAIL); **G3′-c** met at world totals (4 seats, 300 units, 40 beacons); at per-seat counts (1 200 units / 160 beacons) the tick is 33.6 ms mean and 215.9 ms p99 and no tuning value recovers it. Two health warnings that govern how every number is read: (1) **96.5 % of the gate tick is charged, not measured** — the tick minus the pathing substitute is 0.27 ms, so the spike answers “does G2's pathing load plus a sim fit in a tick”, not “is the sim fast”; (2) **the G3′-a verdict is a readout of one unmeasured constant**, the repath burst frequency — G2 reported the burst's size (74 repaths in one tick) but not its rate; at a modelled 1-in-64 the p99 is a burst tick (56 ms), at 1-in-128 it is not (12.1 ms), and the mean moves 2.3 % across the whole range. That constant is a PLACEHOLDER for the owner with G2 at S2's exit; until then G3′-a is quoted as conditional, and the lever that settles it is the per-tick repath cap item 60 already defines (sweep: 8 is unsustainable — the queue never drains and 14 % of the work is dropped; 16 sustainable and passing; 32 and unbounded failing; stability floor 9.45, p99 ceiling 15.4 after the reserve). Per-phase, gate configuration: pathing 10.35 ms (96.5 %, of which the real path-follow is under 0.2 ms), broadphase 0.20 ms, hash 0.018 ms, everything else under 0.01 ms each. Full vs incremental hash: identical digest `44217aea12b1af6a` over 9 600 ticks; incremental re-encodes 7.641 of 8 tables (every live table is dirty on essentially every tick) and saves 0.001–0.0025 ms, inside the hash phase's own run-to-run spread; hashing the chunk store's 9.4 MB of bytes instead of its 288 per-chunk digests would cost 0.834 ms, 43× the whole eight-table hash. Cost model (destruction-off sweep for the sim's own terms): 1 022 ns per unit per tick, 1 234 ns per unit per crater-per-second for G2's repath load, 6 960 ns per voxel edit for the chunk store, 1 041 ns per decision tick per seat, fixed term 0.47 ms that is entirely G2's CSR rebuild; the beacon term did not resolve and is published as a bracket [231, 3 159] ns; the unit cost is measurably super-linear (+34 % quadratic share at 600 units) and the broadphase is the phase to watch. Provisional per-map power budget: **190 kW** = 190 affordable units world-wide at a PLACEHOLDER 1 kW per unit and a PLACEHOLDER 40 % reserve at 20 craters/s (446 at 10/s, 55 at 40/s — the destruction rate and the budget must be set together at S2's exit). Drift over the full 8-minute segment: none (p50 0 ‰, mean −7 ‰ minute 1 to minute 8); 0.003 allocations per tick; peak working set 15.5 MiB, read from inside via a hand-declared `K32GetProcessMemoryInfo` and from outside via PowerShell, agreeing to 8 KiB; overflow checks cost 2.1 % on the mean and 2.2 % on the p99 with a byte-identical hash stream. Four hash streams (full, incremental, cap 16, checks off) byte-identical over 9 600 ticks; the cross-OS comparison is the CI `hash-identity` job. Measured differently from the plan, stated in §9.15: Linux and macOS not run locally (the workflow was not installed at measurement time; the cross-platform figure with signal is `tick_minus_pathing_mean_ms`, because the charged phase reproduces G2's milliseconds on every machine by construction); the repath cap measured for what it costs, not what it picks (item 60's round robin has no subject in a substitute); the fixed overhead the fit returns is G2's CSR rebuild, so S2 must not charge it twice; kill_credit settled only 37 deaths; one map, one seed. One harness defect found and fixed from outside the binary: the pathing calibration in the first 120 ms of the process read a ramping clock, so the driver now pre-warms the pinned core for 6 s; the ±3 % residual is reported, not smoothed. 40 review findings over three lenses, all applied (blockers: the burst frequency deciding the verdict was an unsourced constant, now a named PLACEHOLDER with a sensitivity table; the incremental hash never skipped a table at the gate, now measured with the table counts published; the linearity test ran on the one sweep where linearity was guaranteed by construction, now taken from the destruction-off sweep). The decision plan §8 asks for — its five parts, plus §1's open question and the repath cap — goes to the owner as items 63–69 below.
- **Spike G3′ CI green (2026-09-14, run 34908269505).** `spike-g3-tick-budget` passes on every job: fmt, clippy `-D warnings`, 25 tests, tickbench at the gate configuration and the cost-model sweep on ubuntu, windows and macos (aarch64), and the `hash-identity` job — **all three operating systems agree on all 9 600 per-tick hashes**, digest `44217aea12b1af6a`, with the full and incremental modes identical on each; that is a second, independent three-OS determinism trace on top of G4's, and it covers a 384×384×64 copy-on-write chunk store under 9 600 craters, a CSR broadphase, per-seat power and treasury passes and the kill-credit apportionment. The hosted runners reproduced the shape of the local result — mean tick 10.50 ms ubuntu / 10.59 windows / 11.56 macos (all ≥ 4× real time), p99 55.6–56.4 ms at an unbounded repath cap and 15.2–16.9 ms at a cap of 16, provisional budget 169–179 kW — but the totals agree across platforms by construction, because the charged pathing phase reproduces G2's milliseconds on every machine; the figure with signal is the tick minus that phase: **0.231 ms ubuntu, 0.252 ms windows, 0.229 ms macos**, so plan §3 step 7's “budget set by the slower first-class OS” reads Windows, by 9 % on 2 % of the tick. The first run for the spike commit (34908177661) was cancelled by the concurrency group when the verdict entry was pushed a minute later — every push to `main` had been re-running every spike workflow — so the four spike workflows now carry a `paths` filter and only run when their own spike directory or workflow file changes.
- **Item 63, what the G3′ gate row counts: option 1, world totals — “4 seats, 300 units, 40 beacons” means 300 units and 40 beacons across the whole match (75 and 10 a seat at four seats; about 100 units a seat in a three-seat v1 match).** It is the reading the spike measured as primary, it passes G3′-b with 15 % of headroom, and it is the only reading under which a usable power budget exists: at per-seat counts (1 200 units / 160 beacons) the tick is 33.6 ms mean and 215.9 ms p99, 2.7× over G3′-b and 8.6× over G3′-a, and no tuning value recovers that. Recorded honestly: this is the reading generous to the engine; a later design that wants a seat to field 300 units re-derives the budget rather than scaling it, because the broadphase grew 7.2× for a 4× unit count. Rejected: per-seat counts (a different engine, and the gate would be failed rather than conditional) and leaving it open until S2 (the per-map power budget is the map generator's input and the generator is built at the walking skeleton, so “open” means a number that may move by 4× under a generator already written). Owner's explicit answer, 2026-09-14.
- **Item 64, the tick's cost model: option 1, adopt the five numbers as the skeleton's provisional model, in the shape §9.6 of the plan publishes them.** `a0` = 1 022 ns per unit per tick for the sim's own work (destruction off), `d` = 1 234 ns per unit per crater-per-second for G2's repath load, 6 960 ns per voxel edit for the chunk store, 1 041 ns per decision tick per seat, and a fixed term of 0.47 ms that is entirely G2's CSR rebuild (0.50 ms) with the sim's own overhead lost in the noise beneath it — so S2 must not charge the CSR rebuild twice. The beacon term did not resolve and is carried as a bracket, [231, 3 159] ns per beacon per tick; it cancels out of the budget arithmetic. The chunk-store cost of a crater and its pathing consequence keep different names. The hosted runners corroborate the shape (ubuntu `a0` 885, `d` 1 272; windows 952, 1 276). Recorded honestly: `a0` is a linear fit over a series that is measurably super-linear (+34 % quadratic share at 600 units), so it under-states the marginal unit above 300 and over-states it below — a budget input, not a law. Rejected: publishing only the phase means with no fitted model (step 5 then has no arithmetic and the power budget becomes a guess) and carrying a fitted quadratic term (over-fitting a six-point sweep of a synthetic broadphase whose cell size the real one need not share; the shape is the finding, the coefficient is not). Decided under the owner's standing instruction of 2026-09-14.
- **Item 65, the provisional per-map power budget: option 1, 190 kW — 190 affordable units world-wide at a PLACEHOLDER 1 kW per unit and a PLACEHOLDER 40 % reserve, at 20 craters per second — and the destruction rate is recorded in the same decision, because it moves the answer from 6 873 units at zero to 446 at 10/s, 190 at 20/s and 55 at 40/s.** Arithmetic, every input named (plan §9.7): 12.5 ms mean budget less the 40 % reserve = 7.5 ms; minus the fixed 0.47 ms and the unit-free crater cost at 20/s (2.4 ms) = 4.6 ms; divided by the cost of one unit at 20/s (`a0` + 20·`d` ≈ 25.7 µs) = 190 units; times 1 kW per unit = 190 kW. The p99 constraint does not reduce to a unit count — its tail is a repath burst on a single tick — and solves instead for the per-tick repath cap (item 69). Provisional in a strong sense: two PLACEHOLDERs sit inside the number (the reserve, owner at S2's exit; kW per unit, a section 19 Tuning value at S1/S2) and the third input, G2's repath load, is charged rather than measured; superseded by S2's exit measurement, which sets the map generator's real ceiling. Cross-referenced from spec section 19's Tuning row for kW. Rejected: the quiet-map figure (a map supplying power for a unit count the tick cannot carry the moment destruction starts, which is the configuration the game is always in), deferring the number entirely to S2 (the generator built at the skeleton needs a ceiling, and “no number” is an implicit one nobody wrote down), and a thinner reserve (a tick that is 96 % substituted flatters the real one; if anything 40 % is thin). Decided under the owner's standing instruction of 2026-09-14.
- **Item 66, full or incremental per-tick state hash — part of the determinism contract alongside items 48–51: option 1, full.** Re-hash every table every tick, in declared order, into a reused buffer; the chunk store enters the hash as its 288 per-chunk digests, refreshed only for chunks an edit touched, never as its bytes. Measured: full and incremental produce the identical digest `44217aea12b1af6a` over 9 600 ticks on all three OSes; incremental re-encodes 7.641 of 8 tables at the gate configuration because every live table is dirty on essentially every tick, and saves 0.001–0.0025 ms — 0.02 % of the gate tick and inside the hash phase's own run-to-run spread; hashing the chunk store's 9.4 MB of bytes instead of its digests would cost 0.83 ms, 43× the whole eight-table hash and three times the entire non-pathing tick. The reason is structural, not the saving: an incremental scheme buys its saving with a dirty flag on every write site in the sim, forever, on the determinism artefact itself, and a missed flag produces a wrong hash rather than a slow one — a desync found days later on another operating system. Rejected: incremental at whole-table granularity (what the spike built; nothing to skip in a live tick) and at finer granularity (strictly more flags to miss for a saving there is no evidence of; the one fine-grained scheme that stays, per-chunk voxel digests, is not a dirty-flag scheme but the encoding itself). Decided under the owner's standing instruction of 2026-09-14.
- **Item 67, the table layout and broadphase the skeleton starts from: option 1, keep both, with the caveat written down.** Vec-backed structure-of-arrays tables with the counts fixed at construction (the spike ran 300 and 1 200 units from the same code with zero allocations per tick), and a CSR uniform grid over the map footprint rebuilt every tick by counting sort — cell edge equal to the largest query radius so a radius query touches at most 3 × 3 cells, rebuild O(items + cells), allocation-free after construction. The caveat: the cell size is a tuning value tied to unit density, and the query is super-linear in unit count (+34 % quadratic share at 600 units, marginal cost per unit up 72 % across the sweep), so the structure that is 28 % of the quiet tick today is 47 % at twice the load — the phase to watch before the unit ceiling doubles. Rejected: a two-level or hierarchical grid now (a solution to a problem the gate load does not have at 0.22 ms, and a second structure to keep deterministic and hashed), a BVH or a sorted-by-cell array (the BVH's rebuild is not O(items + cells) and is hard to make allocation-free; the sorted array is what the CSR already is; neither addresses the mechanism, which is more candidates per 3 × 3 neighbourhood as density rises), and `imbl` persistent maps for hot tables as G4's toy used for kill credit (small-allocation traffic back in the hot loop, which is exactly what plan §5's allocator row says makes the budget platform-dependent). Decided under the owner's standing instruction of 2026-09-14.
- **Item 68, the 3 / 5 / 8-minute segment ladder: option 1, affordable as written; keep it.** A full 8-minute segment at the gate configuration shows no drift: p50 0 ‰ and mean −7 ‰ from minute 1 to minute 8, and +16 ‰ / +11 ‰ with destruction off, which is scheduling noise on a machine with turbo enabled; the copy-on-write chunk store (9 600 craters, 467 000 voxels removed) and the accumulating wrecks cost nothing measurable, and the three hosted OSes agree (ubuntu −5 ‰, windows 0 ‰; macos noisier but flat on the p50). Recorded honestly: the check covers this tick — the chunk store's `Arc` sharing and the wreck population are in it — but not process-wide memory growth from a mesher, a renderer, a gateway or a verifier, which S2 re-measures. Rejected: shortening the ladder (the spec's §6 fallback; it discards a measurement that came back clean, and it interacts with the fun gate, whose own fallback ladder starts with a shorter Push ladder — if both gates ever point that way the change is made once, deliberately, not pre-emptively here). Decided under the owner's standing instruction of 2026-09-14.
- **Item 69, the per-tick repath cap's value — the tuning number item 60 left open: option 1, 16 repaths per tick (window 15–16), re-derived at S2's exit once the burst frequency is a measurement.** The lever the G3′ measurement actually needs, and neither of the spec's two fallback levers: 16 is sustainable (mean backlog 2.85 over a full segment, no growth, 100 % served) and turns G3′-a from FAIL (56.2 ms p99 unbounded) to PASS (16.5 ms) at no measurable cost on the mean; 15.4 is the ceiling the p99 arithmetic gives after the 40 % reserve; 9.45 — G2's mean demand per crater at one crater a tick — is the stability floor below which the queue never drains. Served in the round-robin order item 60 fixed, (seat, beacon, unit), so which repaths a busy tick defers is a function of the state and nothing else. Recorded honestly: the window is narrow and both ends move with constants this spike did not measure (the burst frequency, G2's per-repath cost on real code), and the cap was measured for what it costs, not for what it picks. Rejected: 32 (fails G3′-a at 28.1 ms p99), 8 or anything below about 9.5 (the cheapest row in the sweep and not a configuration — the backlog grew by 3 092 over the segment and 14 % of the work was dropped for ever; a cap below mean demand buys a cheap tick by not doing the work), and no cap (a tick that misses its deadline outright whenever a burst lands). Decided under the owner's standing instruction of 2026-09-14.
- **G3′ decision items 63–69 COMPLETE (2026-09-14).** Spike G3′ is closed: GO on the binding mean constraint, G3′-a conditional on the per-tick repath cap and the unmeasured burst frequency; world totals; the five-number cost model adopted as provisional; 190 kW at 20 craters/s with the reserve and kW-per-unit as PLACEHOLDERs; full hash with per-chunk voxel digests; SoA tables and the CSR grid with the broadphase flagged; 3/5/8 kept; repath cap 16. All four stack spikes (G4, G1, G2+P3, G3′) are closed. Next: the walking skeleton, opening with the `crates/mesher` / `WALLED_PACKAGES` harness PR (item 56) and the `math` crate question (AGENTS.md §3 placeholder), and the `spike-end` tag.
- **Item 70, the `math` crate question (AGENTS.md §3 placeholder, walking-skeleton opening): option 1, no separate `math` crate — the integer newtypes (`Fx`, `Sq`, `Angle` + LUT, HP, `$`, `kW`, `Tick`, `Ms`), the RNG streams, the canonical encoding and the state hash stay as modules inside `pharmakos-sim`, and HPA* and the map generator stay there too.** Reasons: the only crates that need those types besides the sim are `plan-core`, `verifier` and `gateway`, and each already depends on `pharmakos-sim` with `default-features = false` for exactly the snapshot and knowledge types this would split out, so a `math` crate would add a crate without removing a dependency edge; the determinism contract (items 48–51, 62, 66) then stays in one crate, which is what makes “the determinism code” a nameable contract path for AGENTS.md §5 and `cargo xtask ci`; and the one consumer that must not reach the sim — the walled mesher — needs none of them (its input is integer chunk data and its output is float vertex buffers, and `client-gdext` talks to the sim only through the gateway). Recorded honestly: the verifier and plan-core compile the whole sim crate to get a handful of types, which costs build time and keeps the sim's stepping API within reach — the research-guard and the “no dry runs” review rule (§3 rule 2) are the mitigation, not the type system. Revisit if the skeleton finds the verifier's build time or its temptation to step the sim to be a real problem; the split is additive then (a `math` crate the sim re-exports) and costs nothing now. The AGENTS.md §3 placeholder is resolved by this item and item 56 together and is removed in the item-56 harness PR. Rejected: a `math` crate now (a second home for contract code, two crates for every contract rule to name, and no dependency edge it actually removes). Decided under the owner's standing instruction of 2026-09-14.
- **Item 71, the `spike-end` tag and retiring `spikes/`: option 1, tag `main` as `spike-end` now that all four spikes are closed, and delete `spikes/` together with the four `.github/workflows/spike-*.yml` in the item-56 harness PR, as `spikes/README.md` rule 4 and rule 6 and `docs/spikes/README.md` already prescribe (frozen at the tag, deleted once harness part 1 is green — it is).** The plans and their Results sections under `docs/spikes/` stay; they are the record. The tag keeps every spike readable and checkable (`git worktree add ../pharmakos-spikes spike-end`), and an agent re-writing the mesher, HPA* or the tick against the real types works with that worktree open — the rule that spike code is never copied into `crates/` holds either way. What survives of the spike workflows is re-expressed inside `cargo xtask ci` as the skeleton lands the corresponding checks: the three-OS hash-stream comparison (G4, G3′) becomes the determinism step's golden chains, the path-identity comparison (G2) joins it when pathing lands at S3, the geometry golden (G1) becomes the headless-screenshot check of harness part 2, and the perf budgets (G1 mesher p99, G3′ tick) arrive with the gates that set them (AGENTS.md §9 item 11). Recorded honestly: deleting now removes the in-tree reference the skeleton agents would otherwise have, and the spike workflows' hosted-runner numbers stop being refreshed; both are what the tag is for. Rejected: keeping `spikes/` until the skeleton has re-written each piece (contradicts the written lifecycle, keeps 30 000 lines of throwaway code in the product tree for months, and keeps four workflows burning runner minutes) and deleting without the tag (loses the code). Decided under the owner's standing instruction of 2026-09-14.
- **Skeleton-opening items 70–71 COMPLETE (2026-09-14).** Next: the item-56 harness PR (add `crates/mesher` to the workspace and `WALLED_PACKAGES`, update `clippy.toml`'s header, the AGENTS.md crate map and §4.9, remove the §3 placeholder, retire `spikes/` and the spike workflows at the `spike-end` tag), `cargo xtask ci` green on the branch, opened for the owner to merge; then the skeleton plan.
- **Skeleton plan adopted as the working plan (2026-09-14).** `docs/design/skeleton-plan.md` — synthesised from three independent plans (contract-first, vista-first, risk-ordered) and three judges, the risk-ordered plan winning on definition-of-done and risk-retired-early with the vista-first plan's demo cadence and the contract-first plan's day-one public type surface grafted in — is the working plan for spec section 17's wk-18.5 row: 23 tasks (T0–T22), nine waves of at most three agents on disjoint crates, 172 agent-days over 14.2 weeks with 1.3 weeks of named float, `crates/sim` under one author for the whole stage, harness part 2's formats frozen in wave 1, the vista at about week 9.8, the stage demo of its §1.1 at the end. Its section 6 decisions are taken below as items 72–84; its section 7 decisions (14–24) are taken when the task that needs each reaches it. Under the standing instruction; the document stays marked PROPOSED until the owner has read it, and the owner's word overrides any item below.
- **Item 72, the Godot project's home (plan §6.1): option 1, `godot/` at the repository root — `godot/project.godot`, `godot/scenes/`, `godot/scripts/` (GDScript views and editor UI only), `godot/bin/` as the staging target, `godot/.godot/` git-ignored with `godot --headless --path godot --import` run first in every CI job and every packaging step — and `godot/` is one ownable unit paired with `crates/client-gdext` under AGENTS.md §6, so exactly one agent holds both at a time.** Downside taken knowingly: it serialises the vista agent against the editor agent, which the schedule pays for by giving one author T12, T16 and T19 in sequence. Rejected: `godot/` inside `crates/client-gdext/` (a `res://` path cannot escape the project folder, the staged library sits under a cargo-managed directory, and the thin-client rule gets harder to read) and `godot/` as a free-for-all (two agents editing GDScript views at once is the collision §6 exists to prevent, and GDScript has no compiler to catch the merge). Decided under the owner's standing instruction of 2026-09-14; the owner may revise any of these before the task it gates starts.
- **Item 73, the gdext and Godot pins and the `.gdextension` shape (plan §6.2): option 1, `godot = "=0.5.5"` and Godot 4.7.2 exactly, in CI and in the export presets — the pair every G1 number was taken on — with a single `godot/pharmakos.gdextension` (`entry_symbol = "gdext_rust_init"`, `compatibility_minimum = 4.7`, `reloadable = false`, explicit per-target library paths for `windows.x86_64` and `linux.x86_64`, debug and release); a bump is determinism-adjacent because it re-runs the geometry golden and goes through a deliberate PR.** Downside: pinning delays upstream fixes. Rejected: a caret range (a silent ABI or renderer change would show up as a moved geometry golden with no commit to blame). Decided under the owner's standing instruction of 2026-09-14; the owner may revise any of these before the task it gates starts.
- **Item 74, the canonical JSON codec (plan §6.3), a dependency decision under §3 rule 5 either way: option 1, a hand-written canonical proto3-JSON codec in `crates/proto`, driven by the descriptor set, with the JSONC comment-and-formatting layer in `plan-core` on top of it; neither `protoc-gen-prost-serde` nor `pbjson` is added, and `buf.gen.yaml`'s prost-serde PLACEHOLDER is resolved by removal.** The requirement is a byte-exact round-trip that preserves comments, formatting and field order (spec section 13), which a general-purpose serde mapping does not promise. Downside: the project owns a codec that must track the proto3 JSON mapping by hand; the round-trip goldens of T1 and T8 hold it honest. Rejected: `protoc-gen-prost-serde` (an unapproved dependency, two JSON paths that can drift) and `pbjson` (unapproved, and it still does not solve the comment round-trip). Decided under the owner's standing instruction of 2026-09-14; the owner may revise any of these before the task it gates starts.
- **Item 75, harness part 2's formats in wave 1 (plan §6.4): option 1, taken — the scenario file format, the golden conventions and the screenshot comparison land in T3 as `xtask` steps that skip with a named reason until a producer exists, and are promoted to required in T20.** AGENTS.md §10 items 1–4 become a runnable command from the first merged PR and no golden's shape is invented under deadline. Downside: a third contract PR in wave 1 on a solo reviewer, mitigated by reviewing T1 → T2 → T3 in that order, T3 depending on nothing. Rejected: the whole of part 2 at the end (six waves of goldens accrete one convention per task, and the runner certifies nothing written before it). Decided under the owner's standing instruction of 2026-09-14; the owner may revise any of these before the task it gates starts.
- **Item 76, `Playbook.kind`'s enum values (plan §6.5), the deferral item 47 left to this stage: option 1, `KIND_UNSPECIFIED = 0`, `PLAYBOOK = 1`, `TEMPLATE = 2`, `SAMPLE = 3`, with the verifier rejecting `KIND_UNSPECIFIED` with a code and a JSON Pointer.** Downside: `TEMPLATE` versus `SAMPLE` is a guess about S6's library and may prove to be one concept; a fourth kind arrives as an additive value. Rejected: `PLAYBOOK` and `TEMPLATE` only (the sample library of spec section 13 then has no tag). Decided under the owner's standing instruction of 2026-09-14; the owner may revise any of these before the task it gates starts.
- **Item 77, the plan fingerprint's hash function (plan §6.6): option 1, xxh3-64 over the canonical JSON bytes under item 48's `STATE_HASH_SEED` with its own domain-separator prefix — so the project has exactly one hash function, as AGENTS.md §5's “never add a second hash function” asks.** Downside: xxh3 is not cryptographic — the fingerprint identifies a plan, it does not authenticate one; if v1.1's published API wants tamper-evidence, that is a second, explicitly different field. Rejected: BLAKE3 or SHA-256 (a second hash function and a new dependency for a v1 property nothing needs). Decided under the owner's standing instruction of 2026-09-14; the owner may revise any of these before the task it gates starts.
- **Item 78, where the rules table lives and what `rules_hash` covers (plan §6.7): option 1, a `gp.v1.RulesTable` message, written on disk as `rules/rules.v1.json` in canonical JSON, loaded by `pharmakos-sim` and hashed with item 48's encoder so `rules_hash` is one of the verifier's inputs by construction.** K and B, 10/14/4, the move cost per tick, the repath cap, the segment ladder, the CSR cell size and every `$`/`kW` number sit in one reviewable file whose shape `buf breaking` guards. Downside: the table becomes a contract file; mitigation — the *shape* is the contract and goes through `buf breaking`, the *values* are data in the JSON and tuning changes during S1 and S2 are ordinary PRs. Rejected: a plain JSONC file with a hand-written schema (a second parser in the sim, no `buf breaking` on its shape) and Rust constants with a generated hash (contradicts AGENTS.md §12 and items 54 and 59). Decided under the owner's standing instruction of 2026-09-14; the owner may revise any of these before the task it gates starts.
- **Item 79, HPA* at the skeleton (plan §6.8), where spec section 17 (S3's depth task) and item 71's “when pathing lands at S3” pull against the wk-18.5 row's editor-with-travel-times, item 57's promise and item 61's never-optimistic contract: option 1, land the mechanism now (T7: cluster-32 HPA*, eager repair, the connectivity oracle, the repath cap, the estimator) and commit the path-hash golden; leave the ±15 % certification on real code, the corner entrance and the endpoint-sweep work at S3 as its depth task.** Downside: S3's named depth task is pulled forward by three stages and the milestone table does not name the 8-day task; the path-identity CI comparison lands earlier than item 71 assumed, which is additive. Rejected: a flat A* behind item 61's signature (cannot produce `legs` or price fog per abstract edge, so the frozen contract is partly unimplementable, and the 96.5 % of the tick G3′ measured goes unmeasured for the stage) and a straight-line stub (optimistic, which item 61 forbids). Decided under the owner's standing instruction of 2026-09-14; the owner may revise any of these before the task it gates starts.
- **Item 80, JSON-RPC parameter casing on the wire (plan §6.9): option 1, lower-case strings on the wire (`depth: "quick"`), translated at the boundary — spec section 12's own examples — resolving `gateway.proto`'s casing PLACEHOLDER.** Downside: the wire and the schema spell the same value two ways, one conversion function to keep tested. Rejected: the enum spelling `DEPTH_QUICK` (contradicts the spec's worked example and reads badly in a hand-written client). Decided under the owner's standing instruction of 2026-09-14; the owner may revise any of these before the task it gates starts.
- **Item 81, which three templates ship at the skeleton (plan §6.10): option 1, Hold & Build, Expand & Mine, and the Safe Playbook.** The first teaches the beacon, the second the route and the economy, and the third is the one the operator files on a timeout anyway — written once, tested twice, and the editor can render “the cost of a timeout”. Downside: nothing attack-shaped until S3, consistent with combat being S2. Rejected: Fortify Under Threat in place of the safe playbook (leans on Defend settings that are S2's, and the safe playbook would have no editor rendering). Decided under the owner's standing instruction of 2026-09-14; the owner may revise any of these before the task it gates starts.
- **Item 82, what `submit_plan` runs at the skeleton (plan §6.11), where section 17's “verifier QUICK” and section 12's “submit always runs FULL” are in tension: option 1, ship `verify_plan{depth}` and `report_hash` complete from day one with FULL's estimate and lint stages present but empty; submit runs FULL as section 12 specifies and gets an honest, short report.** The API shape and the hash contract never change when S1 and S3 fill the stages. Downside: FULL is briefly a promise rather than a depth, and the `report_hash` goldens move once when S3 fills the lint stage — explained in that PR. Rejected: submit runs QUICK until S1 (`verify_plan`'s default and `submit_plan`'s behaviour disagree with section 12 for a stage, and the depth parameter's semantics change under existing clients). Decided under the owner's standing instruction of 2026-09-14; the owner may revise any of these before the task it gates starts.
- **Item 83, the `unsafe_code` allow gdext needs (plan §6.12): option 1, a crate-local `#![allow(unsafe_code)]` in `crates/client-gdext` only, with a comment naming gdext as the reason, raised in T12's PR rather than by editing the `[workspace.lints]` table.** The crate is walled and `wall-guard` proves no deterministic crate depends on it. Downside: an `#[allow]` in source is the shape §4.9 warns about for determinism lints; `unsafe_code` is not one, and T12's PR states the precedent so nobody generalises it. Rejected: a per-crate `[lints]` override (still a lint-table change) and a shim crate that exists only to hold `unsafe` (the crate map does not have it and the thin client does not want it). Decided under the owner's standing instruction of 2026-09-14; the owner may revise any of these before the task it gates starts.
- **Item 84, save and resume in the skeleton (plan §6.13), which spec sections 3 and 15 specify and the wk-18.5 row does not name: option 1, build it (T17, 4 agent-days) — save at Lull boundaries from the frozen segment-end snapshot, stamped with the rules hash and verifier version, into the private match cache; the round-trip test is one of the stage's strongest determinism assertions.** Downside: 4 days on something the row does not name, and a save format S1's and S2's new hashed state will each move. Rejected: deferring to S1 (the demo cannot be paused and resumed, and the format then lands on top of one more stage's state). Decided under the owner's standing instruction of 2026-09-14; the owner may revise any of these before the task it gates starts.
- **Skeleton pre-stage items 72–84 COMPLETE (2026-09-14).** Wave 1 may start: T1 (`crates/proto`: the envelope, the vocabulary, codegen and the canonical JSON codec), T2 (`crates/sim` part 1: the determinism core and the public type surface on day one) and T3 (`xtask`: harness part 2's formats) as three branches for the owner to review, in that order, alongside the in-flight T0 harness PR. The plan's section 7 decisions (14–24) are taken as each task reaches them.
- **Item 85, who merges during the walking skeleton, and the assistant's GitHub access (owner's explicit answers, 2026-09-14): option 3 — the assistant merges every skeleton PR, contract PRs included, once CI is green on the branch and the two adversarial reviews have been applied; the owner reviews the stage demo (AGENTS.md §10 item 8) rather than the intermediate PRs; and `gh` is signed in under the owner's account on the build machine so the assistant can open PRs, read CI logs and cancel runs.** This is a deliberate, time-boxed relaxation of AGENTS.md §5's “open a PR and stop” for the main assistant session only, during the skeleton: sub-agents still open PRs and stop, the two-review gate replaces the owner's per-PR approval, and every merge is a PR with its contract paths named so the audit trail survives. Recorded honestly: it removes the human gate the contract-file rule exists for, on the files whose churn is most expensive (proto, hash, snapshot format, lints), and the owner's word revokes it at any time. Rejected: the owner merging every PR (twenty-three merges on one person, the schedule slipping one for one with review latency) and the owner merging only contract PRs (twelve of them, still the bottleneck by design). CLAUDE.md gets one paragraph recording the delegation and its scope; the AGENTS.md rules are unchanged for every other agent.
- **Item 86, bypass-permissions mode in the owner's Claude Code session (owner's explicit answer, 2026-09-14): option 2 — it stays on, and CLAUDE.md records that the owner accepts it for the main assistant session during the walking skeleton, given item 85 already delegates merging.** Recorded honestly, as the downside the owner accepted: CLAUDE.md's ban exists so that a mistaken or injected instruction cannot silently edit a contract file, and with prompts off and merging delegated nothing between an agent and `main` is a human; the remaining gates are the two adversarial reviews per PR, `cargo xtask ci` on the branch and on `main`, and the owner's review of the stage demo. The rule stays in force for every sub-agent and for any session against a real seat. Rejected: turning it off (more approval prompts, which the owner chose not to pay for).
- **Item 87, the skeleton plan's status (owner's explicit answer, 2026-09-14): option 1 — the delegation stands; `docs/design/skeleton-plan.md` stays the working plan and keeps its PROPOSED mark until the owner has read its §1.1 (the stage demo) and §4 (the wave table), which are the two pages that matter; the assistant takes its remaining §7 decisions as tasks reach them, and the owner redirects whenever a wave's demo or a logged item looks wrong.** Recorded honestly: a wave-1 branch built on a decision the owner later disagrees with has to be reworked. Rejected: a formal approval sight unseen (records an approval not given in substance) and pausing wave 1 until the whole 945-line plan is read (three agents idle).
- **Unblocking interview COMPLETE (items 85–87, 2026-09-14).** `gh` is signed in as `thomvisser-ux`; the T0 harness PR was opened as #1, passed the three-OS `cargo xtask ci` matrix, the DCO check and the cross-OS guard, and was rebase-merged into `main` (`7d3c853`, `8780fad`, `11f4dc1`); `spikes/` is gone from `main` and readable at `spike-end`. Wave 1 (T1, T2, T3) is in flight.
- **Wave 1 progress (2026-09-15).** T1 (`crates/proto`: the gp.v1 and gp.api.v1 schema, the checked-in prost tree, the hand-written canonical JSON codec, `gp.v1.RulesTable` and `rules/rules.v1.json`) merged as PR #2 after two CI fixes worth remembering: the generated-tree check is byte-for-byte, so the tools that produce the bytes are pinned in CI — buf 1.73.0 and `cargo install protoc-gen-prost --version 0.5.0 --locked` — and the committed tree must come from that exact install (a plugin built without `--locked` formats the `#[prost(...)]` attributes differently). T2 (`crates/sim` determinism core) and T3 (`xtask` harness-2 formats) are rebased onto it and opened as PRs #3 and #4.
- **Item 88, `serde` as a direct dependency (raised by T2's review under AGENTS.md §3 rule 5): approved, as `postcard`'s derive companion only.** `serde = "=1.0.229"` with `default-features = false` and `derive`, used for the `Snapshot` derives that postcard's encoding needs; it arrives transitively through postcard regardless, so the approval widens nothing on disk and exists so the PR body's claim is true. AGENTS.md §3 rule 5's approved list now names it in that role. Not approved: `serde_json` or any serde-based JSON path — the canonical JSON codec is hand-written in `crates/proto` (item 74) and stays the only JSON in the product. Decided under the owner's standing instruction of 2026-09-14.
- **Item 89, what `rules_hash` covers (raised by T2's review against item 78): the whole canonical rules table.** `RulesTable::encode` appends `RULES_VERSION` and then the length-prefixed canonical JSON that `crates/proto`'s codec re-emits from the decoded `gp.v1.RulesTable`, so every row — `interface_times`, the fog price, the cluster edge, `lull_ms`, `revision`, `note`, the `economy` and `power` blocks — moves the hash, and a tuning PR that changes a value the verifier or plan-core reads can no longer leave `report_hash` unmoved. Chosen over prost's wire bytes because the canonical JSON is the project's own canonical form (already pinned by a test), so a reformat cannot move the hash and a value change always does. Consequence stated plainly: a change to the table's `note` moves `rules_hash`; that is correct, because the note is part of the file the verifier stamps. Decided now, while nothing depends on `report_hash`, rather than after T6 commits verifier goldens. Rejected: hashing only the rows the tick reads under a narrower name (`sim_rules_hash`), which would have left the verifier without a complete input. With it, from the same review: the broadphase query radius is expressed in voxels and filtered exactly, so a query's answer is a function of the world alone and the CSR cell size (16, a PLACEHOLDER tuning value per item 67) affects only cost — the property the G3′ calibration-constant lesson asks for. Decided under the owner's standing instruction of 2026-09-14.
- **Wave 1 COMPLETE (2026-09-15).** T1 merged as PR #2, T2 as PR #3 (`crates/sim`: the integer newtypes, the counter-based RNG, the canonical encoder and full state hash with per-chunk digests, postcard snapshots, SoA tables and the CSR grid, the eleven named tick phases, the `Operator` and work-counter seams, `fork` behind `research`, the `determinism` binary and its committed hash chain `tests/golden/determinism/expected.hashes.txt`, the rules loader through `gp.v1.RulesTable`), T3 as PR #4 (`xtask`: the scenario file format under `scenarios/`, the golden conventions under `tests/golden/` with per-area notes, the `scenario` and `screenshot` steps skipping with a named reason, the std-only PNG comparator with G1's thresholds, the Linux vista leg wired in `ci.yml`). Each was built by one agent in its own worktree, reviewed twice adversarially, fixed, rebased, green on the three-OS matrix and rebase-merged under item 85. `cargo xtask ci` on `main` now runs the determinism step for real (1 200 ticks, hash chain compared on ubuntu, windows and macos). What the owner can see: a committed cross-OS hash chain, `buf breaking` guarding the filled envelope, a hand-written playbook round-tripping through the canonical codec, and `xtask ci` naming the scenario and screenshot steps as skipped with a reason — the wave-1 column of the plan's §4 table. Next: wave 2 — T5 (`crates/sim` part 2: world, chunk store, seeded map generator), T6 (`crates/verifier` QUICK), T4 (`crates/mesher` greedy meshing) — after the plan's §7 decisions 14, 15, 17, 18 and 19 are taken.
- **Item 90, the first tuning seed set (skeleton plan §7 decision 14): option 1 — one sitting, every value a `rules/rules.v1.json` row labelled `PLACEHOLDER: tuning, owner, <stage>`, never a constant in code.** Taken now, at the start of wave 2, from the spec's stated ranges and the decisions already logged; each row lands with the task that first needs the block (T5's contract PR for the map, power, beacon, commander, locomotion-speed and verifier rows; T14 for the `$` and per-kind rows it reads), and the numbers below are the numbers, so no task writes its own. Spec section 19 re-derives each at the stage named. **Locomotion (spec section 4; amends item 59's representation, not its model):** the spec's commander range of 1–1.5 voxels/s and the 384-voxel map with the 1.5-early-Push spawn rule together put every v1 walker an order of magnitude below the 6 voxels/s that item 59's `move_cost_per_tick = 3` encodes at 20 Hz (a 384-map diagonal is 543 voxels; 1.5 × 180 s of raider travel at 6 voxels/s would be 1 620). Item 59's *model* stands in full — 10/14/4, cost → ticks by ceiling, never optimistic — and the walker's speed becomes a per-kind row **in cost units per second** (`cost_per_second`; at 10 cost per cardinal voxel that is decivoxels per second): commander **10** (1.0 voxels/s, the low end of the spec's range, so "several minutes of commander walking" holds on a 384 map), build / mining / repair-reclaim drones **10**, raider **12**, scout **20**. T7 integrates it as an integer accumulator (add `cost_per_second` per tick, spend a cost unit per 20 accumulated — the tick rate — so no division rounds down), and an estimate is `ceil(cost × 20 / cost_per_second)` ticks, which keeps item 59's ceiling property. `locomotion.move_cost_per_tick` stays in the schema, documented as superseded, because deleting a field is a `buf breaking` failure in `WIRE_JSON` mode and the row costs nothing to carry until the schema's first allowed break. **Commander (section 4, item 21):** HP 300; respawn 30 000 ms base plus 15 000 ms per earlier death in the same Push (the spec's "for example 30 s, growing with each death"), always completing by segment end; arrive radius 2 voxels; placement range 12 voxels; interface range 4 voxels (item 11's "roughly 4-voxel"). **Beacon (section 5, item 11):** sphere radius 24 voxels; a placed beacon costs `$` 60, HP 800, base draw 2 kW (net zero through its own key-core); the pre-placed core HP 3 000, not purchasable; deploy time is `interface_times.place_beacon_deploy_ms` already in the table. **Power (section 7; items 65 and 91):** core deep-bore surplus 10 kW; Generator output by vent richness lean 20 / standard 30 / rich 40 kW; draw 1 kW per unit of every kind; revive margin 2 kW; the per-map ceiling 190 kW with the 40 % reserve and 1 kW per unit inside it (item 91). **Map (sections 3, 9 and 15):** 384 × 384 × 64 voxels, 32³ chunks (item 55), three spawn zones of radius 24 with occupied ones only realised (unoccupied zones are terrain only); per occupied zone one **lean** heat vent 28–44 voxels from the core (outside the core's sphere, inside the sphere of a beacon placed at the placement range) and one **standard** starting scrap seam 8–20 voxels from the core (inside the core's sphere, so the starting mining drone digs from tick one); contested toward the centre: two standard vents, one rich vent, three rich seams; the spawn-distance rule as a fraction, **3/2** early Pushes of raider travel between any two occupied spawn centres, asserted by T5 on the octile ground distance (10/14 per step), which is a lower bound on any walked path and so conservative in the right direction until T7 measures real routes — at raider 12 cost/s and a 180 000 ms first segment that is 3 240 cost units, about 324 voxels, and reported in commander travel too (324 s at commander 10). Total supply at three seats = 3 × 10 + 3 × 20 + 2 × 30 + 40 = 190 kW, exactly the ceiling; two seats = 160 kW. **Economy (section 7):** BMI `$` 100 per settlement; starting `$` = 2 × BMI (a multiplier row, so the spec's rule stays a rule); scaling last place +10 %, leader −5 %, linear between (spec); award fund 50 % of BMI per settlement (S4); recycle refund 50 % of remaining value (spec, decided); wreck and ruin salvage 25 % of build cost; fabricator backlog threshold 2 outstanding work items per drone; ore yield per voxel by seam richness lean 2 / standard 4 / rich 8 `$`, a seam of 150 ore voxels (so 300 / 600 / 1 200 per seam), 2 000 ms per voxel per mining drone. **Units and structures (sections 7 and 9; `$` cost / HP / draw kW / cost_per_second):** build drone 20 / 100 / 1 / 10; mining drone 20 / 100 / 1 / 10; repair-reclaim drone 25 / 100 / 1 / 10 (S2); raider 30 / 120 / 1 / 12 (S2); scout 10 / 60 / 1 / 20 (S2); demolition charge `$` 15 (S2); Generator 80 / 600 / 0; autocannon 60 / 500 / 2 (S2); mortar 90 / 400 / 3 (S2); wall `$` 1 per voxel (S2, HP by material); Survey post 30 / 200 / 1; Resonance Spire 120 / 500 / 3 (S3). Starting force as the spec: the core on Build, the commander, two build drones, one mining drone. **Verifier and interpreter (sections 10 and 11):** decision tick 250 ms; handler cooldown at least 5 000 ms; `max_fires` at most 8; notebook 4 000 characters; reach memory 180 000 ms (S2/S3); the size budget is item 94. Recorded honestly: every number above is a guess sized so the plan's §1.1 demo is affordable (`$` 200 buys the beacon and the Generator with `$` 60 over) and the map's supply fits the ceiling, and a number people plan against tends to stick — the rules-table home and the loud label are the mitigation. Rejected: leaving them unset until S1 (the skeleton has to be playable, so "unset" means an implicit number somebody wrote in code — the harness constants in `crates/sim/src/world.rs` are exactly that and T5 deletes them). Decided under the owner's standing instruction of 2026-09-14.
- **Item 91, `kW` per unit and the reserve inside item 65's 190 kW (plan decision 15): option 1 — 1 kW per unit and a 40 % reserve as written, in the rules table, labelled provisional, re-derived at S2's exit exactly as item 65 says.** Three rows, not one: `power.kw_per_unit = 1`, `power.reserve_percent = 40` and `power.map_ceiling_kw = 190`. The ceiling is a derived number, but its other inputs (the tick's fixed cost, the crater cost, the per-unit cost at 20 craters/s) are measurements of the code rather than rules, so the row is the only place the result can live where the map generator and a reviewer can both read it; S2's exit moves all three together and explains the movement. Every per-kind `draw_kw` row in item 90 is also 1, so the average item 65 used and the per-kind draws agree at the skeleton. Recorded honestly: the generator's supply is sized against a number two of whose three inputs are guesses. Rejected: deferring ("no number" is an implicit number nobody wrote down, on a generator already written). Decided under the owner's standing instruction of 2026-09-14.
- **Item 92, who owns the mesher's input type (plan decision 17): option 1 — the mesher owns a plain borrowed `ChunkView<'a>` over integer data; `pharmakos-sim` exposes a borrowable chunk view of its own; `client-gdext` performs the conversion, which is marshalling and so inside the thin-client rule.** Concretely: T4 adds `ChunkView<'a>` (borrowed materials and borrowed neighbour borders, in the layout `crates/mesher` already documents, `index = x + 32·z + 1024·y`) as the meshing entry point, keeping T0's owned `ChunkInput` as the test and golden form; the light bake is the mesher's and produces the light field from the materials, so the sim never carries light; T5's chunk store exposes each chunk's material bytes by borrow in the sim's own order (`x` east fastest, `y` north, `z` up slowest, `index = x + 32·y + 1024·z`, the order the state hash's per-chunk digests are taken over); and `client-gdext` (T12) transposes sim order into mesher order as it marshals — a 32 KiB copy per remeshed chunk, trivial at K = 4 per frame and the price of neither crate knowing the other's axes. Neither crate depends on the other in either direction, `wall-guard` keeps it that way, and the rules-table values the mesher needs (K, B, the age term, `LIGHT_MAX`, `LIGHT_ATTEN`) cross the wall as integer parameters on the call, never as constants in the mesher and never through a sim dependency. Recorded honestly: one conversion written twice-removed from both ends, and the sim's order was chosen for the sim rather than to make the borrow zero-copy. Rejected: the sim owning the type with the mesher depending on it (a dependency edge from a walled crate into the determinism crate, the edge `wall-guard` exists to reason about) and forcing the sim's chunk layout to match the mesher's so the conversion is a borrow (a presentation concern deciding the layout of hashed state). Decided under the owner's standing instruction of 2026-09-14.
- **Item 93, which diagnostic codes the skeleton implements (plan decision 18): option 1 — every code the v1 vocabulary and the three templates (item 80: Hold & Build, Expand & Mine, Safe Playbook) can actually produce in the QUICK stages, with the rest of the ~35 as catalogue entries that have no emitter yet, so the catalogue is complete, the coverage is honest, and both are recorded in `tests/golden/verifier/expected.catalogue.json`.** The catalogue is a data table: code, family, severity, the stage that emits it or `none at the skeleton (<task>)`, the precise message and the beginner sentence from the one string table. Code numbers are append-only from the day they ship; a code with no emitter still occupies its number, which is the point — omitting it now would make its numbering an S1 argument. FULL's estimate and lint families (E06xx, W06xx, W07xx, I…) are entered and unemitted, consistent with item 82's empty FULL stages, and S1 and S3 fill them without renumbering. Recorded honestly: a catalogue with unreachable entries reads as unfinished. Rejected: implementing only what fires and omitting the rest (tidier; renumbering later). Decided under the owner's standing instruction of 2026-09-14.
- **Item 94, a provisional playbook size budget (plan decision 19): option 1 — a rules-table row, `verifier.size_budget_units = 128`, loudly labelled provisional; P1 sets the real number at S3's exit from the per-rule cost per decision tick.** The unit, so the editor's meter and the verifier count the same thing: one unit per route step, per handler, per step in a handler body, per build target, per protected area and per patrol waypoint — the things spec section 10 says count toward the one budget; conditions and settings count nothing, because their cost is bounded by the node they hang off. The worked example (`expand_east`) is 6 units, the templates are of that order, and 128 leaves the skeleton's FULL — which is QUICK plus two empty stages (item 82) — nowhere near 50 ms; T6 measures and reports QUICK p99 at 128 units without gating on it. Recorded honestly: players and templates plan against the number and it tends to stick. Rejected: no meter until S3 (spec section 13 names the size meter, and a meter with no budget is not a meter). Decided under the owner's standing instruction of 2026-09-14.
- **Wave 2 OPEN (2026-09-15).** Items 90–94 taken. Sequence: first a small contract PR (T5a) that lands items 90–94's rows in `proto/gp/v1/rules.proto` and `rules/rules.v1.json` (revision 3) with the generated tree and the proto golden regenerated, so the three lanes read rows rather than constants; then T5 (`crates/sim` part 2), T6 (`crates/verifier`) and T4 (`crates/mesher`) in parallel from that `main`, each in its own worktree, built, reviewed twice adversarially and fixed by the wave's workflow, then opened as PRs, rebased in merge order and merged under item 85. T6's 10 000-playbook fuzz target ships as an ignored test the nightly workflow will call; wiring it into `nightly-scenarios.yml` waits for T20, since `.github/workflows/**` belongs to no wave agent.
- **Item 95, two corrections to item 90 raised by T5a's reviews (PR #5): the vent-band gloss, and the starting force as rows.** (1) The band 28–44 voxels stands; item 90's gloss “inside the sphere of a beacon placed at the placement range” was wrong arithmetic, not a wrong number: the placement range (`commander.placement_range_voxels` = 12) is measured from the *commander* to the site, and the site must lie inside one of the seat's own spheres, so the commander walks to the edge of the core's sphere and places a beacon up to 24 voxels from the core, whose own sphere then reaches 24 + 24 = 48 — every vent in 28..=44 is inside it with 4 voxels to spare. T5 asserts the band against `2 × beacon.sphere_radius_voxels`, never against the placement range. (2) The starting force — two build drones and one mining drone per occupied seat — is tuning (spec section 9's row says “Tuning starting numbers”), so it is two rows, `units.starting_build_drones = 2` and `units.starting_mining_drones = 1`, PLACEHOLDER tuning, owner, S1; the core and the commander are not rows because every occupied seat has exactly one of each by design. Consequence: `rules_hash` moved a second time inside revision 3, to `b31d8d0789eb207c`; the determinism chain did not move, because the table is an input. Also accepted from T5a as built: `ByRichness.Richness` is nested inside `ByRichness` (the package's without-exception rule that enum values live in their owning message's scope), `ByRichness` is package-scoped because it describes the world while `UnitKind` and `StructureKind` are one block's row shape, and `repair_drone` is item 90's repair-reclaim drone under its short name. Decided under the owner's standing instruction of 2026-09-14.
- **T5a merged (2026-09-15).** PR #5, rebase-merged as `1b64da1`…`5701b25` after the three-OS matrix, the DCO check and the cross-OS guard passed on every commit and two adversarial reviews (fourteen findings, ten applied) were applied. `rules/rules.v1.json` is at revision 3 with every item-90/91/94/95 row; `proto/buf.yaml`'s per-change `RESERVED_MESSAGE_NO_DELETE` waiver is re-scoped to `gp/v1/rules.proto` for the discharged `Economy` 1–11 and `Power` 1–7 ranges, and its standing text now reads “delete this block in the next proto change that does not discharge a held field number, and re-add it in any later change that does”. The three wave-2 lanes (T5, T6, T4) branch from this `main`.
- **Item 96, six questions the wave-2 lanes raised in their pull requests, decided on the recommendation under the owner's standing instruction of 2026-09-14.** (1) *Item 54's ageing term* (T4): “anything older than 2 frames first” is read inclusively — a chunk is stale at `age >= age_frames` — because the row's name says the age at which the term fires and the inclusive side is the one that bounds starvation; `DrainQueue` stays as built and `upload.rs` now states the reading once. (2) *`Diagnostic.path` for a decode error* (T6): when the bytes are not JSON there is no tree to point into, so E0001 carries `/byte/<offset>` and the verifier forwards it; the proto comment on `gp.api.v1.Diagnostic.path` gains that one named exception in the next proto change rather than in a PR of its own, and the editor (T8, S6) branches on the `/byte/` prefix. (3) *W0603* (T6): the shortfall `allow_dormant_beacons` accepts is its own code, distinct from W0601 (the shortfall the grid is already carrying); the two are different sentences in spec section 11 and codes are append-only from the day they ship. (4) *`MoveStep.pace` and `MineSettings.pillar_spacing` unset* (T6): spec section 10's own worked example omits both, so `playbook.proto`'s “an unset enum is a verifier error, never a default” gains a third named case beside `BeaconFilter` and `MineSettings.seam_choice`: unset means *the built-in mandate's own choice*, which T14's mandate contract picks and documents; until that proto change lands the verifier checks neither, as built, and says so at `structure.rs::walk_action`. (5) *`RulesTable::from_message` requires the `map` and `commander` blocks* (T5): accepted; proto3 cannot tell an absent block from an empty one, and a table with no map decoded into a world of zeroes. `rules_hash` is unaffected because `encode` is untouched. (6) *Two AGENTS.md sentences the lanes asked for*, taken as a harness-docs PR at the wave close (PR #8): §12 gains *“a tuning value whose row does not exist yet is a named constant carrying a `PLACEHOLDER` that names the owner and the stage; proposing the row is part of that stage, not of the task that first needed the value”*; §4.8 gains the chunk store's fourth place — a voxel write must mark its chunk, or the bytes change and the digest does not, and the golden does *not* move; §4.9 says the wall lifts exactly the lints in `WALL_ALLOW` and not the panic or rounding ones. Also recorded, no decision needed: T6's commits carry a `Co-Authored-By: Claude Opus 5 (1M context)` trailer where the brief said Fable 5.1 (pushed history is not rewritten, AGENTS.md §6); `crates/proto/src/fingerprint.rs`'s PLACEHOLDER names a `pharmakos_sim::hash::plan_fingerprint` that T2 never added (the proto lane closes it at its next change); spec section 11's “about 35 codes” is 57 rows under item 93's complete catalogue, 43 of which fire today.
- **Wave 2 COMPLETE (2026-09-15).** Three pull requests, each rebase-merged under item 85 after the three-OS matrix, the DCO check and the cross-OS guard passed and two adversarial reviews were applied on the branch: **T4** PR #6 (`c9ba9af…8ec83d7`, `crates/mesher`: `ChunkView<'a>`, the six-direction greedy sweep with Godot's clockwise winding and the `u16` cap as an error, the flood-fill light bake with its reach derived from the flood's own rule, item 54's drain queue, the headless CPU proxy, the `#[ignore]`d p99 alarm; eleven review findings, all applied); **T6** PR #7 (`91a2bf0…af03585`, `crates/verifier`: the six-stage pipeline with QUICK complete and FULL's two stages present and empty, the 57-row catalogue with 43 live emitters, `report_hash` over five inputs, a case and a golden report per emitted code; thirteen findings, eleven applied — the two majors were RFC 6902 `replace` on absent members and an unchecked `move` destination, now E0101/E0111); **T5** PR #9 (`3c4af02…43db091`, `crates/sim`: the 32-cubed copy-on-write chunk store in item 92's order, the beacon/structure/wreck tables, the seeded generator on `Stream::Map` with the 1-Lipschitz closure, snapshot v2 with modified chunks only and a verified digest column, the Voxels phase draining an edit queue; sixteen findings, all applied; on the rebase onto T6 the 48 verifier report goldens were re-blessed, `report_hash` only, because the verifier's fixture encodes a real snapshot and `SNAPSHOT_VERSION` moved to 2). **The determinism chain moved, at tick 0 and on every line**, for four stated causes: 288 real chunk digests instead of zeros, three new tables and a `kind` byte per unit, non-zero starting treasury/supply/draw, and units standing on the surface behind twelve rows of real starting force at the rules-derived 1 voxel/s. `rules_hash` did not move (`b31d8d0789eb207c`). **What the owner can see:** `tests/golden/mapgen/expected.digests.txt` — a seeded map per committed seed (spawn zones, a lean vent 28–44 voxels out and a standard seam 8–20 out per occupied seat, 2 + 1 contested vents and 3 rich seams, 190 kW of supply at three seats) as a store digest and two travel times, compared byte for byte on Windows, Linux and macOS; `tests/golden/verifier/**` — the catalogue and one report per emitted code, with `report_hash` and `plan_fingerprint` in each; `tests/golden/mesher/expected.digests.txt` — ten geometry cases as vertex and index digests; and `tests/golden/determinism/expected.hashes.txt`, re-blessed. Wave 3 (T7, T8, T9) opens from this `main`.
- **Item 97, the scenario assertion vocabulary (plan §7 decision 16): option 1 — T3's frozen scenario format keeps its two assertions, “event fired” and “hash chain equals”, and T15 extends the vocabulary once, when `scenario run` meets real events, by at most “event count in range”, “state hash at tick N” and “terminal hash”.** Taken on the recommendation under the owner's standing instruction of 2026-09-14. *Downside accepted:* a format change after producers exist, which is what T3 exists to avoid; it is accepted because the assertion vocabulary is data inside the format, not the format. What wave 3 owes it: T9's event feed names every event by a kind a scenario file can assert on and carries a per-kind count in each segment digest, so the three later assertions need no new event shape; T8 claims nothing in `render_plan` that a scenario would have to assert differently once they exist.
- **Item 98, the private match cache's location (plan §7 decision 20): option 1 — the OS-standard per-user data directory, one folder per match, with the UI saying it is unencrypted.** Taken on the recommendation under the standing instruction. Resolved with the standard library alone, because three environment variables cover it and a `directories`-style crate is off the approved list (AGENTS.md §3 rule 5): `%LOCALAPPDATA%\Pharmakos\matches\<match-id>\` on Windows, `$XDG_DATA_HOME/pharmakos/matches/<match-id>/` (default `~/.local/share`) on Linux, `~/Library/Application Support/Pharmakos/matches/<match-id>/` on macOS; a missing variable is an error the lobby shows, never a fallback beside the executable. The layout inside the folder is T9's to propose and the PR body's to state; T17 (save, resume, the private replay) inherits it. *Downside:* none material. *Alternative — beside the executable:* easier for a tester to find and send; *downside:* an unsigned zip writing into its own folder fails on a read-only install path. This is enforcement, not cryptography, exactly as the spec says: anyone with filesystem access can read the folder, and nothing here claims otherwise.
- **Item 99, `getrandom` as the gateway's one dependency off the workspace list (raised at the wave-3 opening under AGENTS.md §3 rule 5): approved, for OS entropy only.** The 256-bit seat tokens need a cryptographic entropy source and the standard library has no stable one in rustc 1.98; `unsafe_code = "deny"` rules out a hand-written FFI call, and `RandomState` is a disallowed type. Option 1 **(taken)**: `getrandom = { version = "0.4", default-features = false, features = ["std"] }` in `[workspace.dependencies]`, taken by `crates/gateway` alone, and everything else in the transport hand-written over `std::net` with blocking threads — the RFC 6455 server side (the upgrade with `Host` and `Origin` checks, `Sec-WebSocket-Accept` through a SHA-1 of our own pinned to RFC 6455 §1.3's worked example and the FIPS 180-4 vectors, masked client frames required, text/close/ping/pong only, no extensions, no TLS, a frame-size cap) and JSON-RPC 2.0 over the proto crate's `Json` value and its `base64` module (item 74). *Downside:* six to nine hundred lines of protocol code the project owns on its security surface, mitigated by the loopback-only binding, the upgrade checks, the size cap and the negative tests T9's acceptance already names. *Option 2 — `tungstenite`:* less to write; *downside:* ten crates (`sha1`, `base64`, `http`, `httparse`, `rand`, `utf-8`, `bytes`, `thiserror`, `log`, `data-encoding`) on the surface v1.1 publishes, a second RNG beside the sim's own discipline, and a second base64 and JSON path beside the proto crate's. *Option 3 — no dependency at all:* not available without `unsafe`. Also recorded, no decision needed: the gateway is not a walled crate, so it reads no clock (AGENTS.md §4.5, `clippy.toml`) — time enters it as the host's tick, the rate limiter counts per tick and per window of ticks, the audit log stamps tick and sequence number, `std::time::Duration` on a socket timeout is not a clock read, and the Lull timer stays on the client side of the wall.
- **Wave 3 opens (2026-09-15).** Three lanes from this `main` under the committed workflow (`.claude/workflows/wave-lanes.js`): **T8** on `feat/plan-core-t8` (`crates/plan-core`, `tests/golden/plan-core`; may uncomment the `pharmakos-verifier` workspace line), **T9** on `feat/gateway-t9` (`crates/gateway`, `tests/golden/gateway`; takes item 99's `getrandom` line as it stands), **T7** on `feat/sim-t7` (`crates/sim`, `tests/golden/pathing`, and the determinism and verifier goldens it re-blesses with the reasons). Merge order **T8 → T9 → T7** by `scripts/merge-train.sh`; T7 last because it moves the chain. T8 writes its estimator call site against item 61's frozen signature behind a trait of its own and wires it when T7 is on `main`; T9 makes no `gp.api.v1` change (a method that needs one goes back to the proto lane, plan §3 T9), so item 96(2)'s `Diagnostic.path` comment still waits for the next proto change.
- **Item 100, the questions the wave-3 lanes raised in their pull requests, decided on the recommendation under the owner's standing instruction of 2026-09-14.** (1) *The estimator's signature* (T7, PR #12): item 61's frozen signature is amended to what `crates/sim/src/pathing/estimate.rs` exports — the first parameter is the sim's `&Surface` rather than the world, item 90's `Speed` is a seventh parameter, and `Estimate` carries an additive `fogged` field; plan-core's `TravelEstimator::estimate(start, goal)` (PR #10) stays the seam, and the adapter that owns a surface, a cluster decomposition, a scratch and a speed and implements it lives in the gateway, T13's, because plan-core has no world. (2) *Counted work in benches* (T7): a bench target inside a deterministic crate is linted like the crate, so it reports steps, nodes and components, and a millisecond figure comes from a walled crate driving the sim from outside; AGENTS.md §4.5 now says so (PR #13). (3) *The exact transition bound at cluster 32* (T7): the 14 MB it costs is accepted for the skeleton; S3 revisits it against a measured fragmentation ceiling. (4) *The re-arm rule* (T7): a sealed-in unit re-asks the oracle on every tick whose repair rebuilt the components, wherever the repair was — the broad rule the module doc and the test name claimed is item 60's intent. (5) *`locomotion.repath_cap_per_tick` is a rule, not a knob* (T7): item 60's round robin makes the cap decide which unit repaths on which tick, so it is hashed state, off T2's knob list, and `the_repath_cap_is_a_rule_not_a_knob` says so. (6) *How a token reaches the gateway* (T9, PR #11): `Authorization: Bearer <64 hex>` on the upgrade, authenticated once per socket, and never a query string. (7) *A handshake with no `Origin` header is accepted* (T9): a non-browser client sends none and a browser always does, and the one it sends is checked; accepted as built and documented. (8) *`INTERNAL` for a method this build does not serve* (T9): recorded as one more input to the six derived error codes held open for T13 (`gateway.proto`'s `GatewayError.Code` PLACEHOLDER), not a new code. (9) *`tests/golden/gateway/` joins the frozen area list* (T9): a golden-format addition made at the skeleton, which is when the list is being frozen; `tests/golden/README.md` and plan §5 carry the row (PR #13). (10) *The next proto change's bundle*, owed by three T9 findings and one of T8's, taken as the opening commits of T13's lane in wave 4: `pharmakos_proto::json::base64` becomes public and the gateway's forty-line copy goes; `gp.api.v1.Digest` gains `repeated KindCount counts = 4` (`KindCount { string kind = 1; uint32 count = 2; }`) for item 97's per-kind counts; `gp.api.v1.Diagnostic.path` gets item 96(2)'s comment; and `json::canonicalise` is made to agree with `decode → encode` by *dropping* a field written at its proto3 default — the canonical form is the spelling both committed goldens (`tests/golden/proto/expected.expand_east.json`, `tests/golden/plan-core/**`) already have, so the fix is on `canonicalise` and whatever it moves is re-blessed with that reason. (11) *The plan-core goldens' licence* (T8): `tests/golden/plan-core/**` joins the permissive tier as `tests/golden/proto/` did — the canonical form is the file format an alternative tool must reproduce, and `expand_east`'s golden carries the example's own `MIT OR Apache-2.0` header through the round trip it exists to prove; `REUSE.toml`'s PLACEHOLDER narrows to `schema/` (T13) and `docs/` (T20) (PR #13). (12) *The plan's second T8 PLACEHOLDER* (T8): resolved by item 94's rules row; the provisional constant lives in `crates/verifier/src/limits.rs`, where the row is read from — recorded, nothing to add. Also recorded, no decision needed: T9 takes `pharmakos-proto`, `pharmakos-sim` (`default-features = false`) and `getrandom` only, and the `pharmakos-plan-core`/`pharmakos-verifier` workspace lines stay commented until T13 takes them; item 99's one dependency brings four crates (`getrandom` 0.4.3, `cfg-if`, `libc`, `r-efi`); the lesson T8's matrix paid for — a refusal decided by `std::path::Component` is host-dependent (a backslash is a separator on Windows and a filename byte elsewhere), so a name rule is decided on characters — and two smaller ones: `clippy::expect_used` bites plain helper functions in an integration-test file, and `gh pr checks` lists nothing for a few seconds after a push (the merge train now waits, `53bfd97`).
- **Wave 3 COMPLETE (2026-09-15).** Four pull requests, each rebase-merged under item 85 after the three-OS matrix, the DCO check and the cross-OS guard passed and two adversarial reviews were applied on the branch: **T8** PR #10 (`0eb9d5d…f82305f`, `crates/plan-core`: the lossless JSONC reader and writer with byte-for-byte round trip, the canonical form over the proto crate's codec, RFC 6902 patch with inverse-patch undo as a property, `render_plan`'s template prose, section-5 interface-time and `$`/`kW` projection, `instantiate_template` over the local folder, the `TravelEstimator` seam; seventeen findings, fifteen applied — the two blockers were one defect, a template-id rule decided by `std::path` that passed on Windows and failed on the other two legs; the major was `add` over an existing member losing that member's comments); **T9** PR #11 (`4d0b958…a6dad53`, `crates/gateway`: the hand-written RFC 6455 server on `127.0.0.1`/`::1` with `Host` and `Origin` checks, JSON-RPC 2.0 over the proto crate's `Json`, 256-bit tokens from `getrandom` compared in constant time, the scope table from T1's annotation with the four invariants asserted in code, the fog filter as a per-match policy, the tick-driven rate limiter and the audit log, the segment feed with fog-safe cursors and whole-segment digests carrying per-kind counts, the private match cache at item 98's location, the detail budgets; 131 unit and 35 integration tests; nineteen findings, sixteen applied — the majors were a forgeable audit line and a cursor that counted events a fogged seat could not see); **T7** PR #12 (`e21c806…17444d2`, `crates/sim`: HPA* at cluster 32 with one abstract level, the `(f, h, node_id)` heap, the union-find oracle, eager chunk-footprint repair, the round-robin repath cap, sealed-in as a state, the never-optimistic estimator, item 90's speed accumulator replacing the harness walk, snapshot v3 with route nodes and a checked route digest; nineteen findings, twelve of thirteen distinct items applied — the majors were the Scout's speed slot falling off a six-slot array and a restore that trusted the file's route digests); and the harness-docs PR #13 (`8655c08`: AGENTS.md §4.5, the `gateway/` golden row, plan §5, `REUSE.toml`). **The determinism chain moved, at tick 0 and on every line**, for T7's four stated causes: the router's table joined the encoding (walk state, speed accumulator, route length and cursor, a partial-route flag, the route's digest, the round-robin cursor); the harness walk was deleted for item 90's accumulator, so a unit stands on the column its route gave it; destinations are drawn at the end of the pathing phase rather than two steps out; and every unit's first route begins on the tick the round robin reaches it. `SNAPSHOT_VERSION` 2 → 3 re-blessed the 48 verifier report goldens, `report_hash` only. `rules_hash` did not move (`b31d8d0789eb207c`). **What the owner can see:** `tests/golden/pathing/expected.path-hashes.txt` — route hashes over the pristine map and after crater repairs, compared byte for byte on three operating systems; `tests/golden/plan-core/expand_east/expected.jsonc` — the example playbook reproduced byte for byte through load → canonicalise → save, comments intact, beside `expected.prose.txt` and the `awkward` case; `tests/golden/gateway/**` — one tick's events through each seat's fog filter, a segment digest, an audit log, a handshake. **Spend:** twelve agents, 3.61 M sub-agent tokens, 1 290 tool uses, 4.0 h wall (wave 2: 3.38 M, 938, 86 min); the wall time is mostly the fix passes watching the matrix. Wave 4 (T10, T13, T12, then T11) opens from this `main`.


- **Item 101, the build-from-scratch question and the cost of finishing (raised by the owner, 2026-09-20): reuse nothing, continue as planned, no process changes.** The owner asked whether to end this implementation and restart on an open-source base, and said plainly that cost is the binding constraint: “if we dont do it cheaper, i will cancel it”. Four surveys were run before answering.
  - **Engines and voxel technology — nothing to inherit.** OpenRA is integer-deterministic by the same design as ours (`WDist`, 1024-unit angles, LUT trig) but carries documented cross-OS desyncs (issues 17899, 8863, 20089, 21903) and claims no bit-exactness; its hierarchical pathfinder is welded to `World`/`Locomotor`/traits. Recoil/Spring is floats plus `streflop` to force IEEE compliance, with macOS/ARM exactness only an unofficial fork's claim, and Lua gameplay. 0 A.D. is the closest precedent and *validates* our design — `CFixed_15_16`, integer `Sqrt`/`atan2_approx`, `ComputeStateHash`, a serialisation test — and its recurring OOS-on-rejoin bug (state that is hashed but not serialised) is exactly what AGENTS.md §4.8's three-places rule prevents. Warzone 2100 (QuickJS), Widelands (Lua inside the sync boundary), Mindustry (snapshot authority, explicitly not deterministic) and the Rust rollback crates (GGRS, bevy_ggrs, lightyear — all of which *require* a deterministic sim rather than providing one) are rejected. No mature Rust RTS exists: `oxidator` is dormant since 2023.
  - **Voxel/meshing — adopt nothing, keep `crates/mesher`.** Zylann's `godot_voxel` is alive, MIT and Godot-4.7-ready, but its mesher runs only inside Godot, which breaks the property that makes ours testable: the crate links without gdext so `cargo xtask ci`'s geometry golden and the headless CPU proxy exercise the real mesher with no engine in the loop. Adoption would retire ~2 000 lines, add 700–1 000 plus a build-and-CI redesign, lose that check and inherit a single-maintainer upstream. `block-mesh-rs`, `building-blocks` and `ndshape` are dormant; Veloren, Luanti and `bevy_voxel_world` are engines or ECS-bound. Chunk LOD is ours either way. Read `binary-greedy-meshing` (MIT, active) and Transvoxel for technique when LOD lands; do not take the dependency.
  - **Dependencies — the lever is nearly spent, but one correction was overdue.** Waves 1–3 wrote the replaceable code and pinned it with tests and goldens, so swapping in `tungstenite` (a second RNG, a second hash, and `net.rs` loses the type that makes a LAN bind inexpressible), `serde_json` (float-backed numbers, a second JSON path), `fixed`/`rand`, `pathfinding` or `rkyv` would now *add* work. `proptest` is rejected outright: it pulls `rand`, `rand_chacha` and `rand_xorshift` into the crate that defines the RNG contract (§4.7); T11 reuses `crates/verifier/tests/fuzz.rs`'s stream-seeded generator instead. Two future adoptions stand: `dot_vox` 5.2.0 at T12/T16 with `default-features = false` (its optional `ahash` must stay out of `cargo tree`), already approved; and `lexopt` 0.3.2 (MIT, zero transitive dependencies) as T15's argument parser, saving ~200 lines while keeping our exit codes and error strings ours — *option not yet taken, owner decides at T15*; the alternative `clap` generates help but adds six to ten crates and a proc-macro build, and its `--help` output becomes a golden we do not control.
  - **The gdext version pin was wrong.** `Cargo.toml`'s commented line read `godot = "0.3"`, which predates the Godot 4.7 API; gdext 0.5 is the line targeting 4.7+, current 0.5.5 (MPL-2.0, GPL-compatible, as `imbl` already is; `godot-core` pulls `glam`, which is float maths and exactly why `client-gdext` is in `WALLED_PACKAGES`). Corrected to `0.5.5` here, before T12 opens, so the lane does not spend a cycle discovering it.
  - **The cost of finishing, measured.** Wave 2 delivered ~24 agent-days for 3.38 M sub-agent tokens and wave 3 delivered 26 for 3.61 M — 141 k and 139 k tokens per agent-day, agreeing within 1.5 %, for 63 475 lines and green three-OS CI (~850 lines/agent-day, ~165 tokens/line). Of the plan's 172 agent-days, T0–T9 = 75 are merged and 97 remain: **~19 M tokens all-in (range 14–22 M) over six more runs**, the Godot lanes (T12, T16, T19 = 29 days) carrying a ×1.3–1.6 penalty for untyped GDScript against a live engine. Beyond it: **~40 M to the fun gate (30–55 M)** and **120–200 M to v1**, both speculative — S1–S3 have no task breakdown, stages are gameplay rather than scaffolding, and S2 attaches an open-ended tuning loop to a stage exit through the adversarial alarm bands.
  - **Two options were offered and declined.** *A narrowed wave* — T10 + T11 + a thin T13 + a `gamectl` printer over the event feed, ~25 agent-days and ~4 M tokens, to read a three-minute Push as text and test the premise before buying the Godot half — and *~30 % off every wave at no quality cost* (Lens B to Sonnet, since it compares the branch against a written acceptance list rather than exercising judgement; one pre-extracted context pack per lane instead of four agents each re-reading AGENTS.md, the plan and the log; reviewer file:line evidence carried into the fix prompt). **The owner declined both: “im going to have faith and proceed by allocating tokens as i have them available to spare” — wave 4 runs as planned, with the wave-3 process unchanged.** *Downside accepted, stated plainly:* the gameplay premise stays unobserved until the fun gate, which is the plan's most expensive ordering, and every wave costs ~30 % more than it needs to. Recorded so the choice is legible later, not to reopen it.
  - **Not up for reconsideration, and why:** the three-OS matrix stays — T8's template-id rule decided by `std::path::Component` passed on Windows and failed on the other two legs in that lane's first week, which is the whole bug class. Lens A stays Opus — in wave 3 it found a Scout speed slot falling off a six-slot array, a restore that trusted the file's own route digests, and a forgeable audit line. PR bodies keep their per-finding apply/reject record and their chain-re-blessing rationale, which is what makes a moved golden reviewable at all.
- **Wave 4 opens (2026-09-20), as two runs of two lanes — plan §4's own order, not the handoff's three-then-one.** Process exactly as wave 3 (item 101): the committed `.claude/workflows/wave-lanes.js` unchanged, one builder, two adversarial lenses and one fix pass per lane, every agent Opus at high effort. **Run 1: T12** on `feat/client-gdext-t12` (`crates/client-gdext` + `godot/`, one ownership unit, item 72) **and T10** on `feat/sim-t10` (`crates/sim`; the determinism chain and, if `SNAPSHOT_VERSION` moves, the verifier goldens' `report_hash` lines, re-blessed with the reasons); train **t12 → t10**, T10 last because it moves the chain. **Run 2, once T10 is on `main`: T13** on `feat/gateway-t13` (`crates/gateway` + `crates/proto` + `proto/**`, opening with item 100(10)'s proto bundle) **and T11** on `feat/sim-t11`; train **t13 → t11**. *Why not the handoff's T13 + T12 + T10 at once:* plan §3 says T13 **needs T10**, and it does — the match host drives the runner, and `get_status`, `wait_for`, `get_briefing` and `get_recap` read the phases and the frozen segment-end snapshot T10 builds. Started beside T10, T13 would have written its host against a seam and a test double, the 14-call walkthrough would not have been “against a live gateway”, and a wire-up lane would have followed. Two and two is the same sixteen agents and the same wall-clock bound (a nine-day lane in each run), so it costs nothing and removes the rework. *Downside:* run 1 leaves the second slot idle, and the bundle's `Digest.counts` reaches T12's committed fixtures one run late — an additive field, and the fixtures are regenerated when the bridge meets the real gateway at T16. **One exception to standing rule 1 (plan §3: `xtask` and `.github/workflows/**` belong to no wave agent, at most one such change open at a time):** T12's own Builds line names `cargo xtask stage-client` and its Acceptance line names a CI leg that builds the cdylib, runs `--import` and asserts zero caught panics, so T12 carries both as the one such change open in wave 4, named as contract paths in its PR body; T10, T13 and T11 touch neither. T12 uncomments the `godot` workspace line as item 73 pins it, `godot = "=0.5.5"` — item 101 corrected the version in the comment, and the `=` is item 73's. No plan §7 decision is due; decision 21 (the sim-slip fallback) is checked at the wave's end.
- **Wave 4, run 1 merged (2026-09-20).** **T12** PR #15 (`…1dc2826`, `crates/client-gdext` + `godot/`: the Godot 4.7.2 project with the plan's five settings, the `.gdextension` for `windows.x86_64` and `linux.x86_64`, the bridge as marshalling only, path B with path A behind the `upload-path-a` build-time feature, item 53's four guards each with a test, the self-counting caught-panic guard at every `#[func]` boundary, `cargo xtask stage-client` with the `gdext_rust_init` byte-scan, and a `client extension` CI job on Windows and Linux that installs the pinned Godot, imports first and asserts zero caught panics; fifteen findings, two blockers and four majors, all applied — the blockers were one defect, the Windows client leg red because the install step published an MSYS path the next step could not see; the majors were that nothing executed the engine side of the upload, the owned-material guard had no test, and item 54's budget was carried but not applied). **T10** PR #14 (`…e548678`, `crates/sim`: the `Runner` with Lull / Push / recap, a twelfth tick phase `Match` between `Voxels` and `Hash`, the per-round length list with the coming segment's length in the frozen snapshot, the match-end one-tick rule, the round limit, local and seat elimination, the commander's respawn as per-Push state that completes by segment end, and the event bus as derived output with kinds a scenario can assert on; fourteen findings, no blocker, one major applied — the allocation test drove `World::step` by hand, so the whole match phase sat outside the one assertion that a tick allocates nothing). **The determinism chain moved on every line from tick 0** (the seat block's three new columns and the ninth, match-state block join the encoding) and `SNAPSHOT_VERSION` 3 → 4 re-blessed the 48 verifier report goldens, `report_hash` only; `rules_hash` did not move. No golden moved in T12. **Spend:** eight agents, 2.57 M sub-agent tokens, 1 270 tool uses, 2.6 h wall — 184 k per planned agent-day against waves 2 and 3's 140 k, the Godot lane's predicted penalty.
- **Item 102, the questions wave 4's first run raised, decided on the recommendation under the owner's standing instruction of 2026-09-14.**
  - (1) *`[profile.release]` sets `panic = "abort"`, under which the client's caught-panic counter is inert* (T12): `catch_unwind` catches nothing when panics abort, so in a release build a panic at a `#[func]` boundary takes the engine down instead of being counted. Option 1 **(taken)**: a client-only Cargo profile that inherits `release` with `panic = "unwind"`, added by T21's packaging PR — a `[profile.*]` change is a contract change and T21 is already one — and used for the cdylib alone; the headless binaries keep `abort`, because a sim that panicked must not keep running. Until then `stage-client` stages the debug library, where the guard is real. *Downside:* two release profiles to keep in step, and the workspace compiles twice for a full release. *Option 2 — `unwind` in `[profile.release]` for everything:* one profile; *downside:* a panicking sim thread can be caught and carried on from by a caller that should not, which is the failure `abort` exists to rule out. *Option 3 — ship the counter inert:* nothing to do; *downside:* the instrument T12's acceptance asserts on is dead in the build that ships.
  - (2) *Godot 4.7.2 segfaults at the end of a cold `--import` when an extension registers a class* (T12): bisected to the engine or gdext, not this crate; the artefact the import exists to produce is written by the crashing run. Accepted as built: `import_project` retries once and then checks that `.godot/extension_list.cfg` exists and names our extension, which is the question that matters. Revisit when item 73's pins move.
  - (3) *Nothing in the harness bans `client-gdext → pharmakos-sim`* (T12): `wall-guard` checks the other direction only, and `crates/client-gdext/tests/no_sim.rs` fills the gap for this crate. It becomes a general check beside `wall-guard` in T20's `xtask` half-week, as one of that PR's named changes; not before, because `xtask` changes are single-purpose PRs (plan §3 standing rule 1).
  - (4) *A second engine run under `--features upload-path-a`* (T12, a rejected review finding): not taken. It doubles the client job for the arm with the least engine surface; the two paths' byte-for-byte equality is proved on all three OS legs by `tests/geometry_paths.rs`.
  - (5) *A seat that was never placed is not eliminated* (T10): `Standing` / `Fallen` / `Unplaced`, and only `Fallen` is elimination — accepted as built. Spec section 3's rule is about a seat that lost its beacons, and a seat with no beacon row never had a core to lose.
  - (6) *`World::step` is the tick and `Runner` decides whether a tick happens* (T10): accepted as built, with the frozen snapshot taken by the `Runner` outside the tick because a capture allocates and a tick must not. A world stepped by hand stays in its opening Lull.
  - (7) *The round limit is a lobby setting, not a rules row* (T10): accepted — `DEFAULT_ROUND_LIMIT = 6` is the spec's default and carries its PLACEHOLDER to T22's lobby; tuning lives in the rules table and a host's choice does not.
  - (8) *Degenerate segment lengths are clamped, not refused* (T10): structure is refused at the snapshot door and values are normalised, because a default snapshot must stay restorable; a gateway that wants to *refuse* a degenerate lobby setting does so at the lobby, where there is a user to tell — T13's to note.
  - Also recorded, no decision needed: item 92's chunk transposition is a straight copy for today's two axis conventions, written as the relabel it is so a renamed axis fails a test with the explanation attached; the mesher's byte colours make the quantisation probe's choice immaterial today and the test says so; the `screenshot` job's gate moved from `godot/project.godot` to the vista golden's existence, since T12 created the former; and two halves of item 20 — the mandate ends, bound units retreat and re-home — are T14's, since neither a mandate lifecycle nor a home column exists yet.
- **Wave 4, run 2 merged (2026-09-21).** **T13** PR #16 (`…5af02f5`, `crates/gateway` + `crates/proto` + `proto/**`: item 100(10)'s proto bundle as the opening commit — `json::base64` public and the gateway's copy deleted, `Digest.counts` with `KindCount`, `Diagnostic.path`'s comment, `json::canonicalise` agreeing with decode → encode by dropping a field at its proto3 default, **which moved no golden**; then the match host as the one module that steps a `Runner`, the twenty-two methods of the plan's Builds list each with scope, phase gate, rate-limit class and audit line, item 100(1)'s `TravelEstimator` adapter owning its own `Surface`, `Clusters`, `Scratch` and `Speed`, `get_schema` served from the generated artefact with its golden under `tests/golden/schema`, draft continuity, the safe playbook filed for a seat that sealed nothing, and spec §12's 14-call walkthrough replayed against a live gateway **and** over the real WebSocket transport, ending with `submit_plan`'s `report_hash` equal to the FULL pre-check's; twenty-one findings, two blockers that were one defect and four majors, all applied — the blocker was the lane's own fix for a Lull that consumes no sim tick walking the gateway's tick backwards by a whole Lull at every Lull → Push boundary, which starved the rate limiter for the whole Push). **T11** PR #17 (`…638b583`, `crates/sim`: the playbook interpreter at the v1 vocabulary — a decision every 250 ms, one rule body at a time, the fixed 20 % reflex as the only interrupt with the same three exits a `move` has, the six route steps with their guards, late-bound selectors pinned per step, interface rows committing at the end of their own durations at the rules table's rates, `on_death` and `fallback`, all of it a tenth block of hashed state; five committed transcripts under `tests/golden/interpreter`, a 96-playbook stream-seeded halting corpus in place of `proptest`, and a 3 600-tick scenario chain for `expand_east`; thirty-two findings, two blockers that were one — nothing had been committed and the 48 verifier report goldens were not re-blessed — and twelve majors, all applied but three rejected with reasons). **Contract changes carried by the merges:** `GetMapSummaryResponse.map_seed` is deprecated and `string match_seed = 4` replaces it, because a `uint32` cannot carry the 64-bit match seed the map is a function of (the held range shrinks from 4–15 to 5–15 and `buf.yaml`'s per-change waiver is re-scoped to `gateway.proto`); `tests/golden/schema/**` joins `REUSE.toml`'s permissive tier; `SNAPSHOT_VERSION` 4 → 5 re-blessed the 48 verifier report goldens, `report_hash` only; fourteen new append-only event kinds (ids 13–26) and two new golden line formats. **The determinism chain moved on every line from tick 0** (the interpreter block joins the encoding — measured by commenting the block out, which reproduces `main`'s tick 0 exactly), from tick 1 because commanders stop drawing harness destinations, and from tick 31 of the first draft's chain because the harness playbook deploys a beacon; `rules_hash` did not move. The expected rebase conflict in `tests/golden/verifier/**` did not happen: T13's canonicalise fix moved nothing there and the two lanes share no file. **What happened instead was a clean rebase that did not compile:** T13's gateway reads the sim's event bus exhaustively, T11 added fourteen kinds, and the train stopped on three red legs; the main session added one commit to T11's branch (`fix(gateway): carry the interpreter's event kinds, private to their own seat`, `crates/gateway/src/strings.rs` and `surface.rs` only), ran `cargo xtask ci` green on the rebased tree and restarted the train from t11. The lesson for the train: two lanes that share no file can still share a type, so the second lane of a run is rebuilt locally against the first's merge before its CI is waited on. **Spend:** eight agents, 3.20 M sub-agent tokens, 1 514 tool uses, 3.8 h wall — 188 k per planned agent-day; wave 4's two runs together are sixteen agents, 5.77 M tokens for 31 planned agent-days, 186 k per agent-day against waves 2 and 3's 140 k.
- **Item 103, the questions wave 4's second run raised, decided on the recommendation under the owner's standing instruction of 2026-09-14.**
  - (1) *A sealed playbook does not reach the sim yet* (found by the main session at the merge, not by either lane): T13 built against a `main` whose sim had only the `PlaybookInterpreter` seam, T11 then added `Plan::compile` and `World::seal_playbook`, and `Runner` exposes neither a world handle nor a seal call — so the gateway seals a submission in its own private store, the hosted world never hears of it, and every commander stands still. Option 1 **(taken)**: a one-lane wire-up run before wave 5 opens, **T13b** on `feat/gateway-t13b`, owning `crates/gateway` and the one method it needs in `crates/sim`: `Runner::seal_playbook(seat, plan)`, legal in a Lull and refused in a Push or a recap; `submit_plan` compiles a playbook the verifier qualified and, on a `PlanError`, leaves the previous seal alone and answers a method error from the existing closed set naming the construct — a playbook that is *valid* and that *this build cannot execute* is not an invalid playbook, so it is not a `qualifies:false` report, and it is never discovered at `begin_push`; `Surface::begin_push` seals every seat's plan, the filed safe playbook included, before the runner leaves the Lull; and an end-to-end test that a submitted playbook moves the commander with `plan_sealed` on the feed. Every `PlanError` reachable past a qualifying report is listed in that PR as a verifier gap with the stage that closes it. *Downside:* an unplanned lane, about half a million tokens, and two doors visible to a client until S3 closes the gaps. *Option 2 — fold it into T15:* no extra lane; *downside:* T14 owns `crates/sim` in the same wave, so either two agents share a crate or T15 builds `scenario run` against a seam again, which is the rework the two-runs split existed to avoid. *Option 3 — leave it to T19's integration:* nothing now; *downside:* wave 5's `scenario run` and watch rig would show a match in which no order is executed, and the premise the skeleton exists to test stays untested for two more waves.
  - (2) *Are handlers evaluated once the guaranteed tail has taken over?* (T11, an explicit owner question): **yes**, as built. Spec section 10 lists rules with resume semantics and a guaranteed tail as two separate things a playbook has and nowhere says the first stops when the second starts; `max_fires` and the cooldown floor still bound it, so every playbook halts. *Downside:* a rule can pull a commander out of its fallback a bounded number of times. *The other reading:* the tail is final; *downside:* an author's flee rule silently stops working the moment the route runs out, which is an invented rule.
  - (3) *The spec's flagship `expand_east` playbook cannot complete its first step on the skeleton's map* (T11, a tuning finding): the commander walks east for its whole 120 s timeout without arriving, the site is then out of placement range, and the tail names `b_01`, which is seat 1's core and `not_own` to seat 0. No tuning value moves now — walk speed against map span is the owner's at S1 with this measurement in hand — and the committed scenario stays as the honest record it is. A second scenario whose per-seat playbooks complete a visit and a deploy on the skeleton map goes to **T15**, which owns the scenario runner; item 81's templates are instantiated against the seat's own snapshot, so a fixed `b_NN` in a shared playbook is a property of the worked example and not of what T18 will generate.
  - (4) *Accepted as built in T11:* a `move` to a voxel is a move to the ground under it (the router stands a walker on a column, so the author's `z` could never be arrived at); every selector arm and a fixed `b_NN` answer over the seat's **own** living beacons, and a live beacon of another seat fails with `not_own`; a voxel off the map, an enum value this build does not name and a rules table with no `interface_times` or `verifier` block are refused at `Plan::compile` rather than defaulted; the beacon table's ceiling is seats + 40, computed by one function at construction and at a restore; the plans are inputs and are not hashed, the interpreter's state is. `DEFAULT_ON_FAIL = SKIP` and `max_deaths_before_fallback = 0` reading as *no limit* stay PLACEHOLDERs to S3, because the spec is silent and proto3 cannot tell absent from zero.
  - (5) *Deferred, additive:* a `step_started` line's `value` is a route index or a rule-body index and the line does not say which — T15's assertion vocabulary decides whether it needs the split; a save carries no plan fingerprint, so resuming into a different playbook is not refused — T17, beside the rules hash in the save's stamp; a `Stream::Harness` for the test corpus is not added, since nothing the generator draws reaches sim state and the stream enum is a contract.
  - (6) *The 14-call walkthrough is a gateway test with a committed transcript, not a scenario file* (T13): a scenario is a headless match written down and item 97 freezes its assertion vocabulary; a method-call script is not a match. Accepted.
  - (7) *T9's six derived `GatewayError` codes* (T13): five fire and are tested; `UNSUPPORTED_SCHEMA_VERSION` is unreached in this build and stays for the day a converter can fail before a report exists; no seventh was needed. The PLACEHOLDER comment closes in the next `gateway.proto` change, which is T14's forecast fields.
  - (8) *The gateway's tick during a Lull* (T13): derived clock-free from `rules.match.lull_ms` and the host's remaining figure, carried forward as a monotonic offset. Accepted as built — and checked at the merge that `set_phase_remaining_ms` is a host-side Rust call and not a JSON-RPC method, so a seat cannot inflate its own rate budget by lying about the timer.
  - (9) *Two sentences AGENTS.md is missing* (T13): §3 rule 2 names `plan-core` and the verifier and never the gateway, so the crate that hosts the match is held to “no dry runs” only by its own `tests/confinement.rs`; and nothing says the host must give the gateway ticks during a phase the sim does not advance. Both are written at **T20's** harness half-week, with item 102(3)'s general check, as that PR's named changes; until then they ride in every gateway-touching lane's extras.
  - (10) *Who may read the interpreter's event lines* (decided by the main session at the merge, because T13's default would have answered it by accident): the seal, the step, rule, reflex and visit lines and the fallback are `Audience::Private` to the seat whose commander it is; `beacon_placed` is a world event and fog decides it. Those lines carry step and rule indices, which are the shape of a sealed playbook, and AGENTS.md section 7 says a playbook never leaves the gateway for another seat — under T13's default each would have been a world event placed at the origin, visible to anyone with eyes on voxel (0, 0, 0) and to everyone in a no-fog match. `Surface::audience_of` now has one arm per kind so the next kind cannot default. *Downside:* a no-fog spectator and a later shared replay cannot narrate an opponent's orders from the live feed; if S7's watch view wants that after the match, it is a deliberate recap-time disclosure and not a feed default. *The other option — world events owned by the seat:* simpler, and the watch rig would show both commanders' steps; *downside:* it leaks the opponent's playbook structure mid-match, which is the one thing the secrecy rule exists to stop. T13b's Lens A is asked to attack this decision.
  - (11) *Also recorded, no decision needed:* the four methods `gateway.proto` gives no request/response pair (`query_area`, `list_known_enemies`, `get_reports`, `get_capabilities`) answer `INTERNAL` until the stage that introduces what each reports on; `get_schema`'s golden needed no `xtask` change; `plan-core`'s `context::segment_length_ms` PLACEHOLDER is discharged; `REUSE.toml`'s PLACEHOLDER narrows to `tests/golden/docs/` at T20; plan §7 decision 21 (the sim-slip fallback) is **not invoked** — T10 and T11 both landed inside their runs.
- **Wave 4, run 3 merged (2026-09-21): item 103(1)'s wire-up.** **T13b** PR #18 (`…8538901`, `crates/gateway` and `crates/sim/src/runner.rs` with its test, nothing else): `Runner::seal_playbook(seat, plan) -> Result<(), SealRefused>`, legal in a Lull and refused with a typed answer in a Push, a recap and an ended match, additive in the literal sense — the diff deletes no line of the runner — consuming no tick and leaving the frozen planning snapshot and its identity hash alone; `submit_plan` compiles a qualifying playbook at the door with `Plan::compile`, a pure function of the playbook and the rules table, and holds the compiled plan with the seal; `Surface::begin_push` refuses out of phase before touching anything, files the safe playbook for a seat whose seal is not this round's, opens the segment's feed and seals every seat in ascending id through `Host::seal_plans` — all of them or none — before the runner leaves the Lull; and end-to-end tests over a live `Surface` and once over the WebSocket in which **a commander moves**, read from unit positions and not from the feed. **No golden moved**: the determinism chain, `SNAPSHOT_VERSION`, the verifier goldens and the walkthrough transcript are byte-identical. Ten findings, no blocker, one major, all applied — the major was that the confinement test's new needle was the runner's `seal_playbook` while the call a handler would actually write is this crate's `seal_plans`, so the guard did not guard. **Spend:** four agents, 0.96 M sub-agent tokens, 417 tool uses, 2.8 h wall — about twice the half-million item 103(1) estimated, because the lane measured every row of the two-door table rather than reasoning from neighbours.
- **Item 104, the questions the wire-up lane raised, decided on the recommendation under the owner's standing instruction of 2026-09-14.**
  - (1) *The code for a playbook that qualifies and that this build cannot execute is `INVALID_ARGUMENT`* (T13b): accepted as built. The closed set has no word for it; `INTERNAL` is defined as never the caller's doing, `NO_QUALIFYING_PLAN` already means the seat sealed nothing, and `UNSUPPORTED_SCHEMA_VERSION` fits one variant only. The message carries `PlanError`'s own words, which name the construct and the stage that gives it an effect, and a test asserts both per row. *Downside:* a client branching on the code alone cannot tell a wrongly typed param from a construct that arrives at S2; it has to read the message, and the gap closes stage by stage rather than by a new code.
  - (2) *The two doors disagree on ten constructs today, and the table that says so fails when either door moves* (T13b, `crates/gateway/tests/methods.rs`'s `two_door_cases()`): `recycle`, `add_build_target`, `remove_build_target`, Survey probe areas and Build targets or protected areas close at **T14**; Defend settings, Attack settings and `beacon_under_attack` at **S2**; `queue_structure` at **S4**; more than 64 handlers at **S3**. Because the table is a gateway test and T14 is a sim lane, **T14 is granted three named places in `crates/gateway` for wave 5 and no more**: the rows of `two_door_cases()` it closes, and an arm in `strings::event_text` and in `Surface::audience_of` for each event kind it adds — both match exhaustively, so a new kind does not compile without them, which is run 2's lesson applied before the wave rather than at its train. The rest of `crates/gateway` is T15's for the wave if `scenario run` needs the host to grow.
  - (3) *`TooManyHandlers` is reachable past a qualifying report* (T13b, a review finding that overturned the lane's own argument): 65 handlers with empty bodies cost 66 size units against a budget of 128, so the editor's meter shows a playbook well inside budget that this build will not run. Whether an empty handler body is legal at all, and how `MAX_HANDLERS` relates to `verifier.size_budget_units`, join T11's PLACEHOLDER on both constants at **S3**; `TooManySteps` is unreachable under the same budget, reasoned from the two numbers and flagged as reasoned.
  - (4) *A restored runner needs its plans sealed again from the gateway's private store* (T13b): the gateway has no restore path yet, so this is a PLACEHOLDER on `Surface::plans_for_round` naming **T17**, beside item 103(5)'s plan fingerprint.
  - (5) *Item 103(10) was attacked and stands*: Lens A found no interpreter kind on the wrong side of the line, `beacon_placed` stays the one that fog decides, and the digests' per-kind counts were already taken after the fog filter — true before this lane and untested at the method level, now pinned over a real two-seat Push under both fog policies by counting that the two seats' pages sum to the bus exactly.
  - (6) *Also recorded, no decision needed:* `Surface::attach` now refuses a host whose world does not cover every seat the surface was built for; the AGENTS.md sentence item 103(9) books to T20 gains a clause — compiling a playbook is not driving a match — and `tests/confinement.rs` holds both lines until then; `UnknownEnum` is not reachable through a JSON door at all, because proto3 JSON spells an enum by name and the decoder refuses an unknown one before a report can exist.
- **Wave 4 COMPLETE (2026-09-21).** Five lanes in three runs, each rebase-merged under item 85 after green three-OS CI and two adversarial reviews: T12 PR #15, T10 PR #14, T13 PR #16, T11 PR #17, T13b PR #18; `main` = `8538901` plus this entry; every lane worktree and branch is removed and no PR is open. **The plan's milestone row is met on both halves:** the spec's 14-call walkthrough passes against a live gateway, over the real transport, ending with `submit_plan`'s `report_hash` equal to the FULL pre-check's; and the extension loads in Godot 4.7.2 on a fresh checkout with zero caught panics, on Windows and Linux in CI. **What the owner can see beyond the row:** a playbook submitted through the gateway is compiled, sealed at the end of the Lull and executed — `cargo test -p pharmakos-gateway --test methods two_seats_submit` moves two commanders under two different sealed orders; `tests/golden/interpreter/*/expected.transcript.txt` reads as a commander's decisions five ticks apart; `tests/golden/gateway/walkthrough/expected.walkthrough.txt` is the spec's session call by call. **What it does not show yet, said out loud:** the spec's flagship `expand_east` cannot finish its first step on the skeleton's map (item 103(3)), the economy charges nothing (T14), and nothing is drawn but the client check scene (T16). **Spend:** twenty agents, 6.73 M sub-agent tokens, 3 201 tool uses, 9.2 h of lane wall-clock over two days — 186 k per planned agent-day for the four planned lanes against waves 2 and 3's 140 k, plus 0.96 M for the unplanned wire-up; the Godot and gateway lanes run hotter than the sim's, which is the number to budget wave 5's T16 and wave 6's T19 with. **Plan §7 decision 21 (the sim-slip fallback) is not invoked:** T10 and T11 both landed inside their runs, so there is no additive `math` crate and no second agent in the sim. **Two process lessons, both applied from wave 5 on:** a task's *Needs* line is checked against `main` before its lane is launched, not the handoff's summary of it (T13 needed T10; the wave became two runs); and the second lane of a train is rebuilt locally against the first's merge before its CI is waited on, because two lanes that share no file can share a type (T11 on T13's exhaustive matches) — and where the shared type is known before the wave, the lane that moves it is granted the named places that must follow (item 104(2)).
- **Wave 5 OPEN (2026-09-21), on the owner’s word (“resume”) after the wave-4 close.** Three lanes in one run, every *Needs* line checked against `main` = `1340179` and met: T14 (`crates/sim` part 6, 10 d), T15 (`crates/gamectl`, 8 d), T16 (`godot/` + `crates/client-gdext`, 10 d). Process is item 101’s, unchanged; budget 28 agent-days ≈ 5.2 M sub-agent tokens at wave 4’s 186 k per agent-day; the lanes build cold because every lane cache under `D:\build` was deleted at the wave-4 close (35.8 GB). Ownership and the train order t16 → t14 → t15 are item 104(2)’s and the handoff’s.
- **Item 105, the wave’s opening decisions, taken on the recommendation under the owner’s standing instruction of 2026-09-14.** **(1) T14’s `$` and `kW` rows ride as T14’s opening commits; there is no rows-first PR.** Checked against `main` before deciding: T5a’s revision 3 already carries nearly every number T14’s PLACEHOLDER line names — `economy.bmi_dollars`, `starting_bmi_multiplier`, the scaling percents, `recycle_refund_percent`, `salvage_percent`, `backlog_threshold_per_drone`, the ore yields; `power.core_surplus_kw`, `generator_output_kw` by richness, `kw_per_unit`, `revive_margin_kw`, `beacon_base_draw_kw`; `beacon.sphere_radius_voxels`, `commander.placement_range_voxels`, the locomotion costs per second, and every unit’s and structure’s cost, HP and draw — so a T5a-style PR would be close to empty. A row T14 finds genuinely missing goes in as its **first** commit (`proto/gp/v1/rules.proto`, `rules/rules.v1.json`, the regenerated tree, the revision bumped), with the new `rules_hash` recorded from that commit alone so the PR body can separate “the table moved the hash” from “behaviour moved the chain”; a number nothing in T14’s acceptance needs stays a named constant with a PLACEHOLDER (AGENTS.md §12). *Options and downsides:* rows first as their own PR (T5a’s precedent) gives the cleanest separation and lets the other two lanes start on the final `rules_hash`, but costs a fourth lane (0.4–1 M tokens under item 101’s process) to move a set that mostly exists, and the set would be guessed before the builder knows it, so a second table change would likely follow anyway; riding along (chosen, T13’s precedent) makes T14’s PR carry a `proto/**` contract change beside its hashed-state one, which the commit order and the PR body have to keep legible. **(2) The economy forecast’s response fields do not land this wave.** `GetEconomyForecastResponse` is filled by plan-core’s `$`/`kW` projection through the gateway’s knowledge handler, and neither crate’s forecast path has an owner in wave 5; fields landed now would be a shape guessed ahead of its only producer, and a proto field is forever. They land with the lane that fills them (S1’s economy work, or T19 if the wizard needs them first). *Downside:* `get_economy_forecast` keeps its documented stub through the skeleton, and the `GatewayError.Code` comment of item 103(7) stays open until the next `gp.api.v1` change, whoever makes it. **(3) `lexopt` 0.3.2 is approved for `crates/gamectl` and for no other crate** (an owner decision under AGENTS.md §3 rule 5). It has no dependencies, no proc macros and an MIT licence, and it handles what a hand-rolled parser gets wrong (`--`, combined short flags, non-UTF-8 paths on Windows). *Options and downsides:* `clap` brings a large tree, proc macros, build time and cargo-deny surface for five subcommands; a std-only parser like `xtask`’s costs nothing to approve but re-makes those edge cases in a tool whose exit codes and messages a player reads; `lexopt` generates no `--help`, so `gamectl` writes its help by hand from the one string table. T15 adds the `[workspace.dependencies]` line (root `Cargo.toml`, named in its PR body); AGENTS.md §3’s approved list gains the name in the next harness-docs PR. **(4) `xtask` is expected to need no change this wave.** Read before launch: `step_scenario` turns itself on when a `gamectl` binary with a `scenario` subcommand exists, and `step_screenshot` when `godot/project.godot` and `tests/golden/vista/expected.vista.png` exist, loading `res://scenes/vista_shot.tscn`. T15 and T16 therefore build to those names. If either finds the step cannot work as written, the change is T15’s to carry as a named contract path (the plan gives T15 the one `xtask` PR); T16 reports what it needs and the main session sequences it after T15’s, because one `xtask` change is open at a time. **(5) The first vista golden is rendered by CI’s Linux leg, not on the Windows host**, since the step compares under xvfb + lavapipe only: T16 pushes, downloads the leg’s `actual.vista.png`, looks at it, and commits it as the golden with what it shows written in the area’s README; the main session looks at it again before the merge.
- **Item 106, a hole in the plan under T16, found by checking T16’s *Needs* against `main` rather than against the plan’s wording (wave 4’s first lesson), and what the wave does about it.** **(1) What is missing.** T16 is written as if three things existed, and none does, in the plan, the log, the spec or the code: *a view feed* — no `gp.api.v1` method returns terrain voxels, dirty chunks or unit, commander and structure positions (`query_area` answers `INTERNAL`, item 103(11)), while `client-gdext` may depend on `godot`, `pharmakos-proto` and `pharmakos-mesher` only, and plan decision 17’s “the sim exposes a borrowable chunk view; client-gdext performs the conversion” presumes an edge that T12’s acceptance and item 102(3) forbid; *a host for the client* — the spec’s architecture makes the Godot client a JSON-RPC client over the loopback WebSocket and the gateway the in-process host of the sim, and nothing says which process that is or who starts it (there is no binary but `gamectl`, whose `main.rs` is a stub); *a pacing owner* — nothing outside tests calls `Host::step`, no method steps a match, and the gateway rightly reads no clock, so T16’s 2–4× speed and skip have nothing to act on. The demo of plan §1.1 needs all three (the Push is watched, the commander walks, a Generator goes up, edited voxels remesh). **(2) The wave becomes two runs, as wave 4 did.** Run 1 now: T14 and T15, which the hole does not touch. In parallel a design step writes the missing task into the plan as **T16a** (the view feed, match control and the host entry: `proto/gp/api/v1` + `crates/gateway`) and its decisions are logged as item 107 before its lane launches as run 1’s third lane. Run 2: T16, the client, after T16a merges. *Cost, said out loud:* about 8 unplanned agent-days, ≈ 1.5 M tokens at 186 k, taking the wave from 5.2 M to about 6.7 M. *Options and downsides:* launching T16 as written would have spent a 10-day lane discovering the hole and inventing a private answer inside the client, which is the expensive way to make a contract; feeding the vista from a client-side copy of the map generator (what spike G1 did) breaks the client→sim ban and the server-side fog the spec requires, and would be thrown away; deferring the view to S1 would leave the skeleton’s demo unwatchable, which is the one thing a walking skeleton is for. **(3) “The client path”, for T15’s parity acceptance, is defined now** because T15 launches now: a match hosted by the gateway’s `Host` behind a `Surface`, with each seat’s playbook arriving through `submit_plan` over a live loopback WebSocket session and sealed by `Surface::begin_push` — what `crates/gateway/tests/websocket.rs` and `two_seats_submit` already do. `gamectl scenario run` hosts through the same `Surface` and `Host` in process (the crate map gives `gamectl` no edge to the sim), and the parity test asserts its per-tick chain (`TickReport::hash`) equals the chain of that transport-driven session for the same seed, rules and playbooks. **(4) Ownership changes that follow.** `crates/gateway` beyond T14’s three named places (item 104(2)) is T16a’s, not T15’s: read before deciding, `Surface::{new, attach, open_lull, call, begin_push, step, end_recap}` and `TickReport` are already public, so `scenario run` needs no gateway change; if T15 finds otherwise it reports the exact need and stops short of editing the crate.
- **Item 107, the design of T16a, taken on the recommendation under the owner’s standing instruction of 2026-09-14, after a design step of one designer, two adversarial critics (the rules; cost and scope) and one reviser.** The section is in `skeleton-plan.md` §3; every option with its downside, T16’s amendment and the notes for T15, T17, T18, T19, T21, T22 and S7 are in `docs/design/skeleton-plan-t16a-notes.md`. **(1) Generated terrain is served whole at the skeleton; what is fogged is what changes it and what stands on it, voxel-exactly.** The design step marked this as the owner’s sentence, so it is flagged to the owner rather than buried: it is taken on the recommendation, the wire shape is identical under either answer, and switching costs about 1.5 agent-days. It adds no rule — `main` already serves the match seed to every seat as “not a secret”, the map is a pure function of seed and rules, and the estimator prices every leg as known terrain — and real terrain fog arrives with S3’s knowledge store as one mask feeding the view and the estimator. *Downsides:* the spec leans the other way (“unexplored ground”, “fogged voxels use last-known state or ×1.5 cost”); vent and seam sites are visible from round 1 (as they are to anyone who runs the generator on the public seed). *Rejected:* voxel-exact terrain fog now (+1.5 d, and the first Lull’s map is an island with the vent undrawn, because the vent is 28–44 voxels out and the core sphere is 24); chunk-granular fog (a sight rule 31 voxels wide that the spec does not state); hiding the seed (re-opens a merged T13 answer). **(2) The process model: the Godot lobby spawns a child process.** All logic is a gateway library module, `serve.rs`; the binary is `gamectl host`, about thirty lines, a named place in run 2. One config line goes in on the child’s stdin and one announce line (port, match id, admin token, the human seat’s token) comes back on its stdout pipe; the binary refuses to run with a terminal on stdout; the host exits when its stdin closes. No token touches argv, disk or a console. *Downsides:* a second process to package on two systems (T21); an internal two-line format that is not `gp.api.v1`; `session.rs` splits in two. *Rejected:* linking the gateway into the client (breaks the crate map and item 102(3)); a `[[bin]]` in the gateway (cannot link the operator at T18); a second extension (a workspace split); control over stdin (a second protocol outside scopes, limits and audit). **(3) The client paces, through four `admin` methods: `end_lull` 90, `advance_push {ms}` 91, `end_recap` 92, `report_host_clock {elapsed_ms, remaining_ms}` 93.** Game milliseconds only: no tick and no state hash ever reaches the wire (the proto’s own transport note, and a hash would hand the full state hash to the human seat’s process). Speed is how much game time the pacer asks for; skip is `advance_push {60 000}` repeated until the footer leaves `PUSH`; arguments out of range are refused, never clamped. The clock report is phase-neutral and bounded, because otherwise every token has 8 calls for a whole recap and for ever after match end — where the full-map unlock lands — and its answer carries `all_ready`, the one bit the lobby needs for “Ready ends the Lull”. *Downsides:* four forever method names only a lobby calls; match pace follows the client’s frame loop (its outcome does not, and that is tested); outside a Push the limiter’s clock is what the local lobby says it is, bounded per call. *Rejected:* the host pacing itself (a clock in a wall-guarded crate). **(4) The confinement rule changes, and says so:** no handler may step or seal, *except* the admin-scoped control handlers in `surface/control.rs`, which drive only the live match through `Surface`’s three driving methods and may name none of `Runner`, `Host`, `World`, `host_mut`, `seal_plans`, `seal_playbook`, `snapshot`, `.clone()`; `tests/confinement.rs` is rewritten to that rule and a descriptor-driven test asserts each control method needs `admin`. **(5) The view feed is one read method, `get_view` = 71 (`observe`), cursor-driven:** an empty cursor is a keyframe, any other is “what changed since”. Chunks are materials only, run-length encoded in the sim’s voxel order, one codec in `pharmakos_proto::chunk_rle`, the material table and order written into the proto comment and pinned to the sim’s by a gateway test; a chunk is in a delta iff *its bytes for that viewer* changed, so an unseen edit produces nothing at all and the unlock is just the viewer becoming unfogged. Entities are the complete visible list, built by inclusion, with opaque viewer-scoped ids (a dense row id is a count), `owner` spelt `seat.0`, whole-voxel positions, and **no** HP, power state, heading, destination, step, mandate, treasury or count. Stamps are the feed’s own `view_seq`, never a sim tick, because visibility changes in a Lull and a recap, which consume none. *Downsides:* per-viewer derived state in the gateway, rebuilt on restore (T17); full entity lists are 20–30 KB a poll at S2’s 300 units, so S2 adds an opt-in delta mode; a byte format inside `bytes` is a contract `buf breaking` cannot see. **(6) The production `Vision` is `host::SphereVision`, a labelled stopgap:** own, alive beacon spheres at `beacon.sphere_radius_voxels`, the one sight rule the spec states (“spheres give passive vision”) and the table numbers. After T14 merges, a small sim follow-up replaces it with `World::in_own_sphere` and adds Survey-lite’s sightings. `FogPolicy::eliminate` gets its first caller. **A defect on `main`, logged and not taken here:** the casual policy has no phase term, so a casual match is unfogged in the Lull too; the fix is `FogPolicy::unfogged(seat, phase)` for every fog-filtered method, at hardening or T20, because it re-blesses T13’s goldens and runs through T14’s named place. **(7) A host-side test seam, reached by no wire method:** `Host::file_voxel_edit` and `Host::file_damage`, because nothing on `main` edits a voxel or kills a seat in a hosted match and three acceptance lines need one. T16’s two hash-chain tests move to T16a, where the chain can be read in process. **(8) One committed keyframe fixture,** `tests/golden/gateway/view_keyframe/expected.keyframe.jsonl` with a readable twin, produced and drift-checked by the gateway; T16 commits a guarded byte-identical copy under `godot/fixtures/` for the hostless vista shot. No `xtask` change. **(9) Lane mechanics: 10 agent-days, one pull request by default.** The reviser recommended two (view and control, then the host loop) so T16’s vista could start earlier; the main session takes one, because a second review cycle is four more agents and token spend comes first — *downside:* T16 waits for the whole of T16a, about three lane-days of wall-clock. The seam stays as the fallback. **The rule for the one binary all three lanes regenerate:** T16a’s proto change is its first commit and alone in it; whichever lane merges later rebases, *regenerates* `crates/proto/src/generated/descriptor.binpb` and never hand-merges it, and re-blesses its schema-derived goldens with that reason. **(10) Harness-docs asks, for the wave’s harness-docs PR:** AGENTS.md §3 rule 2 naming the match host and its control handlers; the `gamectl` row gaining `host` (approved now, because run 2 needs it); the `proto` row gaining the voxel run-length codec; and the `gamectl` row’s edge to `pharmakos-operator`, which is decided at T18’s opening and not before. **(11) Seen read-only in T15’s worktree by the design step:** `gamectl` was taking a dependency on `pharmakos-sim`, because `Host::open` takes the sim’s `WorldConfig` — the gateway gives `gamectl` no way to host a match without naming a sim type. T16a therefore adds `Host::open_from(rules_json, seed, seats, settings)`; the main session moves `scenario run` onto it and removes the edge at T15’s local rebase, which the train order already puts last. **(12) Cost, said out loud.** The design step spent 0.84 M tokens over four agents against the 0.35 M the main session expected; it found three blockers the first draft would have shipped (ticks on the wire, raw ids and enemy state, acceptance lines that could not run). T16a at 10 days is about 1.9 M. The wave is now about 7.9 M against the handoff’s 5.2 M.
- **Item 108, the owner's interview on wave 5's open decisions (2026-09-21), one item at a time, each answer logged as given.** **(1) Generated terrain at the skeleton: served whole — the owner's answer, confirming item 107(1).** Every seat sees the whole generated map from the first Lull; enemy units, beacons, structures and voxel edits appear only inside own beacon spheres until the match-end unlock. *Options put and their downsides:* terrain whole (chosen) — the spec leans the other way, vent and seam sites are visible from round 1, real terrain fog waits for S3; fog terrain now — +1.5 agent-days, the first Lull's map is an island with the starting vent undrawn, protects nothing while the seed is public, S3 redefines it; fog terrain and hide the seed — re-opens a merged T13 answer and its goldens, scenario files name the seed, and the skeleton has no scouting loop to uncover ground with. The PLACEHOLDER stays: terrain fog's unit and memory are S3's, as one mask feeding the view and the estimator. **(2) The host is a child process — the owner's answer, after asking for the question to be investigated further, confirming item 107(2).** The Godot lobby spawns `gamectl host`; config goes in on its stdin, the port and tokens come back on its stdout pipe, and it exits when the game does. *What the investigation found.* Measured on this machine (Godot 4.7.2, Windows, `OS.execute_with_pipe` with `blocking = false`): the handshake round trip takes 150–190 ms, the child exits 20 ms after the pipe is closed and 0.6 s after the Godot process is hard-killed, so there is no orphan; Linux is not yet measured and is T16's to confirm in CI. From sources: Godot spawns the child with no console window and, since 4.4, inherits only the pipe handles; there is no half-close (none is needed); reads must be polled non-blocking; on Linux Godot's pipe descriptors leak into any LATER child, harmless while the lobby spawns one; Godot's macOS exporter has a `helper_executables` slot, so notarisation costs one more hardened-runtime executable; SmartScreen does not check a `CreateProcess` child, though unsigned indie executables do draw antivirus false positives; shipped precedent is Barotrauma and Don't Starve Together (recurring pain: "server failed to start"), while the in-process integrated server (Minecraft, Veloren, Luanti, Factorio, OpenTTD) is the commoner pattern. *Options put and their downsides:* child process (chosen) — one more unsigned executable for antivirus and notarisation, a "host failed to start" failure to handle, Linux pipe behaviour still to confirm; a second extension in the same process — a new workspace crate, the unfogged world in the render process's memory with compile-time separation only, and a forced choice on panics that the repo's rule (item 102(1): anything owning the sim aborts, "a sim that panicked must not keep running") makes ugly — abort closes the game window without a word, unwind leaves a dead sim to detect; child now and re-decide at T21 — throws away about a day of lobby code if it switches. `serve::run` takes any reader and writer, so the second-extension route stays additive, and a headless `gamectl host` is what v1.1's agents will want in any case. **(3) No pause control at the skeleton — the owner's answer.** The watch rig ships what the spec names: 1×, 2–4× and the instant skip to segment end. A client-paced match can be paused for free and pausing moves no hash, but the spec says "the human alone decides speed-up and skip" and never names a pause, so it is a question for the fun gate and not something a lane adds because it costs nothing. *Consequence for T16:* the pacer accumulates game time from elapsed wall time, not from frames drawn, so an unfocused or minimised window does not slow a Push beyond the pacer's backlog bound — which is also what keeps the playtest metric "Push time focused vs unfocused" meaningful. *Options put and their downsides:* no button (chosen) — a player who must step away can only skip or let it run; a pause button now — a rule the spec does not state, and a little less of "orders you cannot take back"; pause only while unfocused — erases the focused-vs-unfocused signal the fun gate wants. **(4) The main session stops and asks before run 2 — the owner's answer, against the recommendation (launch automatically).** When T16a, T14 and T15 are merged, the main session reports what landed and what it cost and waits for the owner's go before launching T16 (about 1.9 M tokens). This is a checkpoint on spend, not a change to item 101's process, and it does not re-open the declined savings. *Options put and their downsides:* launch automatically — 1.9 M committed with no checkpoint; stop and ask (chosen) — the wave idles until the owner answers and the lane caches go cold; stop, and prepare a compact first — the same, plus re-reading state afterwards. *The main session's reading, not the owner's words:* where a wave grows beyond what the owner said go to, ask before spending on the growth; T16a itself was launched without that checkpoint, under the standing instruction, and the owner was told at once. **(4, corrected by the owner the same day: “i made a mistake with answer 4. follow your recommendation and launch automatically”.)** Run 2 launches on the main session’s own word as soon as T16a, T14 and T15 are merged, under item 101’s unchanged process; the report of what run 1 landed and cost is still written, and is not a gate. The main session’s reading above — ask before spending on a wave’s growth — is withdrawn with the answer it was read from; the standing instruction of 2026-09-14 stands as it was.
- **Item 109, wave 5's run 1 merged (2026-09-22), and the decisions its three pull requests raised, taken on the recommendation under the owner’s standing instruction of 2026-09-14 and open to the owner’s overrule.** **(1) What merged.** **T16a** PR #20 (`3b067bb`): the view feed (`get_view = 71`), the four admin control methods (`end_lull 90`, `advance_push 91`, `end_recap 92`, `report_host_clock 93`), `serve::run` and `Host::open_from`, `pharmakos_proto::chunk_rle`, and the keyframe golden at `tests/golden/gateway/view_keyframe/expected.keyframe.jsonl` (288 chunks, 201 558 bytes of runs, one 256 KiB page; not cut). **T14** PR #21 (`af95103`): the economy, the grid, the mandate layer, programs and Survey-lite. **T15** PR #19 (`cbf07cf`): `gamectl` and `scenario run`. **Harness** PR #22: item 107(10)’s four AGENTS.md asks, the approved list (`getrandom`, `lexopt`), (3), (7) and (8) below, and `proto/buf.yaml`’s spent waiver deleted as its own comment instructs (T16a’s note: T16a was a proto change that discharged no held number, so the block should have gone then). **(2) How run 1 was recovered, and a rule for next time.** An API outage (529 Overloaded) on 2026-09-21 killed T14’s builder, T16a’s builder and T15’s fix pass. Both runs were resumed with a note appended only to the dead agent’s prompt (`c436cb7`). *What that got wrong:* the workflow cache replays only the unchanged **prefix** of agent calls, in call order, so the note on T14’s builder, the run’s first call, sent every later call live, including T15’s finished build and both its reviews. *Rule from now:* when one agent of a multi-lane run dies, re-run that agent alone, with its original prompt and the resume note, unless it is the first call in the run’s order. Then the previous session ended while T14’s fix pass ran its final full suite, after both fix commits and before the push. The main session finished that pass itself, because what remained was mechanical: it rebased onto T16a, fixed the one interaction (T14 added `DamageOrder::by`, and T16a’s test support builds a `DamageOrder`; `SeatId::NEUTRAL`, because the helper’s damage is nobody’s), ran the full suite, wrote the Review section from the two reviews and the fix commits, and pushed. The D: drive that holds the build targets dropped out for a while the same day; `wave-lanes.js` already takes a `build_root` argument for that. **(3) `gamectl` keeps its edge to `pharmakos-sim`.** Item 107(11) expected `Host::open_from` to remove it; it does not, because the gateway’s API is written in the sim’s types and `gamectl` names about a dozen of them (`RulesTable`, `SeatId`, `Ms`, `Tick`, `MatchSettings`, `WorldConfig`, `Plan::compile`, `EventKind`, `SeatEconomy`, `Snapshot`, `hex`). The edge is taken with `default-features = false` in rule 1’s own form, AGENTS.md’s crate map now lists it, and `gamectl` joins `xtask`’s `GUARDED_PACKAGES` and `WALL_GUARDED_PACKAGES` now instead of at T20, as T15 asked. `gamectl` drives a match only through the gateway’s `Host` and `Surface`, and its own `tests/confinement.rs` holds that. `scenario run` stays on `Host::open`: moving it to `Host::open_from` removes nothing while the edge stays. *Options put and their downsides:* keep the edge and guard it (chosen) — the CLI compiles the sim, as plan-core and the verifier already do; a re-export module in the gateway — a second public face for the sim’s types and the same compiled graph; gateway-owned wrapper types — a large duplicate API for one client. **(4) T15’s harness seal door stays as built.** `examples/playbooks/expand_east.jsonc` cannot be submitted on the skeleton map by either seat (`E0401`, `E0403`), so `expand-east-segment` plays only because the runner seals a playbook the verifier refused. Every such run prints a `note` line, and `tests/scenarios.rs` pins the door to exactly that scenario. *Options put and their downsides:* keep the door (chosen) — a harness can seal what no client could, loudly; refuse — the scenario step goes red on a committed file; change the scenario’s seats — T11’s record and T14’s re-bless both move; delete the scenario — the only scenario pinning an interpreter refusal goes with it. The door comes out when the example is re-authored for a map where it is legal, at S1. **(5) T14’s open rule questions stay PLACEHOLDERs, routed to the owner.** None is ruled here, because each belongs to the stage that tunes it; the built behaviour is the recommendation in each case. *A unit’s program comes from its kind, not its home beacon’s writ* (S1, with the mandate’s settings): gating on the writ would idle the starting mining drone, which item 90 homes to a core on Build, and leave the skeleton with no income. *The commander walks through a brownout and still draws* (S1, with the grid): parking it would make a total blackout a lockout. *An anchor dropped from a Build list strands what it paid for* (S2, with ruins). *No probe area is recorded as visited* (S3, with the reach window). *“One Generator per vent” is judged by grade and patch span* until a hashed vent table exists (S1). **(6) Deferred to T20’s harness half-week:** AGENTS.md’s status paragraphs are stale (“twelve steps”, scenarios “not in `cargo xtask ci` today”, no hash chains to compare), and are refreshed together rather than piecemeal. **(7) AGENTS.md section 4.8 gains T14’s clause:** widening the snapshot moves every verifier `report_hash` golden, because the fixture snapshot is one of that hash’s inputs. **(8) The `cargo xtask ci` job’s limit goes from 45 to 90 minutes.** `windows-latest` ran the suite in 41 minutes on PR #21 and was cancelled at 45 on PR #19, whose headless matches add several minutes; nothing failed, and macOS and Linux finished well inside it. The limit guards against a hang and is not a budget; the suite’s time budget, and which tests pay for it, are T20’s to set (a PLACEHOLDER in `ci.yml`). **(9) What run 1 cost.** Measured on the Workflow tool’s counter, the one waves 2 to 4 were measured on: T16a 1.82 M (0.72 M before the outage, 1.11 M after); T14 and T15 4.04 M (1.91 M before, about 2.13 M after, estimated from the transcripts because that run’s counter was lost when the session ended); 5.86 M in all, plus the 0.84 M design step. That is about 209 k tokens per agent-day over the plan’s 28, against wave 4’s 186 k; most of the difference is the outage, which re-ran T15’s build and reviews and paid for three agents twice.
- **Item 110, wave 5 closed (2026-09-23): run 2, T16, merged, and the decisions its pull request raised, taken on the recommendation under the owner’s standing instruction of 2026-09-14 and open to the owner’s overrule.** **(1) What merged.** **T16** PR #26 (`bd8ae2b`): `gamectl host` (the named place, naming no sim type and refusing a terminal standard output); in `crates/client-gdext` the view feed’s one decode entry (`view_apply`, `chunk_rle` through the palette, transposed, keyed by origin, the six neighbour borders T12 left unwired now wired), the pacer (`pacer.rs`, the one wall clock: elapsed time × speed owes game milliseconds, one `advance_push` in flight, the host clock reported outside a Push, the keep-alive) and the connection scheduling (`rig.rs`); in `godot/` the lobby that spawns `gamectl host` with `OS.execute_with_pipe` and holds the two tokens in variables only, the vista, the watch rig (1×, 2×, 4× and skip; no pause, item 108(3)) and the headless `watch_check` scene; and three goldens under `tests/golden/vista` (the PNG, the geometry digests, the event list). **Item 108(2) is confirmed on Linux:** the `client extension` job now runs `watch_check` against a real `gamectl host` on Windows and Linux, idles 35 s with no dropped connection, plays a segment at 4× to the recap with no caught panic, and checks that no `gamectl` outlives the client. **The vista golden** was rendered on CI’s Linux leg, looked at by the lane, and looked at again by the main session before the merge (item 105(5)): the whole generated map from above its north-east corner, one unbroken diamond with no cracks, no missing chunk and no back-face holes, as the area’s README describes; a same-runner re-render differed in 0 of 921 600 pixels. **(2) Recovery.** The session ended while the builder was waiting on its last CI run, after it had pushed all six build commits and before it opened the pull request. The builder is the run’s first call, so item 109(2)’s rule allowed the plain resume: same run id, a note on the builder only, and the reviews and fix pass ran live after it. The D: drive dropped out again; the fix pass built on C:. **(3) Contract paths accepted in the lane’s own pull request.** T16 changed `.github/workflows/ci.yml` (the `screenshot` job’s Godot install filled, the extension staged, a bootstrap render while no golden exists, an `xvfb-run` version-probe shim, `if-no-files-found` flipped to `error`, its limit 30 to 45 minutes; the `client` job’s watch run and orphan check) and `REUSE.toml` (the fixture’s annotation), and one line of `crates/gamectl/tests/confinement.rs` outside its named place (the new `host.rs` added to the files the test scans). Kept in the one pull request rather than split into a CI PR: each change is what the file itself assigned to T16 or what the brief asked for, the PR names them, and a split would have added a matrix run for nothing. *Options put and their downsides:* accept (chosen) — a lane PR that carries CI changes, against plan section 3’s standing preference for single-purpose CI PRs; split — one more PR and one more three-OS run, same content. **(4) Booked for T20’s harness half-week.** `xtask`’s `tool_available` probes a tool with `--version`, which `xvfb-run` does not have, so the screenshot step called an installed `xvfb-run` missing: probe it by path, then delete the shim. `cargo xtask screenshot` skips before rendering when no golden exists and never stages the extension: give it a render-only mode and a staging pre-step, then delete the bootstrap and staging steps from `ci.yml`. AGENTS.md: the `client-gdext` row should name the pacer, the Lull timer and the connection scheduling as the sanctioned walled exception to “it marshals; it does not decide” (the notes already rank above the row, and `tests/no_arithmetic.rs` exempts `pacer.rs` by name); a sentence should say the live gateway’s answers are not canonical proto JSON (the `_status` footer, lower-case enum values per item 80) and the client translates them; and a named-place grant should list the crate’s confinement test. These join items 102(3), 103(9), 104(6) and 109(6). **(5) Booked for the next gateway lane, beside the `SphereVision` replacement.** `serve::Config` sets no upper bound on the seat count, so `gamectl host` hosts a 99-seat match when asked; v1 allows three. Refuse seats outside 1 to 3 as `INVALID_ARGUMENT`. The lobby hard-codes two seats, so nothing reaches it today. **(6) Left for the owner at T16’s demo review, as PLACEHOLDERs in code:** the pacer period (100 ms), its backlog bound (5 000 game ms) and the speed set ({1, 2, 4}; is 3× a speed?); the keep-alive interval (5 s) and the host-clock cadence (250 ms), with `READ_TIMEOUT` at hardening; the easing, derived facing, camera and the Pall’s values; the lobby’s match settings (the golden seed, two seats, round limit 6); where a packaged client finds `gamectl`, the rules and the library (T21). Decision 22, the screenshot tolerance, is still recommended and not logged; G1’s thresholds held at 0 pixels on the first real vista. The Lull’s length is not on the wire, so the rig reads `match.lull_ms` from the client’s pinned rules copy, as the T16a notes’ section D expected. **(7) What wave 5 cost.** Run 2: about 1.54 M (0.72 M before the session ended, estimated from the transcript; 0.82 M on the Workflow counter after). Wave 5 in all: 7.40 M across 38 planned agent-days, about 195 k per agent-day, against wave 4’s 186 k; with the 0.84 M design step, 8.24 M against the 7.9 M estimate of item 106. **(8) Wave 5 is closed.** Wave 6 is next: T17 save and resume with the private replay, T18 the Easy operator and the safe playbook, T19 the editor wizard (plan section 3, row W6).
- **Item 111, wave 6's opening (2026-09-23): the holes found by checking T17, T18 and T19 against `main`, a design step, and the decisions taken for the wave, on the recommendation under the owner’s standing instruction of 2026-09-14 and open to the owner’s overrule.** **(1) What was found.** Checking each task’s *Needs* against `main` rather than against the plan’s wording (item 106’s lesson) found that none of the three is buildable as written. No wire path carries the operator’s suggestion or a “why” to the wizard, templates declare no parameters, and `render_plan` answers one prose string, so a chip has nothing to hang on (H1). The operator’s only seam, `serve::BuiltInSeat`, takes the gateway’s own `Request`, and the “gp.api.v1 service traits” AGENTS.md section 3 names do not exist (H2). The safe playbook filed on a miss is one constant per host, not the seat’s own (H3). T17’s save lives in the gateway, which has no restore path, and sealed playbooks are not on disk at all: `MatchCache::seat_directory` has no caller, so notebooks, drafts and seals live only in memory (H4). The design step found eight more: a built-in seat cannot finish a round under the rate limit, because a Lull’s tick moves only on a clock report (H8); nothing a client can call returns `$` or `kW`, so neither the operator nor the demo’s meter can read them (H9, H10); `Setup.built_in` is sized before the config line is read (H11); the operator’s tuning rows are on no wire (H12); the constant is not the spec’s safe playbook (H13); a new `BeaconSummary` field breaks the Godot unit’s test literals (H14); an advisor’s `get_view` writes into the human’s per-viewer feed (H15); and the cache cannot resume (H16). Item 84 had already answered plan decision 13 (build save and resume); it does not mention the private replay, which rides T17’s plan line only. **(2) The design step.** One designer, two adversarial critics (the rules; buildability, cost and scope) and one reviser, as item 107’s, for 0.90 M tokens. Its result, `docs/design/skeleton-plan-w6-notes.md`, is adopted without change: where it and plan section 3’s T17, T18 and T19 lines disagree, it wins. The plan gains a `### T18a` section (the notes’ A1) and an amendment line under each of the three. **(3) The decisions** (the notes’ section C gives every option and its downside). C1: a template declares its parameters in `gp.v1.Meta.parameters = 6` (pointer and label, untyped until S6). C2: `instantiate_template` gains `suggested` and answers the filled parameters and a “why”, computed by an advisor at each Lull’s start for every seat with no built-in operator. C3: chips move to S3; the rule list is `render_plan`’s prose lines. C4: the operator depends on `pharmakos-proto` alone and drives a call closure that `gamectl host` adapts, so rule 3 becomes a dependency fact. C5: each seat’s safe playbook is the operator’s, filed after its own FULL verify with the constant as the audited fallback, and `get_safe_plan` answers the seat’s own. C6: in-process tokens get their own rate limit, and Easy’s call budget is derived from its evaluation units. C7: `GetEconomyForecastResponse` 1–4 as present-state values, and `BeaconSummary`’s owner plus own-only core, priority and powered — **a departure from item 105(2), taken in the open**, because the safe playbook cannot tell that power is short without them and the demo row promises a meter. C8: saves are automatic at item 84’s Lull boundaries (at `begin_push`, and on control end-of-file in a Lull), one slot per match, and a sealed save resumes into its Push. C9: resume is the config line’s seventh field. C10: a gateway-owned JSON save container; `SNAPSHOT_VERSION` does not move. C11: the private replay is inputs only (sealed files, per-segment hash files), its log a PLACEHOLDER for S7. C12: `World::in_own_sphere`, as items 107(6) and 110(5) name it. C13: a flat `library/` at the repository root, MIT OR Apache-2.0. C14: two runs. C15: no random draw at Easy. C16: the safe template is the spec’s, the constant becomes its nothing-raised form, and `deploy-and-visit`’s chain moves once, in T18a, for that stated reason. C17: the operator reads the public rules text for its tuning rows. **(4) Five owner questions, taken on the recommendation and flagged here so they are not buried** (the notes’ section D, “for the owner now”). An in-process seat gets its own rate limit (the alternative, a resumable operator, costs about 1.5 d). A save made at a Push’s start resumes into that Push (the alternative lets a player take back sealed orders by killing the client). The advisor holds a `plan`-scoped in-process token on the human’s seat, never `plan.submit`, behind a method allow-list (the alternative, no advisor for the human, saves about 2 d and loses the spec’s pre-fill). Scouts and sightings stay out of the live view, as item 108(1) says, although item 107(6) read otherwise. Saving is automatic where the spec says the host “can” save. Each changes a lane’s brief if overruled; the first and third touch run 1. **(5) The lane plan.** Before run 1 the main session lands a struct-update base on the two `BeaconSummary` literals in `crates/client-gdext/tests/fixtures.rs` (H14, in this item’s pull request). Run 1: T18a (`proto/**`, plan-core, the gateway, `library/`, `gamectl host.rs`’s library path; a contract PR) and T19 PR 1 (the Godot unit), merged t19a then t18a. Between the runs: T18a merged, then a harness-docs PR (the operator row, the `gamectl` → operator edge, rule 3’s sentence on in-process rates, rule 2’s `file_advice`, `library/`’s licence, `buf.yaml`’s ignore block deleted) and the item recording run 1. Run 2, launched automatically after (item 108(4)): T17 (sim and gateway; a contract PR), T18 (operator and `gamectl`’s named place), T19 PR 2, merged t17, t18, t19b, with `library/` frozen. **(6) Cost.** 34.75 agent-days against the plan’s 22 (T18a 8.75, T19 PR 1 5.5, T17 6, T18 8, T19 PR 2 6.5): about 6.8 M tokens at wave 5’s 195 k per agent-day, 7.5–8.2 M if the Godot lanes run dear, plus this step’s 0.90 M. The plan’s total moves from 182 to about 194.75 agent-days. Five cuts, in order, would bring the wave to about 30.25 d (5.9 M): no advisor for the human (−2 d), defer the replay (−0.75), operator-defined parameters (−0.75), the meter to S1 (−0.5), Resume to T22 (−0.5). None is taken; the estimate is reported, not a gate (item 108(4)). **(7) Housekeeping.** `wave-lanes.js` now asks lane commits for the model that runs the lanes, Opus 5.5, where it named an older one, and its header shows the `scriptPath` form it is launched with. Item 93’s “item 80” for the three templates means item 81 (item 80 is the JSON-RPC casing).
